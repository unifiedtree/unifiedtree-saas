package com.hrms.fnf.enums;

/**
 * Whether a settlement component adds to the gross payable (EARNING)
 * or subtracts from it (DEDUCTION).
 */
public enum FnfComponentType {
    EARNING,
    DEDUCTION
}
