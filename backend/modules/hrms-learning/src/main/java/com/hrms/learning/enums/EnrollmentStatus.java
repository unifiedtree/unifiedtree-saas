package com.hrms.learning.enums;

/**
 * Lifecycle of an employee's enrollment in a training program:
 * ENROLLED → COMPLETED, or DROPPED if the employee leaves the program.
 */
public enum EnrollmentStatus {
    ENROLLED,
    COMPLETED,
    DROPPED
}
