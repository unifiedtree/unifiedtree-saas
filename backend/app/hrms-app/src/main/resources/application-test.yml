spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:tc:postgresql:16:///hrms_test}
    username: test
    password: test
  flyway:
    clean-on-validation-error: true
  kafka:
    bootstrap-servers: ${TEST_KAFKA_SERVERS:localhost:9093}
  data:
    redis:
      host: localhost
      port: 6370

logging:
  level:
    com.hrms: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
