// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component ApprovalCard.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrAvatar, HrButton, HrStatusPill } from '@/shared/components/hr'
import { arr, txt } from './dc-runtime'
import './ApprovalCard.view.css'

export function ApprovalCardView({ v }: { v: any }) {
  return (
    <>
      <article style={{display: "flex", flexWrap: "wrap", gap: "14px 20px", alignItems: "flex-start", padding: "16px 18px", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "14px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}} className="dc-approval-card-0">
        <div style={{flex: "1 1 220px", minWidth: "0", display: "grid", gap: "10px"}}>
          <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px", flexWrap: "wrap"}}>
            <HrAvatar name={v.r?.name} sub={v.r?.sub} />
            <HrStatusPill tone={v.r?.tone}>
              {txt(v.r?.statusLabel)}
            </HrStatusPill>
          </div>
          <dl style={{margin: "0", display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(110px,1fr))", gap: "8px 14px", fontSize: "13px"}}>
            {arr(v.r?.facts).map((x: any, $index: number) => (
              <Fragment key={$index}>
                <div>
                  <dt style={{fontSize: "11px", fontWeight: "700", letterSpacing: ".06em", textTransform: "uppercase", color: "#64748b"}}>
                    {txt(x?.k)}
                  </dt>
                  <dd style={{margin: "3px 0 0", fontWeight: "600", fontVariantNumeric: "tabular-nums"}}>
                    {txt(x?.v)}
                  </dd>
                </div>
              </Fragment>
            ))}
          </dl>
          <p style={{margin: "0", fontSize: "13px", color: "#334155", lineHeight: "1.5"}}>
            <span style={{color: "#64748b"}}>
              {"Reason:"}
            </span>
            {" "}{txt(v.r?.reason)}
          </p>
          <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "8px 14px", fontSize: "12px", color: "#64748b"}}>
            <span>
              {"Raised "}{txt(v.r?.raised)}
            </span>
            {v.r?.hasAttachment ? (
              <>
                <button type="button" onClick={v.r?.onAttachment} data-tip="Opens attachment preview" style={{display: "inline-flex", alignItems: "center", gap: "5px", background: "none", border: "0", padding: "0", font: "inherit", fontWeight: "600", color: "#0f6e56", cursor: "pointer"}}>
                  {txt(v.icFile)}{" View attachment · "}{txt(v.r?.attachment)}
                </button>
              </>
            ) : null}
          </div>
          {v.r?.hasNote ? (
            <>
              <p style={{margin: "0", padding: "8px 10px", borderRadius: "8px", background: "#f8fafc", border: "1px solid #f1f5f9", fontSize: "12.5px", color: "#475569"}}>
                <strong style={{color: "#0f172a"}}>
                  {"Decision note:"}
                </strong>
                {" "}{txt(v.r?.note)}
              </p>
            </>
          ) : null}
        </div>
        {v.r?.pending ? (
          <>
            <div style={{flex: "0 1 260px", minWidth: "0", display: "grid", gap: "8px", marginLeft: "auto"}}>
              {v.canDecide ? (
                <>
                  <textarea value={v.note} onChange={v.setNote} rows={2} maxLength={300} placeholder="Decision note (optional)" aria-label="Decision note" style={{font: "inherit", fontSize: "13px", padding: "8px 10px", border: "1px solid #cbd5e1", borderRadius: "8px", resize: "vertical", outline: "none", color: "#0f172a"}} className="dc-approval-card-1">

                  </textarea>
                  <div style={{display: "flex", gap: "8px", justifyContent: "flex-end"}}>
                    <HrButton variant="ghost" size="sm" onClick={v.reject} data-tip="Rejects this request">
                      {"Reject"}
                    </HrButton>
                    <HrButton size="sm" onClick={v.approve} data-tip={v.approveTip}>
                      {txt(v.approveLabel)}
                    </HrButton>
                  </div>
                </>
              ) : null}
            </div>
          </>
        ) : null}
      </article>
    </>
  )
}
