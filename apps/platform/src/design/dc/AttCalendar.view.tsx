// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component AttCalendar.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrButton } from '@/shared/components/hr'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { arr, txt } from './dc-runtime'
import { SectionState } from './SectionState'

export function AttCalendarView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "flex", flexWrap: "wrap", gap: "16px", alignItems: "flex-start", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        {v.isError ? (
          <>
            <div style={{flex: "1 1 100%", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", padding: "8px"}}>
              <SectionState kind="error" retry={v.retry} />
            </div>
          </>
        ) : null}
        {v.isLoading ? (
          <>
            <div style={{flex: "1 1 100%"}}>
              <SkeletonBlock {...(v.sk || {})} />
            </div>
          </>
        ) : null}
        {v.showCal ? (
          <>
            <section aria-labelledby="cal-title" style={{flex: "1.7 1 440px", minWidth: "0", display: "grid", gap: "14px", padding: "20px 22px", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
              <div style={{display: "flex", flexWrap: "wrap", alignItems: "flex-start", justifyContent: "space-between", gap: "10px 18px"}}>
                <div>
                  <h3 id="cal-title" style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "800"}}>
                    {txt(v.monthLabel)}
                  </h3>
                  <p style={{margin: "4px 0 0", fontSize: "13px", color: "#64748b"}}>
                    {txt(v.bestLine)}
                  </p>
                </div>
                <ul style={{display: "flex", flexWrap: "wrap", gap: "6px 14px", margin: "0", padding: "0", listStyle: "none", fontSize: "12px", color: "#475569"}}>
                  {arr(v.legend).map((l: any, $index: number) => (
                    <Fragment key={$index}>
                      <li style={{display: "inline-flex", alignItems: "center", gap: "6px"}}>
                        <svg width="12" height="12" aria-hidden="true">
                          <rect x="0.5" y="0.5" width="11" height="11" rx="3.5" fill={l?.color} stroke="rgba(15,23,42,.1)" />
                        </svg>
                        {txt(l?.label)}
                      </li>
                    </Fragment>
                  ))}
                </ul>
              </div>
              <div aria-hidden="true" style={{display: "grid", gridTemplateColumns: "repeat(7,minmax(0,1fr))", gap: "6px", fontSize: "11px", fontWeight: "700", letterSpacing: ".06em", textTransform: "uppercase", color: "#64748b", textAlign: "center"}}>
                <span>
                  {"Mon"}
                </span>
                <span>
                  {"Tue"}
                </span>
                <span>
                  {"Wed"}
                </span>
                <span>
                  {"Thu"}
                </span>
                <span>
                  {"Fri"}
                </span>
                <span>
                  {"Sat"}
                </span>
                <span>
                  {"Sun"}
                </span>
              </div>
              <div style={{display: "grid", gridTemplateColumns: "repeat(7,minmax(0,1fr))", gap: "6px"}}>
                {arr(v.cells).map((c: any, $index: number) => (
                  <Fragment key={$index}>
                    {c?.blank ? (
                      <>
                        <span aria-hidden="true" />
                      </>
                    ) : null}
                    {c?.isDay ? (
                      <>
                        <button type="button" onClick={c?.onClick} aria-pressed={c?.sel} aria-label={c?.aria} data-tip={c?.tip} style={{display: "block", width: "100%", padding: "0", border: "0", borderRadius: "12px", background: "none", font: "inherit", cursor: "pointer"}}>
                          <svg viewBox={v.cv?.vb} aria-hidden="true" style={{display: "block", width: "100%", height: "auto", fontFamily: "Inter,sans-serif", fontVariantNumeric: "tabular-nums"}}>
                            <rect x={v.cv?.inset} y={v.cv?.inset} width={v.cv?.w} height={v.cv?.h} rx={v.cv?.r} fill={c?.bg} stroke={c?.stroke} strokeWidth={c?.sw} />
                            <text x={v.cv?.tx} y={v.cv?.ty1} textAnchor={v.cv?.anchor} fontSize={v.cv?.fs1} fontWeight="800" fill={c?.fg}>
                              {c?.day}
                            </text>
                            <text x={v.cv?.tx} y={v.cv?.ty2} textAnchor={v.cv?.anchor} fontSize={v.cv?.fs2} fontWeight="800" fill={c?.fg}>
                              {c?.main}
                            </text>
                          </svg>
                        </button>
                      </>
                    ) : null}
                  </Fragment>
                ))}
              </div>
            </section>
            <aside aria-live="polite" style={{flex: "1 1 280px", minWidth: "0", display: "grid", gap: "14px", alignContent: "start", padding: "20px 22px", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
              <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "8px"}}>
                <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "800"}}>
                  {txt(v.d?.title)}
                </h3>
                {v.d?.hasChip ? (
                  <>
                    <span style={{padding: "3px 10px", borderRadius: "999px", background: "#f1f5f9", fontSize: "12px", fontWeight: "700", color: "#334155"}}>
                      {txt(v.d?.chip)}
                    </span>
                  </>
                ) : null}
              </div>
              {v.d?.isWork ? (
                <>
                  <div style={{display: "flex", flexWrap: "wrap", alignItems: "flex-end", gap: "6px 12px"}}>
                    <strong style={{fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "34px", fontWeight: "800", lineHeight: "1", fontVariantNumeric: "tabular-nums"}}>
                      {txt(v.d?.present)}
                    </strong>
                    <span style={{fontSize: "13.5px", color: "#475569", paddingBottom: "3px"}}>
                      {"of "}{txt(v.d?.expected)}{" expected came in"}
                    </span>
                    {v.d?.isGreat ? (
                      <>
                        <span style={{marginLeft: "auto", padding: "4px 10px", borderRadius: "999px", background: "#059669", color: "#fff", fontSize: "12px", fontWeight: "800", whiteSpace: "nowrap"}}>
                          {txt(v.d?.rate)}{" · Great"}
                        </span>
                      </>
                    ) : null}
                    {v.d?.isGood ? (
                      <>
                        <span style={{marginLeft: "auto", padding: "4px 10px", borderRadius: "999px", background: "#a7f3d0", color: "#065f46", fontSize: "12px", fontWeight: "800", whiteSpace: "nowrap"}}>
                          {txt(v.d?.rate)}{" · Good"}
                        </span>
                      </>
                    ) : null}
                    {v.d?.isLow ? (
                      <>
                        <span style={{marginLeft: "auto", padding: "4px 10px", borderRadius: "999px", background: "#fde68a", color: "#92400e", fontSize: "12px", fontWeight: "800", whiteSpace: "nowrap"}}>
                          {txt(v.d?.rate)}{" · Needs a look"}
                        </span>
                      </>
                    ) : null}
                  </div>
                  <ul style={{margin: "0", padding: "0", listStyle: "none", display: "grid", gap: "10px"}}>
                    {arr(v.d?.rows).map((r: any, $index: number) => (
                      <Fragment key={$index}>
                        <li style={{display: "grid", gap: "5px"}}>
                          <span style={{display: "flex", justifyContent: "space-between", gap: "8px", fontSize: "13px", color: "#334155"}}>
                            <span>
                              {txt(r?.label)}
                            </span>
                            <strong style={{fontVariantNumeric: "tabular-nums", color: "#0f172a"}}>
                              {txt(r?.n)}
                            </strong>
                          </span>
                          <svg width="100%" height="8" aria-hidden="true" style={{display: "block"}}>
                            <rect width="100%" height="8" rx="4" fill="#f1f5f9" />
                            <rect width={r?.w} height="8" rx="4" fill={r?.color} />
                          </svg>
                        </li>
                      </Fragment>
                    ))}
                  </ul>
                  <HrButton onClick={v.d?.open} data-tip={v.d?.openTip}>
                    {"Open this day’s logs "}{txt(v.icChevron)}
                  </HrButton>
                </>
              ) : null}
              {v.d?.isOff ? (
                <>
                  <p style={{margin: "0", padding: "14px", borderRadius: "12px", background: "#f8fafc", fontSize: "13.5px", lineHeight: "1.5", color: "#475569"}}>
                    {txt(v.d?.offText)}
                  </p>
                </>
              ) : null}
              {v.d?.isFuture ? (
                <>
                  <p style={{margin: "0", padding: "14px", borderRadius: "12px", background: "#f8fafc", fontSize: "13.5px", lineHeight: "1.5", color: "#475569"}}>
                    {"This day hasn’t happened yet. Come back once it’s over."}
                  </p>
                </>
              ) : null}
              {v.d?.isNone ? (
                <>
                  <p style={{margin: "0", padding: "14px", borderRadius: "12px", background: "#f8fafc", fontSize: "13.5px", lineHeight: "1.5", color: "#475569"}}>
                    {"No attendance was recorded for this day."}
                  </p>
                </>
              ) : null}
            </aside>
          </>
        ) : null}
      </div>
    </>
  )
}
