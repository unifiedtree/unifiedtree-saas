// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component ShiftRequests.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrAvatar, HrButton, HrDrawer, HrStatusPill } from '@/shared/components/hr'
import { EmptyState } from '@/shared/components/EmptyState'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { arr, txt } from './dc-runtime'
import { DatePicker } from './DatePicker'
import { SectionState } from './SectionState'
import { ShiftBar } from './ShiftBar'
import './ShiftRequests.view.css'

export function ShiftRequestsView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "grid", gap: "16px", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        {v.isError ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", padding: "8px"}}>
              <SectionState kind="error" retry={v.retry} />
            </div>
          </>
        ) : null}
        {v.isLoading ? (
          <>
            <SkeletonBlock {...(v.sk || {})} />
          </>
        ) : null}
        {v.showHr ? (
          <>
            <section aria-labelledby="sr-wait" style={{display: "grid", gap: "10px"}}>
              <h3 id="sr-wait" style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "800"}}>
                {"Waiting for your OK"}
              </h3>
              {v.caughtUp ? (
                <>
                  <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
                    <EmptyState icon={v.okIcon} title="All caught up" description="Nobody is waiting to change shifts." />
                  </div>
                </>
              ) : null}
              {arr(v.pending).map((r: any, $index: number) => (
                <Fragment key={$index}>
                  <article style={{display: "grid", gap: "14px", padding: "18px 20px", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
                    <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "8px 12px"}}>
                      <HrAvatar name={r?.name} sub={r?.sub} />
                      <span style={{display: "inline-flex", alignItems: "center", gap: "10px", fontSize: "12.5px", color: "#64748b"}}>
                        <HrStatusPill tone="warn">
                          {"Waiting"}
                        </HrStatusPill>
                        {"Asked on "}{txt(r?.submitted)}
                      </span>
                    </div>
                    <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "10px"}}>
                      <div style={{flex: "1 1 220px", minWidth: "0", display: "grid", gap: "10px", padding: "14px", borderRadius: "14px", background: "#f8fafc", border: "1px solid #eef2f1"}}>
                        <span style={{fontSize: "11px", fontWeight: "800", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
                          {"Now"}
                        </span>
                        <span style={{display: "flex", alignItems: "center", gap: "10px", minWidth: "0"}}>
                          <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                            <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                              <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={r?.from?.tint} stroke={r?.from?.edge} />
                            </svg>
                            <span style={{position: "relative", display: "inline-flex"}}>
                              {txt(r?.from?.iconEl)}
                            </span>
                          </span>
                          <span style={{display: "grid", minWidth: "0"}}>
                            <strong style={{fontSize: "14px"}}>
                              {txt(r?.from?.name)}
                            </strong>
                            <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                              {txt(r?.from?.range)}
                            </span>
                          </span>
                        </span>
                        <ShiftBar start={r?.from?.start} end={r?.from?.end} color={r?.from?.bar} />
                      </div>
                      {v.isDesktop ? (
                        <>
                          <span aria-hidden="true" style={{flex: "0 0 36px", width: "36px", height: "36px", borderRadius: "999px", background: "#fff", border: "1px solid #e2e8f0", color: "#0f6e56", display: "inline-flex", alignItems: "center", justifyContent: "center", boxShadow: "0 1px 2px rgba(15,23,42,.06)"}}>
                            {txt(v.icArrow)}
                          </span>
                        </>
                      ) : null}
                      <div style={{flex: "1 1 220px", minWidth: "0", display: "grid", gap: "10px", padding: "14px", borderRadius: "14px", background: "#f0fdf8", border: "1px solid #a7f3d0"}}>
                        <span style={{fontSize: "11px", fontWeight: "800", letterSpacing: ".08em", textTransform: "uppercase", color: "#0f6e56"}}>
                          {"Wants"}
                        </span>
                        <span style={{display: "flex", alignItems: "center", gap: "10px", minWidth: "0"}}>
                          <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                            <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                              <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={r?.to?.tint} stroke={r?.to?.edge} />
                            </svg>
                            <span style={{position: "relative", display: "inline-flex"}}>
                              {txt(r?.to?.iconEl)}
                            </span>
                          </span>
                          <span style={{display: "grid", minWidth: "0"}}>
                            <strong style={{fontSize: "14px"}}>
                              {txt(r?.to?.name)}
                            </strong>
                            <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                              {txt(r?.to?.range)}
                            </span>
                          </span>
                        </span>
                        <ShiftBar start={r?.to?.start} end={r?.to?.end} color={r?.to?.bar} />
                      </div>
                    </div>
                    <div style={{display: "flex", flexWrap: "wrap", gap: "6px 18px", fontSize: "13px", lineHeight: "1.5", color: "#334155"}}>
                      <span>
                        {"Would start on "}
                        <strong>
                          {txt(r?.starts)}
                        </strong>
                      </span>
                      <span>
                        <span style={{color: "#64748b"}}>
                          {"Reason:"}
                        </span>
                        {" "}{txt(r?.reason)}
                      </span>
                    </div>
                    <div style={{display: "flex", flexWrap: "wrap", alignItems: "flex-end", gap: "8px"}}>
                      <textarea value={r?.note} onChange={r?.setNote} rows={1} maxLength={300} placeholder={r?.notePh} aria-label="Decision note" style={{flex: "1 1 260px", minWidth: "0", minHeight: "40px", boxSizing: "border-box", font: "inherit", fontSize: "13px", padding: "9px 12px", border: "1px solid #cbd5e1", borderRadius: "10px", resize: "vertical", outline: "none", color: "#0f172a"}} className="dc-shift-requests-0">

                      </textarea>
                      <HrButton variant="ghost" onClick={r?.reject} data-tip="Keeps the current shift">
                        {"Reject"}
                      </HrButton>
                      <HrButton onClick={r?.approve} data-tip={r?.approveTip}>
                        {txt(v.icCheck)}{" Approve change"}
                      </HrButton>
                    </div>
                  </article>
                </Fragment>
              ))}
            </section>
            {v.hasDecided ? (
              <>
                <section aria-labelledby="sr-done" style={{display: "grid", gap: "10px"}}>
                  <h3 id="sr-done" style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "800"}}>
                    {"Already decided"}
                  </h3>
                  <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", overflow: "hidden"}}>
                    {arr(v.decided).map((r: any, $index: number) => (
                      <Fragment key={$index}>
                        <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "8px 16px", padding: "12px 16px", borderBottom: "1px solid #f1f5f9"}}>
                          <HrAvatar name={r?.name} sub={r?.sub} />
                          <span style={{flex: "1 1 220px", minWidth: "0", display: "grid", gap: "2px", fontSize: "13px"}}>
                            <strong>
                              {txt(r?.line)}
                            </strong>
                            <span style={{color: "#64748b"}}>
                              {txt(r?.noteText)}
                            </span>
                          </span>
                          <HrStatusPill tone={r?.tone}>
                            {txt(r?.label)}
                          </HrStatusPill>
                        </div>
                      </Fragment>
                    ))}
                  </div>
                </section>
              </>
            ) : null}
          </>
        ) : null}
        {v.showMine ? (
          <>
            <article style={{display: "grid", gap: "16px", padding: "20px 22px", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
              <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "12px"}}>
                <div style={{display: "flex", alignItems: "center", gap: "14px", minWidth: "0"}}>
                  <span style={{position: "relative", flex: "0 0 52px", width: "52px", height: "52px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                    <svg width="52" height="52" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                      <rect x="0.5" y="0.5" width="51" height="51" rx="14" fill={v.my?.tint} stroke={v.my?.edge} />
                    </svg>
                    <span style={{position: "relative", display: "inline-flex"}}>
                      {txt(v.my?.iconBig)}
                    </span>
                  </span>
                  <div style={{minWidth: "0"}}>
                    <p style={{margin: "0", fontSize: "11px", fontWeight: "800", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
                      {"Your shift"}
                    </p>
                    <h3 style={{margin: "2px 0 0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "22px", fontWeight: "800", letterSpacing: "-.01em"}}>
                      {txt(v.my?.name)}
                    </h3>
                  </div>
                </div>
                {v.canAsk ? (
                  <>
                    <HrButton onClick={v.openAsk} data-tip="Opens Ask for a different shift">
                      {txt(v.icSwap)}{" Ask for a different shift"}
                    </HrButton>
                  </>
                ) : null}
                {v.hasPendingMine ? (
                  <>
                    <HrStatusPill tone="warn">
                      {"Your request is with HR"}
                    </HrStatusPill>
                  </>
                ) : null}
              </div>
              <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "4px 10px", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontVariantNumeric: "tabular-nums"}}>
                <span style={{fontSize: "24px", fontWeight: "800", letterSpacing: "-.02em"}}>
                  {txt(v.my?.startLabel)}
                </span>
                <span style={{display: "inline-flex", color: "#94a3b8"}}>
                  {txt(v.icArrow)}
                </span>
                <span style={{fontSize: "24px", fontWeight: "800", letterSpacing: "-.02em"}}>
                  {txt(v.my?.endLabel)}
                </span>
              </div>
              <ShiftBar start={v.my?.start} end={v.my?.end} color={v.my?.bar} ticks={true} />
              <ul style={{display: "flex", flexWrap: "wrap", gap: "6px", margin: "0", padding: "0", listStyle: "none"}}>
                <li style={{display: "inline-flex", alignItems: "center", gap: "6px", padding: "5px 10px", borderRadius: "999px", background: "#f8fafc", border: "1px solid #eef2f1", fontSize: "12.5px", fontWeight: "600", color: "#334155"}}>
                  {txt(v.icClock)}{txt(v.my?.hoursLabel)}
                </li>
                <li style={{display: "inline-flex", alignItems: "center", gap: "6px", padding: "5px 10px", borderRadius: "999px", background: "#f8fafc", border: "1px solid #eef2f1", fontSize: "12.5px", fontWeight: "600", color: "#334155"}}>
                  {txt(v.icTimer)}{txt(v.my?.lateLabel)}
                </li>
                <li style={{display: "inline-flex", alignItems: "center", gap: "6px", padding: "5px 10px", borderRadius: "999px", background: "#f8fafc", border: "1px solid #eef2f1", fontSize: "12.5px", fontWeight: "600", color: "#334155"}}>
                  {txt(v.icCoffee)}{txt(v.my?.breakLabel)}
                </li>
                <li style={{display: "inline-flex", alignItems: "center", gap: "6px", padding: "5px 10px", borderRadius: "999px", background: "#f8fafc", border: "1px solid #eef2f1", fontSize: "12.5px", fontWeight: "600", color: "#334155"}}>
                  {txt(v.icCal)}{txt(v.my?.sinceLabel)}
                </li>
              </ul>
            </article>
            <section aria-labelledby="sr-mine" style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "18px 20px", display: "grid", gap: "12px"}}>
              <h3 id="sr-mine" style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "800"}}>
                {"Your requests"}
              </h3>
              {v.myEmpty ? (
                <>
                  <p style={{margin: "0", padding: "18px", textAlign: "center", border: "1px dashed #cbd5e1", borderRadius: "12px", fontSize: "13px", color: "#64748b"}}>
                    {"You haven’t asked for a change yet."}
                  </p>
                </>
              ) : null}
              <div style={{display: "grid", gap: "8px"}}>
                {arr(v.myList).map((m: any, $index: number) => (
                  <Fragment key={$index}>
                    <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "8px 16px", padding: "12px 14px", border: "1px solid #f1f5f9", borderRadius: "12px", background: "#f8fafc", fontSize: "13px"}}>
                      <span style={{display: "grid", gap: "2px", minWidth: "0"}}>
                        <strong>
                          {txt(m?.line)}
                        </strong>
                        <span style={{color: "#64748b"}}>
                          {txt(m?.noteText)}
                        </span>
                      </span>
                      <HrStatusPill tone={m?.tone}>
                        {txt(m?.label)}
                      </HrStatusPill>
                    </div>
                  </Fragment>
                ))}
              </div>
            </section>
          </>
        ) : null}
      </div>
      {v.askOpen ? (
        <>
          <HrDrawer title="Ask for a different shift" onClose={v.close} footer={v.footer} width="max-w-md">
            <div style={{display: "grid", gap: "16px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
              <div style={{display: "grid", gap: "8px"}}>
                <span style={{fontSize: "13px", fontWeight: "600"}}>
                  {"Which shift would you like? *"}
                </span>
                <div role="radiogroup" aria-label="Shifts" style={{display: "grid", gap: "8px"}}>
                  {arr(v.opts).map((o: any, $index: number) => (
                    <Fragment key={$index}>
                      {o?.active ? (
                        <>
                          <button type="button" role="radio" aria-checked="true" onClick={o?.onClick} style={{display: "grid", gap: "10px", padding: "12px 14px", borderRadius: "14px", border: "2px solid #0f6e56", background: "#f0fdf8", textAlign: "left", font: "inherit", cursor: "pointer"}}>
                            <span style={{display: "flex", alignItems: "center", gap: "12px"}}>
                              <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={o?.tint} stroke={o?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(o?.iconEl)}
                                </span>
                              </span>
                              <span style={{flex: "1", display: "grid", minWidth: "0"}}>
                                <strong style={{fontSize: "14px"}}>
                                  {txt(o?.name)}
                                </strong>
                                <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                                  {txt(o?.range)}
                                </span>
                              </span>
                              <span style={{flex: "0 0 22px", width: "22px", height: "22px", borderRadius: "999px", background: "#0f6e56", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                {txt(v.icCheckSm)}
                              </span>
                            </span>
                            <ShiftBar start={o?.start} end={o?.end} color={o?.bar} />
                          </button>
                        </>
                      ) : null}
                      {o?.inactive ? (
                        <>
                          <button type="button" role="radio" aria-checked="false" onClick={o?.onClick} style={{display: "grid", gap: "10px", padding: "13px 15px", borderRadius: "14px", border: "1px solid #e2e8f0", background: "#fff", textAlign: "left", font: "inherit", cursor: "pointer"}} className="dc-shift-requests-1">
                            <span style={{display: "flex", alignItems: "center", gap: "12px"}}>
                              <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={o?.tint} stroke={o?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(o?.iconEl)}
                                </span>
                              </span>
                              <span style={{flex: "1", display: "grid", minWidth: "0"}}>
                                <strong style={{fontSize: "14px"}}>
                                  {txt(o?.name)}
                                </strong>
                                <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                                  {txt(o?.range)}
                                </span>
                              </span>
                              <span style={{flex: "0 0 22px", width: "22px", height: "22px", boxSizing: "border-box", borderRadius: "999px", border: "2px solid #cbd5e1"}} />
                            </span>
                            <ShiftBar start={o?.start} end={o?.end} color={o?.bar} />
                          </button>
                        </>
                      ) : null}
                    </Fragment>
                  ))}
                </div>
              </div>
              <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                <span>
                  {"Starting from *"}
                </span>
                <DatePicker value={v.f?.date} min={v.tomorrowMin} onChange={v.setDate} label="Starting from" />
              </div>
              <label style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                {"Why do you need it? *"}
                <textarea value={v.f?.reason} onChange={v.setReason} rows={3} maxLength={300} placeholder="e.g. My bus timings changed" style={{font: "inherit", fontWeight: "400", padding: "9px 12px", border: "1px solid #cbd5e1", borderRadius: "8px", outline: "none", resize: "vertical"}} className="dc-shift-requests-0">

                </textarea>
              </label>
            </div>
          </HrDrawer>
        </>
      ) : null}
    </>
  )
}
