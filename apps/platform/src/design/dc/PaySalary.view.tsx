// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component PaySalary.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrButton, HrDrawer, HrSelect, TableCard } from '@/shared/components/hr'
import { DataTable } from '@/shared/components/DataTable'
import { EmptyState } from '@/shared/components/EmptyState'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { Modal } from '@unifiedtree/ui-kit'
import { arr, txt } from './dc-runtime'
import { DatePicker } from './DatePicker'
import './PaySalary.view.css'

export function PaySalaryView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "grid", gridTemplateColumns: "minmax(0,1fr)", gap: "16px", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        <header style={{position: "relative", overflow: "hidden", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "20px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
          <span aria-hidden="true" style={{position: "absolute", right: "-60px", top: "-150px", width: "380px", height: "320px", borderRadius: "999px", background: "rgba(16,185,129,.10)", filter: "blur(60px)", pointerEvents: "none"}} />
          <div style={{position: "relative", display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "16px 24px", padding: "clamp(18px,2.4vw,26px) clamp(18px,2.6vw,30px)"}}>
            <div style={{flex: "999 1 300px", minWidth: "0", display: "grid", gap: "6px"}}>
              <p style={{margin: "0", fontSize: "12.5px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#0f6e56"}}>
                {"Payroll / Salary Structure"}
              </p>
              <h1 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "clamp(22px,2.4vw,28px)", fontWeight: "800", letterSpacing: "-.02em", lineHeight: "1.2"}}>
                {"Salary Structure"}
              </h1>
              <p style={{margin: "0", fontSize: "14px", lineHeight: "1.5", color: "#475569", textWrap: "pretty"}}>
                {"Monthly pay split for each person. Payroll uses this to calculate payslips."}
              </p>
            </div>
            <div style={{flex: "1 1 auto", display: "flex", flexWrap: "wrap", justifyContent: "flex-end", gap: "8px"}}>
              {v.canBulk ? (
                <>
                  <HrButton onClick={v.openBulk} data-tip="Opens Bulk revise CTC">
                    {txt(v.icTrend)}{" Bulk revise CTC"}
                  </HrButton>
                </>
              ) : null}
            </div>
          </div>
        </header>
        {v.isLoading ? (
          <>
            <div role="status" aria-label="Loading" style={{display: "grid", gap: "10px"}}>
              <SkeletonBlock {...(v.skA || {})} />
              <SkeletonBlock {...(v.skB || {})} />
            </div>
          </>
        ) : null}
        {v.isError ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
              <EmptyState icon={v.errIcon} title="Couldn’t load salary structures" description="Something went wrong on our side and nothing was changed. Try again in a moment." action={v.retry} />
            </div>
          </>
        ) : null}
        {v.isEmpty ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
              <EmptyState icon={v.emptyIcon} title="No salary structures yet" description="Add people in Workforce Directory first, then set their monthly pay here." action={v.emptyAction} />
            </div>
          </>
        ) : null}
        {v.live ? (
          <>
            {v.hasNotice ? (
              <>
                <section role="status" style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "10px 14px", padding: "14px 16px", border: "1px solid #a7f3d0", borderRadius: "16px", background: "#ecfdf5"}}>
                  <span style={{display: "inline-flex", color: "#0f6e56"}}>
                    {txt(v.icOk)}
                  </span>
                  <span style={{flex: "1 1 260px", minWidth: "0", fontSize: "13.5px", lineHeight: "1.5", color: "#065f46", textWrap: "pretty"}}>
                    <strong>
                      {txt(v.noticeName)}
                    </strong>
                    {" now has a salary structure. Re-process the "}{txt(v.runLabel)}{" run to include them."}
                  </span>
                  <HrButton size="sm" onClick={v.openRun} data-tip={v.runTip}>
                    {"Open "}{txt(v.runLabel)}{" run"}
                  </HrButton>
                  <button type="button" onClick={v.dismiss} aria-label="Dismiss" style={{width: "32px", height: "32px", display: "inline-flex", alignItems: "center", justifyContent: "center", border: "0", borderRadius: "8px", background: "transparent", color: "#047857", cursor: "pointer"}} className="dc-pay-salary-0">
                    {txt(v.icX)}
                  </button>
                </section>
              </>
            ) : null}
            {v.hasMissing ? (
              <>
                <section style={{display: "grid", gap: "10px", padding: "16px 18px", border: "1px solid #fcd34d", borderRadius: "16px", background: "#fffbeb"}}>
                  <div style={{display: "flex", alignItems: "flex-start", gap: "10px"}}>
                    <span style={{display: "inline-flex", marginTop: "1px", color: "#b45309"}}>
                      {txt(v.icAlert)}
                    </span>
                    <div style={{display: "grid", gap: "2px"}}>
                      <strong style={{fontSize: "14px", color: "#78350f"}}>
                        {txt(v.missingTitle)}
                      </strong>
                      <span style={{fontSize: "13px", color: "#92400e"}}>
                        {"They’re skipped in the "}{txt(v.runLabel)}{" run until you add one."}
                      </span>
                    </div>
                  </div>
                  {arr(v.missing).map((k: any, $index: number) => (
                    <Fragment key={$index}>
                      <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "8px 12px", padding: "10px 12px", borderRadius: "12px", background: "#fff", border: "1px solid #fde68a"}}>
                        <div style={{flex: "1 1 200px", minWidth: "0", display: "flex", flexWrap: "wrap", alignItems: "baseline", gap: "2px 10px"}}>
                          <strong style={{fontSize: "13.5px"}}>
                            {txt(k?.name)}
                          </strong>
                          <span style={{fontFamily: "'JetBrains Mono',ui-monospace,monospace", fontSize: "12.5px", fontWeight: "600", color: "#475569"}}>
                            {txt(k?.code)}
                          </span>
                          <span style={{fontSize: "12.5px", color: "#64748b"}}>
                            {txt(k?.dept)}{" · joined "}{txt(k?.joined)}
                          </span>
                        </div>
                        <HrButton size="sm" onClick={k?.onAdd} data-tip="Opens Add salary structure">
                          {txt(v.icPlus)}{" Add structure"}
                        </HrButton>
                      </div>
                    </Fragment>
                  ))}
                </section>
              </>
            ) : null}
            <TableCard search={v.search} filters={v.filters} onClearFilters={v.clear} actions={v.actions} footer={v.pager}>
              {v.isDesktop ? (
                <>
                  <DataTable columns={v.columns} data={v.pageRows} keyField="id" emptyMessage="No one matches these filters." onRowClick={v.openRow} />
                </>
              ) : null}
              {v.isMobile ? (
                <>
                  <div style={{display: "grid", gap: "8px", padding: "12px"}}>
                    {arr(v.pageRows).map((r: any, $index: number) => (
                      <Fragment key={$index}>
                        <article style={{display: "grid", gap: "10px", padding: "12px 14px", border: "1px solid #e2e8f0", borderRadius: "14px", background: "#fff"}}>
                          <div style={{display: "flex", alignItems: "flex-start", gap: "10px"}}>
                            <div style={{flex: "1", minWidth: "0", display: "grid", gap: "2px"}}>
                              <strong style={{fontSize: "14px"}}>
                                {txt(r?.name)}
                              </strong>
                              <span style={{fontSize: "12px", color: "#64748b"}}>
                                <span style={{fontFamily: "'JetBrains Mono',ui-monospace,monospace", fontWeight: "600", color: "#334155"}}>
                                  {txt(r?.code)}
                                </span>
                                {" · "}{txt(r?.dept)}
                              </span>
                            </div>
                            <HrButton size="sm" variant="ghost" onClick={r?.onEdit}>
                              {"Edit"}
                            </HrButton>
                          </div>
                          <dl style={{margin: "0", display: "grid", gridTemplateColumns: "repeat(3,minmax(0,1fr))", gap: "6px", fontSize: "12px"}}>
                            <div>
                              <dt style={{color: "#64748b"}}>
                                {"Basic"}
                              </dt>
                              <dd style={{margin: "2px 0 0", fontWeight: "600", fontVariantNumeric: "tabular-nums"}}>
                                {txt(r?.basicL)}
                              </dd>
                            </div>
                            <div>
                              <dt style={{color: "#64748b"}}>
                                {"HRA"}
                              </dt>
                              <dd style={{margin: "2px 0 0", fontWeight: "600", fontVariantNumeric: "tabular-nums"}}>
                                {txt(r?.hraL)}
                              </dd>
                            </div>
                            <div>
                              <dt style={{color: "#64748b"}}>
                                {"Net payable"}
                              </dt>
                              <dd style={{margin: "2px 0 0", fontWeight: "800", color: "#0f6e56", fontVariantNumeric: "tabular-nums"}}>
                                {txt(r?.netL)}
                              </dd>
                            </div>
                          </dl>
                        </article>
                      </Fragment>
                    ))}
                    {v.noRows ? (
                      <>
                        <p style={{margin: "0", padding: "20px", textAlign: "center", fontSize: "13px", color: "#64748b"}}>
                          {"No one matches these filters."}
                        </p>
                      </>
                    ) : null}
                  </div>
                </>
              ) : null}
            </TableCard>
          </>
        ) : null}
      </div>
      {v.drawerOpen ? (
        <>
          <HrDrawer title={v.dTitle} onClose={v.closeDrawer} footer={v.dFooter} width="max-w-md">
            <div style={{display: "grid", gap: "16px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
              <div style={{display: "grid", gap: "2px"}}>
                <strong style={{fontSize: "16px"}}>
                  {txt(v.dName)}
                </strong>
                <span style={{fontSize: "13px", color: "#64748b"}}>
                  <span style={{fontFamily: "'JetBrains Mono',ui-monospace,monospace", fontWeight: "600", color: "#334155"}}>
                    {txt(v.dCode)}
                  </span>
                  {" · "}{txt(v.dMeta)}
                </span>
              </div>
              <label style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                {"Monthly gross (₹) *"}
                <input type="number" inputMode="numeric" min="10000" step="500" value={v.fMonthly} onChange={v.setMonthly} style={{font: "inherit", fontWeight: "500", height: "42px", padding: "0 12px", boxSizing: "border-box", border: "1px solid #cbd5e1", borderRadius: "10px", outline: "none", fontVariantNumeric: "tabular-nums"}} className="dc-pay-salary-1" />
                {v.fBad ? (
                  <>
                    <span style={{fontSize: "12px", fontWeight: "400", color: "#be123c"}}>
                      {txt(v.minNote)}
                    </span>
                  </>
                ) : null}
              </label>
              <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                <span>
                  {"Effective from *"}
                </span>
                <DatePicker value={v.fDate} min={v.fDateMin} onChange={v.setDate} label="Effective from" />
              </div>
              <section aria-label="Monthly breakdown" style={{display: "grid", padding: "14px 16px", borderRadius: "14px", background: "#f8fafc", border: "1px solid #eef2f1"}}>
                {arr(v.preview).map((l: any, $index: number) => (
                  <Fragment key={$index}>
                    <div style={{display: "flex", justifyContent: "space-between", gap: "12px", padding: "7px 0", borderBottom: "1px dashed #e2e8f0", fontSize: "13px"}}>
                      <span style={{color: "#334155"}}>
                        {txt(l?.k)}
                      </span>
                      <span style={{fontWeight: "600", fontVariantNumeric: "tabular-nums"}}>
                        {txt(l?.v)}
                      </span>
                    </div>
                  </Fragment>
                ))}
                <div style={{display: "flex", justifyContent: "space-between", gap: "12px", padding: "10px 0 0", fontSize: "14px"}}>
                  <strong style={{color: "#065f46"}}>
                    {"Net payable"}
                  </strong>
                  <strong style={{color: "#0f6e56", fontVariantNumeric: "tabular-nums"}}>
                    {txt(v.pNet)}
                  </strong>
                </div>
              </section>
              <p style={{margin: "0", fontSize: "12.5px", lineHeight: "1.5", color: "#64748b"}}>
                {txt(v.splitNote)}
              </p>
            </div>
          </HrDrawer>
        </>
      ) : null}
      <Modal open={v.bulkOpen} onOpenChange={v.setBulkOpen} title="Bulk revise CTC?" description="Raise monthly pay for a group of people from a chosen date." size="sm">
        <div style={{display: "grid", gap: "14px", marginTop: "4px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
          {" "}{txt(v.bModeBlock)}
          <label style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
            {txt(v.bValueLabel)}
            <input type="number" min={v.bMin} max={v.bMax} step={v.bStep} value={v.bPct} onChange={v.setPct} style={{font: "inherit", fontWeight: "500", height: "42px", padding: "0 12px", boxSizing: "border-box", border: "1px solid #cbd5e1", borderRadius: "10px", outline: "none"}} className="dc-pay-salary-1" />
          </label>
          <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
            <span>
              {"Who"}
            </span>
            <HrSelect value={v.bDept} onChange={v.setBDept} options={v.deptOptions} />
          </div>
          {txt(v.bWhoBlock)}{" "}
          <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
            <span>
              {"Effective from *"}
            </span>
            <DatePicker value={v.bDate} min={v.bDateMin} onChange={v.setBDate} label="Effective from" />
          </div>
          {txt(v.bReasonBlock)}{" "}
          <p style={{margin: "0", padding: "10px 12px", borderRadius: "12px", background: "#ecfdf5", border: "1px solid #a7f3d0", fontSize: "13px", lineHeight: "1.45", color: "#065f46"}}>
            {txt(v.bPreview)}
          </p>
          {txt(v.bPreviewBlock)}{" "}
          <div style={{display: "flex", flexWrap: "wrap", justifyContent: "flex-end", gap: "8px"}}>
            <HrButton variant="ghost" onClick={v.closeBulk}>
              {"Cancel"}
            </HrButton>
            <HrButton onClick={v.applyBulk} disabled={v.bBad}>
              {txt(v.bApplyLabel)}
            </HrButton>
          </div>
        </div>
      </Modal>
    </>
  )
}
