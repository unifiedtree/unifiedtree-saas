// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component PayDashboard.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrButton, HrStatCard, HrStatusPill } from '@/shared/components/hr'
import { EmptyState } from '@/shared/components/EmptyState'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { arr, txt } from './dc-runtime'
import { ProcessSteps } from './ProcessSteps'
import './PayDashboard.view.css'

export function PayDashboardView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "grid", gridTemplateColumns: "minmax(0,1fr)", gap: "16px", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        <header style={{position: "relative", overflow: "hidden", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "20px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
          <span aria-hidden="true" style={{position: "absolute", right: "-60px", top: "-150px", width: "380px", height: "320px", borderRadius: "999px", background: "rgba(16,185,129,.10)", filter: "blur(60px)", pointerEvents: "none"}} />
          <div style={{position: "relative", display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "16px 24px", padding: "clamp(18px,2.4vw,26px) clamp(18px,2.6vw,30px)"}}>
            <div style={{flex: "999 1 300px", minWidth: "0", display: "grid", gap: "6px"}}>
              <p style={{margin: "0", fontSize: "12.5px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#0f6e56"}}>
                {"Payroll / Payroll Dashboard"}
              </p>
              <h1 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "clamp(22px,2.4vw,28px)", fontWeight: "800", letterSpacing: "-.02em", lineHeight: "1.2"}}>
                {"Payroll Dashboard"}
              </h1>
              <p style={{margin: "0", fontSize: "14px", lineHeight: "1.5", color: "#475569", textWrap: "pretty"}}>
                {txt(v.companyName)}{" · cost, dues and where this month’s run stands."}
              </p>
            </div>
            <div style={{flex: "1 1 auto", display: "flex", flexWrap: "wrap", justifyContent: "flex-end", gap: "8px"}}>
              <HrButton variant="ghost" onClick={v.goSalary} data-tip="Opens Salary Structure">
                {txt(v.icList)}{" Salary structures"}
              </HrButton>
              <HrButton onClick={v.openRun} data-tip={v.runTip}>
                {txt(v.icPlay)}{" "}{txt(v.openRunLabel)}
              </HrButton>
            </div>
          </div>
        </header>
        {v.isLoading ? (
          <>
            <div role="status" aria-label="Loading" style={{display: "grid", gap: "12px"}}>
              <SkeletonBlock {...(v.skA || {})} />
              <SkeletonBlock {...(v.skB || {})} />
            </div>
          </>
        ) : null}
        {v.isError ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
              <EmptyState icon={v.errIcon} title="Couldn’t load the payroll dashboard" description="Something went wrong on our side and nothing was changed. Try again in a moment." action={v.retry} />
            </div>
          </>
        ) : null}
        {v.isEmpty ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
              <EmptyState icon={v.emptyIcon} title="No payroll yet" description="Add salary structures for your people, then run your first payroll." action={v.emptyAction} />
            </div>
          </>
        ) : null}
        {v.live ? (
          <>
            <div style={{display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(min(100%,230px),1fr))", gap: "16px"}}>
              {arr(v.stats).map((c: any, $index: number) => (
                <Fragment key={$index}>
                  <HrStatCard icon={c?.icon} color={c?.color} value={c?.value} label={c?.label} sub={c?.sub} trend={c?.trend} onClick={c?.onClick} />
                </Fragment>
              ))}
            </div>
            <div style={{display: "flex", flexWrap: "wrap", alignItems: "stretch", gap: "16px"}}>
              <section style={{flex: "3 1 440px", minWidth: "0", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "18px", alignContent: "start"}}>
                <div style={{display: "flex", flexWrap: "wrap", alignItems: "baseline", justifyContent: "space-between", gap: "4px 12px"}}>
                  <h2 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
                    {"Payroll cost · last 6 months"}
                  </h2>
                  <span style={{fontSize: "12.5px", color: "#64748b"}}>
                    {"Gross pay per month"}
                  </span>
                </div>
                <div role="img" aria-label={v.chartAria} style={{display: "flex", alignItems: "flex-end", gap: "clamp(6px,2.2vw,22px)", padding: "0 2px"}}>
                  {arr(v.bars).map((b: any, $index: number) => (
                    <Fragment key={$index}>
                      <div style={{flex: "1 1 0", minWidth: "0", display: "grid", justifyItems: "center", gap: "8px"}}>
                        <svg viewBox="0 0 40 170" preserveAspectRatio="none" aria-hidden="true" style={{display: "block", width: "min(100%,56px)", height: "170px"}}>
                          <rect x="0" y="0" width="40" height="170" rx="6" fill="#f8fafc" />
                          <rect x="0" y={b?.y} width="40" height={b?.h} rx="6" fill={b?.fill} />
                        </svg>
                        <span style={{display: "grid", justifyItems: "center", gap: "1px", textAlign: "center"}}>
                          <strong style={{fontSize: "12.5px"}}>
                            {txt(b?.m)}
                          </strong>
                          <span style={{fontSize: "11px", color: "#64748b", whiteSpace: "nowrap", fontVariantNumeric: "tabular-nums"}}>
                            {txt(b?.label)}
                          </span>
                        </span>
                      </div>
                    </Fragment>
                  ))}
                </div>
              </section>
              <section style={{flex: "2 1 320px", minWidth: "0", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gridTemplateColumns: "minmax(0,1fr)", gap: "16px", alignContent: "start"}}>
                <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "8px"}}>
                  <div style={{display: "grid", gap: "2px"}}>
                    <span style={{fontSize: "11px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
                      {"This month’s run"}
                    </span>
                    <div style={{display: "flex", alignItems: "center", gap: "8px"}}>
                      <h2 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "20px", fontWeight: "800", letterSpacing: "-.01em"}}>
                        {txt(v.runLabel)}
                      </h2>
                      <HrStatusPill tone={v.pillTone}>
                        {txt(v.pillLabel)}
                      </HrStatusPill>
                    </div>
                  </div>
                  <strong style={{fontSize: "13px", color: "#0f6e56", fontVariantNumeric: "tabular-nums"}}>
                    {txt(v.netLabel)}{" net"}
                  </strong>
                </div>
                <ProcessSteps steps={v.steps} current={v.cur} complete={v.complete} label={v.progressLabel} />
                <p style={{margin: "0", fontSize: "13px", lineHeight: "1.5", color: "#475569", textWrap: "pretty"}}>
                  {txt(v.hint)}
                </p>
                <HrButton onClick={v.openRun} data-tip={v.runTip}>
                  {txt(v.runCta)}
                </HrButton>
              </section>
            </div>
            <div style={{display: "flex", flexWrap: "wrap", alignItems: "stretch", gap: "16px"}}>
              <section style={{flex: "1 1 360px", minWidth: "0", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "4px", alignContent: "start"}}>
                <h2 style={{margin: "0 0 8px", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
                  {"Statutory dues"}
                </h2>
                {arr(v.dues).map((d: any, $index: number) => (
                  <Fragment key={$index}>
                    <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "6px 12px", padding: "12px 0", borderTop: "1px solid #f1f5f9"}}>
                      <span style={{flex: "0 0 36px", width: "36px", height: "36px", borderRadius: "10px", background: "#ecfdf5", border: "1px solid #d1fae5", color: "#0f6e56", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                        {txt(v.icCal)}
                      </span>
                      <div style={{flex: "1 1 160px", minWidth: "0", display: "grid", gap: "2px"}}>
                        <strong style={{fontSize: "13.5px"}}>
                          {txt(d?.what)}
                        </strong>
                        <span style={{fontSize: "12.5px", color: "#64748b"}}>
                          {"Due "}{txt(d?.when)}{txt(d?.note)}
                        </span>
                      </div>
                      <strong style={{fontSize: "14px", fontVariantNumeric: "tabular-nums"}}>
                        {txt(d?.amountLabel)}
                      </strong>
                    </div>
                  </Fragment>
                ))}
                {v.noDues ? (
                  <>
                    <p style={{margin: "0", padding: "12px 0", borderTop: "1px solid #f1f5f9", fontSize: "13px", color: "#64748b"}}>
                      {txt(v.duesEmpty)}
                    </p>
                  </>
                ) : null}
              </section>
              <section style={{flex: "1 1 360px", minWidth: "0", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "4px", alignContent: "start"}}>
                <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "8px", marginBottom: "8px"}}>
                  <h2 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
                    {"Recent runs"}
                  </h2>
                  <button type="button" onClick={v.goRuns} data-tip="Opens Processing & Payslips" style={{padding: "0", border: "0", background: "none", font: "inherit", fontSize: "13px", fontWeight: "700", color: "#0f6e56", cursor: "pointer"}} className="dc-pay-dashboard-0">
                    {"All runs →"}
                  </button>
                </div>
                {arr(v.recent).map((r: any, $index: number) => (
                  <Fragment key={$index}>
                    <button type="button" onClick={r?.onOpen} data-tip={r?.tip} style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "6px 12px", padding: "12px 8px", margin: "0 -8px", border: "0", borderTop: "1px solid #f1f5f9", borderRadius: "10px", background: "transparent", font: "inherit", color: "inherit", textAlign: "left", cursor: "pointer"}} className="dc-pay-dashboard-1">
                      <div style={{flex: "1 1 160px", minWidth: "0", display: "grid", gap: "2px"}}>
                        <strong style={{fontSize: "13.5px"}}>
                          {txt(r?.label)}
                        </strong>
                        <span style={{fontSize: "12.5px", color: "#64748b"}}>
                          {txt(r?.meta)}
                        </span>
                      </div>
                      <strong style={{fontSize: "14px", fontVariantNumeric: "tabular-nums"}}>
                        {txt(r?.netLabel)}
                      </strong>
                      <HrStatusPill tone="ok">
                        {"Paid"}
                      </HrStatusPill>
                    </button>
                  </Fragment>
                ))}
                {v.noRecent ? (
                  <>
                    <p style={{margin: "0", padding: "12px 0", borderTop: "1px solid #f1f5f9", fontSize: "13px", color: "#64748b"}}>
                      {txt(v.recentEmpty)}
                    </p>
                  </>
                ) : null}
              </section>
            </div>
          </>
        ) : null}
      </div>
    </>
  )
}
