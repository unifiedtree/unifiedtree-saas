// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component PayAdvances.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrButton, HrDrawer, HrSelect, HrStatusPill, TableCard } from '@/shared/components/hr'
import { DataTable } from '@/shared/components/DataTable'
import { EmptyState } from '@/shared/components/EmptyState'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { Modal } from '@unifiedtree/ui-kit'
import { arr, txt } from './dc-runtime'
import './PayAdvances.view.css'

export function PayAdvancesView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "grid", gridTemplateColumns: "minmax(0,1fr)", gap: "16px", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        <header style={{position: "relative", overflow: "hidden", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "20px", boxShadow: "0 1px 2px rgba(15,23,42,.04)"}}>
          <span aria-hidden="true" style={{position: "absolute", right: "-60px", top: "-150px", width: "380px", height: "320px", borderRadius: "999px", background: "rgba(16,185,129,.10)", filter: "blur(60px)", pointerEvents: "none"}} />
          <div style={{position: "relative", display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "16px 24px", padding: "clamp(18px,2.4vw,26px) clamp(18px,2.6vw,30px)"}}>
            <div style={{flex: "999 1 300px", minWidth: "0", display: "grid", gap: "6px"}}>
              <p style={{margin: "0", fontSize: "12.5px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#0f6e56"}}>
                {"Payroll / Advances"}
              </p>
              <h1 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "clamp(22px,2.4vw,28px)", fontWeight: "800", letterSpacing: "-.02em", lineHeight: "1.2"}}>
                {"Advances & Loans"}
              </h1>
              <p style={{margin: "0", fontSize: "14px", lineHeight: "1.5", color: "#475569", textWrap: "pretty"}}>
                {"Money paid ahead of salary and how it’s recovered from payroll."}
              </p>
            </div>
            <div style={{flex: "1 1 auto", display: "flex", flexWrap: "wrap", justifyContent: "flex-end", gap: "8px"}}>
              <HrButton onClick={v.openIssue} data-tip="Opens Issue advance">
                {txt(v.icPlus)}{" Issue advance"}
              </HrButton>
            </div>
          </div>
        </header>
        {v.isLoading ? (
          <>
            <div role="status" aria-label="Loading" style={{display: "grid", gap: "10px"}}>
              <SkeletonBlock {...(v.skA || {})} />
              <SkeletonBlock {...(v.skB || {})} />
            </div>
          </>
        ) : null}
        {v.isError ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
              <EmptyState icon={v.errIcon} title="Couldn’t load advances" description="Something went wrong on our side and nothing was changed. Try again in a moment." action={v.retry} />
            </div>
          </>
        ) : null}
        {v.isEmpty ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px"}}>
              <EmptyState icon={v.emptyIcon} title="No advances or loans" description="When you give someone money ahead of salary, it shows up here with its recovery plan." action={v.issueAction} />
            </div>
          </>
        ) : null}
        {v.live ? (
          <>
            <div style={{display: "flex", flexWrap: "wrap", gap: "8px"}}>
              {arr(v.chips).map((c: any, $index: number) => (
                <Fragment key={$index}>
                  <span style={{display: "inline-flex", alignItems: "center", gap: "8px", padding: "8px 14px", borderRadius: "999px", background: "#fff", border: "1px solid #e2e8f0", fontSize: "13px", color: "#475569"}}>
                    <span>
                      {txt(c?.label)}
                    </span>
                    <strong style={{color: "#0f172a", fontVariantNumeric: "tabular-nums"}}>
                      {txt(c?.value)}
                    </strong>
                  </span>
                </Fragment>
              ))}
            </div>
            <TableCard search={v.search} filters={v.filters} onClearFilters={v.clear} actions={v.actions}>
              {v.isDesktop ? (
                <>
                  <DataTable columns={v.columns} data={v.rows} keyField="id" emptyMessage="No advances match these filters." onRowClick={v.openRow} />
                </>
              ) : null}
              {v.isMobile ? (
                <>
                  <div style={{display: "grid", gap: "8px", padding: "12px"}}>
                    {arr(v.rows).map((r: any, $index: number) => (
                      <Fragment key={$index}>
                        <article style={{display: "grid", gap: "10px", padding: "12px 14px", border: "1px solid #e2e8f0", borderRadius: "14px"}}>
                          <div style={{display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: "8px"}}>
                            <div style={{display: "grid", gap: "2px"}}>
                              <strong style={{fontSize: "14px"}}>
                                {txt(r?.name)}
                              </strong>
                              <span style={{fontSize: "12px", color: "#64748b"}}>
                                {txt(r?.type)}
                              </span>
                            </div>
                            <HrStatusPill tone={r?.tone}>
                              {txt(r?.statusLabel)}
                            </HrStatusPill>
                          </div>
                          <dl style={{margin: "0", display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: "6px", fontSize: "12px"}}>
                            <div>
                              <dt style={{color: "#64748b"}}>
                                {"Principal"}
                              </dt>
                              <dd style={{margin: "2px 0 0", fontWeight: "600"}}>
                                {txt(r?.principalL)}
                              </dd>
                            </div>
                            <div>
                              <dt style={{color: "#64748b"}}>
                                {"Remaining"}
                              </dt>
                              <dd style={{margin: "2px 0 0", fontWeight: "700"}}>
                                {txt(r?.leftL)}
                              </dd>
                            </div>
                          </dl>
                          <div style={{display: "flex", gap: "8px"}}>
                            <HrButton size="sm" variant="ghost" onClick={r?.onView}>
                              {"View"}
                            </HrButton>
                            {r?.pending ? (
                              <>
                                <HrButton size="sm" onClick={r?.onApprove}>
                                  {"Approve"}
                                </HrButton>
                              </>
                            ) : null}
                          </div>
                        </article>
                      </Fragment>
                    ))}
                  </div>
                </>
              ) : null}
            </TableCard>
          </>
        ) : null}
      </div>
      {v.viewOpen ? (
        <>
          <HrDrawer title={v.vTitle} onClose={v.closeView} footer={v.vFooter} width="max-w-md">
            <div style={{display: "grid", gap: "16px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
              <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px"}}>
                <div style={{display: "grid", gap: "2px"}}>
                  <strong style={{fontSize: "16px"}}>
                    {txt(v.v?.name)}
                  </strong>
                  <span style={{fontSize: "13px", color: "#64748b"}}>
                    <span style={{fontFamily: "'JetBrains Mono',ui-monospace,monospace", fontWeight: "600", color: "#334155"}}>
                      {txt(v.v?.code)}
                    </span>
                    {" · "}{txt(v.v?.type)}
                  </span>
                </div>
                <HrStatusPill tone={v.v?.tone}>
                  {txt(v.v?.statusLabel)}
                </HrStatusPill>
              </div>
              <dl style={{margin: "0", display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: "10px"}}>
                {arr(v.v?.facts).map((f: any, $index: number) => (
                  <Fragment key={$index}>
                    <div style={{display: "grid", gap: "3px", padding: "10px 12px", borderRadius: "12px", background: "#f8fafc", border: "1px solid #f1f5f9"}}>
                      <dt style={{fontSize: "11px", fontWeight: "700", letterSpacing: ".06em", textTransform: "uppercase", color: "#64748b"}}>
                        {txt(f?.k)}
                      </dt>
                      <dd style={{margin: "0", fontSize: "13.5px", fontWeight: "600", fontVariantNumeric: "tabular-nums"}}>
                        {txt(f?.v)}
                      </dd>
                    </div>
                  </Fragment>
                ))}
              </dl>
              <section style={{display: "grid"}}>
                <h4 style={{margin: "0 0 4px", fontSize: "11px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
                  {"Recovery plan"}
                </h4>
                {arr(v.v?.plan).map((m: any, $index: number) => (
                  <Fragment key={$index}>
                    <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "12px", padding: "9px 0", borderBottom: "1px dashed #e2e8f0", fontSize: "13px"}}>
                      <span style={{display: "flex", alignItems: "center", gap: "8px"}}>
                        <svg width="8" height="8" aria-hidden="true">
                          <circle cx="4" cy="4" r="4" fill={m?.dot} />
                        </svg>
                        {txt(m?.month)}
                      </span>
                      <span style={{display: "flex", alignItems: "center", gap: "10px"}}>
                        <span style={{color: "#64748b"}}>
                          {txt(m?.note)}
                        </span>
                        <strong style={{fontVariantNumeric: "tabular-nums"}}>
                          {txt(m?.amount)}
                        </strong>
                      </span>
                    </div>
                  </Fragment>
                ))}
              </section>
            </div>
          </HrDrawer>
        </>
      ) : null}
      {v.issueOpen ? (
        <>
          <HrDrawer title="Issue advance" onClose={v.closeIssue} footer={v.iFooter} width="max-w-md">
            <div style={{display: "grid", gap: "14px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
              <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                <span>
                  {"Employee *"}
                </span>
                <HrSelect value={v.i?.emp} onChange={v.setEmp} options={v.empOptions} />
              </div>
              <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                <span>
                  {"Type"}
                </span>
                <HrSelect value={v.i?.type} onChange={v.setType} options={v.typeOptions} />
              </div>
              <label style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                {"Amount (₹) *"}
                <input type="number" min="1000" step="500" value={v.i?.amount} onChange={v.setAmount} placeholder="e.g. 20000" style={{font: "inherit", fontWeight: "500", height: "42px", padding: "0 12px", boxSizing: "border-box", border: "1px solid #cbd5e1", borderRadius: "10px", outline: "none"}} className="dc-pay-advances-0" />
              </label>
              <div style={{display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: "12px"}}>
                <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                  <span>
                    {"Recover over"}
                  </span>
                  <HrSelect value={v.i?.months} onChange={v.setMonths} options={v.monthOptions} />
                </div>
                <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                  <span>
                    {"First deduction"}
                  </span>
                  <HrSelect value={v.i?.start} onChange={v.setStart} options={v.startOptions} />
                </div>
              </div>
              <p style={{margin: "0", padding: "10px 12px", borderRadius: "12px", background: "#ecfdf5", border: "1px solid #a7f3d0", fontSize: "13px", lineHeight: "1.45", color: "#065f46"}}>
                {txt(v.iPreview)}
              </p>
            </div>
          </HrDrawer>
        </>
      ) : null}
      <Modal open={v.approveOpen} onOpenChange={v.setApproveOpen} title={v.aTitle} description={v.aDesc} size="sm">
        <div style={{display: "flex", flexWrap: "wrap", justifyContent: "flex-end", gap: "8px", marginTop: "8px"}}>
          <HrButton variant="ghost" onClick={v.closeApprove}>
            {"Cancel"}
          </HrButton>
          <HrButton onClick={v.doApprove}>
            {"Approve"}
          </HrButton>
        </div>
      </Modal>
    </>
  )
}
