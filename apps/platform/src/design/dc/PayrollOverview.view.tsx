// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component PayrollOverview.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrButton, HrStatusPill } from '@/shared/components/hr'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { arr, txt } from './dc-runtime'
import './PayrollOverview.view.css'

export function PayrollOverviewView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "flex", flexWrap: "wrap", alignItems: "flex-start", gap: "16px", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        <div style={{flex: "3 1 520px", minWidth: "0", display: "grid", gap: "16px"}}>
          <section style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "16px"}}>
            <div style={{display: "flex", flexWrap: "wrap", alignItems: "baseline", justifyContent: "space-between", gap: "4px 12px"}}>
              <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
                {"Pay breakdown"}
              </h3>
              <span style={{fontSize: "12.5px", color: "#64748b"}}>
                {txt(v.breakdownNote)}
              </span>
            </div>
            {v.calc ? (
              <>
                <div style={{display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(240px,1fr))", gap: "16px 28px"}}>
                  <div style={{display: "grid", alignContent: "start"}}>
                    <p style={{margin: "0 0 4px", fontSize: "11px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
                      {"Earnings"}
                    </p>
                    {arr(v.earnings).map((l: any, $index: number) => (
                      <Fragment key={$index}>
                        <div style={{display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: "12px", padding: "9px 0", borderBottom: "1px dashed #e2e8f0"}}>
                          <span style={{minWidth: "0", display: "grid", gap: "1px"}}>
                            <span style={{fontSize: "13.5px", color: "#334155"}}>
                              {txt(l?.label)}
                            </span>
                            {l?.hasNote ? (
                              <>
                                <span style={{fontSize: "12px", color: "#047857"}}>
                                  {txt(l?.note)}
                                </span>
                              </>
                            ) : null}
                          </span>
                          <span style={{fontSize: "13.5px", fontWeight: "600", fontVariantNumeric: "tabular-nums", whiteSpace: "nowrap"}}>
                            {txt(l?.amount)}
                          </span>
                        </div>
                      </Fragment>
                    ))}
                    <div style={{display: "flex", justifyContent: "space-between", gap: "12px", padding: "10px 0 0", fontSize: "13.5px"}}>
                      <strong>
                        {"Gross earnings"}
                      </strong>
                      <strong style={{fontVariantNumeric: "tabular-nums"}}>
                        {txt(v.grossLabel)}
                      </strong>
                    </div>
                  </div>
                  <div style={{display: "grid", alignContent: "start"}}>
                    <p style={{margin: "0 0 4px", fontSize: "11px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
                      {"Deductions"}
                    </p>
                    {arr(v.deductions).map((l: any, $index: number) => (
                      <Fragment key={$index}>
                        <div style={{display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: "12px", padding: "9px 0", borderBottom: "1px dashed #e2e8f0"}}>
                          <span style={{minWidth: "0", display: "grid", gap: "1px"}}>
                            <span style={{fontSize: "13.5px", color: "#334155"}}>
                              {txt(l?.label)}
                            </span>
                            {l?.hasNote ? (
                              <>
                                <span style={{fontSize: "12px", color: "#64748b"}}>
                                  {txt(l?.note)}
                                </span>
                              </>
                            ) : null}
                          </span>
                          <span style={{fontSize: "13.5px", fontWeight: "600", fontVariantNumeric: "tabular-nums", whiteSpace: "nowrap"}}>
                            {txt(l?.amount)}
                          </span>
                        </div>
                      </Fragment>
                    ))}
                    <div style={{display: "flex", justifyContent: "space-between", gap: "12px", padding: "10px 0 0", fontSize: "13.5px"}}>
                      <strong>
                        {"Total deductions"}
                      </strong>
                      <strong style={{fontVariantNumeric: "tabular-nums"}}>
                        {txt(v.dedLabel)}
                      </strong>
                    </div>
                  </div>
                </div>
                <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "6px 16px", padding: "14px 16px", borderRadius: "14px", background: "#ecfdf5", border: "1px solid #a7f3d0"}}>
                  <span style={{display: "grid", gap: "2px"}}>
                    <strong style={{fontSize: "14px", color: "#065f46"}}>
                      {"Net pay"}
                    </strong>
                    <span style={{fontSize: "12.5px", color: "#047857"}}>
                      {txt(v.netNote)}
                    </span>
                  </span>
                  <strong style={{fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "24px", fontWeight: "800", letterSpacing: "-.02em", color: "#0f6e56", fontVariantNumeric: "tabular-nums"}}>
                    {txt(v.netLabel)}
                  </strong>
                </div>
              </>
            ) : null}
            {v.busy ? (
              <>
                <div role="status" aria-label="Calculating pay" style={{display: "grid", gap: "10px"}}>
                  <SkeletonBlock {...(v.skRow || {})} />
                  <SkeletonBlock {...(v.skRow || {})} />
                  <SkeletonBlock {...(v.skRow || {})} />
                  <SkeletonBlock {...(v.skNet || {})} />
                </div>
              </>
            ) : null}
            {v.notCalc ? (
              <>
                <div style={{display: "flex", alignItems: "flex-start", gap: "12px", padding: "16px", border: "1px dashed #cbd5e1", borderRadius: "14px", background: "#fcfdfd"}}>
                  <span style={{display: "inline-flex", flexShrink: "0", color: "#0f6e56"}}>
                    {txt(v.icInfo)}
                  </span>
                  <span style={{display: "grid", gap: "3px"}}>
                    <strong style={{fontSize: "13.5px"}}>
                      {"Nothing calculated yet"}
                    </strong>
                    <span style={{fontSize: "13px", lineHeight: "1.5", color: "#475569", textWrap: "pretty"}}>
                      {"Earnings, deductions and net pay show up here once you process this run. Check the list below first."}
                    </span>
                  </span>
                </div>
              </>
            ) : null}
          </section>
          <section style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "4px"}}>
            <div style={{display: "grid", gap: "3px", marginBottom: "8px"}}>
              <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
                {txt(v.checksTitle)}
              </h3>
              <p style={{margin: "0", fontSize: "13px", color: "#64748b"}}>
                {txt(v.checksSub)}
              </p>
            </div>
            {arr(v.checks).map((c: any, $index: number) => (
              <Fragment key={$index}>
                <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", gap: "10px 12px", padding: "12px 0", borderTop: "1px solid #f1f5f9"}}>
                  {c?.ok ? (
                    <>
                      <span style={{flex: "0 0 36px", width: "36px", height: "36px", borderRadius: "10px", background: "#ecfdf5", border: "1px solid #d1fae5", color: "#0f6e56", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                        {txt(c?.icon)}
                      </span>
                    </>
                  ) : null}
                  {c?.warn ? (
                    <>
                      <span style={{flex: "0 0 36px", width: "36px", height: "36px", borderRadius: "10px", background: "#fffbeb", border: "1px solid #fde68a", color: "#b45309", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                        {txt(c?.icon)}
                      </span>
                    </>
                  ) : null}
                  <div style={{flex: "1 1 220px", minWidth: "0", display: "grid", gap: "2px"}}>
                    <strong style={{fontSize: "13.5px"}}>
                      {txt(c?.title)}
                    </strong>
                    <span style={{fontSize: "12.5px", lineHeight: "1.45", color: "#64748b", textWrap: "pretty"}}>
                      {txt(c?.detail)}
                    </span>
                  </div>
                  <button type="button" onClick={c?.onOpen} data-tip={c?.tip} style={{display: "inline-flex", alignItems: "center", gap: "4px", padding: "0", border: "0", background: "none", font: "inherit", fontSize: "13px", fontWeight: "700", color: "#0f6e56", cursor: "pointer", whiteSpace: "nowrap"}} className="dc-payroll-overview-0">
                    {txt(c?.cta)}{" →"}
                  </button>
                </div>
              </Fragment>
            ))}
          </section>
        </div>
        <div style={{flex: "2 1 320px", minWidth: "0", display: "grid", gap: "16px"}}>
          <section style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "14px"}}>
            <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
              {"Run details"}
            </h3>
            <dl style={{margin: "0", display: "grid", gridTemplateColumns: "auto minmax(0,1fr)", gap: "10px 16px", fontSize: "13px"}}>
              {arr(v.details).map((d: any, $index: number) => (
                <Fragment key={$index}>
                  <dt style={{color: "#64748b"}}>
                    {txt(d?.k)}
                  </dt>
                  {d?.set ? (
                    <>
                      <dd style={{margin: "0", fontWeight: "600", textAlign: "right", overflowWrap: "anywhere", fontVariantNumeric: "tabular-nums"}}>
                        {txt(d?.v)}
                      </dd>
                    </>
                  ) : null}
                  {d?.unset ? (
                    <>
                      <dd style={{margin: "0", textAlign: "right", color: "#94a3b8"}}>
                        {"Not yet"}
                      </dd>
                    </>
                  ) : null}
                </Fragment>
              ))}
            </dl>
          </section>
          {v.showBank ? (
            <>
              <section style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "12px"}}>
                <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px"}}>
                  <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
                    {"Bank disbursement"}
                  </h3>
                  <HrStatusPill tone={v.bankTone}>
                    {txt(v.bankLabel)}
                  </HrStatusPill>
                </div>
                <p style={{margin: "0", fontSize: "13px", lineHeight: "1.5", color: "#64748b", textWrap: "pretty"}}>
                  {txt(v.bankNote)}
                </p>
                {arr(v.batches).map((b: any, $index: number) => (
                  <Fragment key={$index}>
                    <div style={{display: "flex", alignItems: "center", gap: "12px", padding: "12px", border: "1px solid #e2e8f0", borderRadius: "12px"}}>
                      <span style={{flex: "0 0 36px", width: "36px", height: "36px", borderRadius: "10px", background: "#f8fafc", border: "1px solid #e2e8f0", color: "#334155", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                        {txt(v.icBank)}
                      </span>
                      <div style={{flex: "1", minWidth: "0", display: "grid", gap: "2px"}}>
                        <strong style={{fontSize: "13.5px"}}>
                          {txt(b?.bank)}
                        </strong>
                        <span style={{fontSize: "12.5px", color: "#64748b", fontVariantNumeric: "tabular-nums"}}>
                          {txt(b?.count)}{" employees · "}{txt(b?.amount)}
                        </span>
                      </div>
                      {b?.canDownload ? (
                        <>
                          <HrButton variant="ghost" size="sm" onClick={b?.onDownload} data-tip={b?.tip}>
                            {txt(v.icDownload)}{" File"}
                          </HrButton>
                        </>
                      ) : null}
                      {b?.isPaid ? (
                        <>
                          <HrStatusPill tone="ok">
                            {"Paid"}
                          </HrStatusPill>
                        </>
                      ) : null}
                    </div>
                  </Fragment>
                ))}
                <button type="button" onClick={v.openBank} data-tip="→ /hrms/bank-disbursement" style={{justifySelf: "start", padding: "0", border: "0", background: "none", font: "inherit", fontSize: "13px", fontWeight: "700", color: "#0f6e56", cursor: "pointer"}} className="dc-payroll-overview-1">
                  {"Open Bank Disbursement →"}
                </button>
              </section>
            </>
          ) : null}
          <section style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "20px 22px", display: "grid", gap: "14px"}}>
            <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
              {"Activity"}
            </h3>
            <ol style={{listStyle: "none", margin: "0", padding: "0", display: "grid", gap: "14px"}}>
              {arr(v.log).map((a: any, $index: number) => (
                <Fragment key={$index}>
                  <li style={{display: "grid", gridTemplateColumns: "12px minmax(0,1fr)", gap: "10px"}}>
                    <svg width="10" height="10" aria-hidden="true" style={{marginTop: "5px"}}>
                      <circle cx="5" cy="5" r="5" fill={a?.dot} />
                    </svg>
                    <div style={{display: "grid", gap: "2px"}}>
                      <span style={{fontSize: "13px", lineHeight: "1.45", color: "#0f172a", textWrap: "pretty"}}>
                        {txt(a?.what)}
                      </span>
                      <span style={{fontSize: "12px", color: "#64748b", fontVariantNumeric: "tabular-nums"}}>
                        {txt(a?.meta)}
                      </span>
                    </div>
                  </li>
                </Fragment>
              ))}
            </ol>
          </section>
        </div>
      </div>
    </>
  )
}
