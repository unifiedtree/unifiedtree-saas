// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component ShiftRoster.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { HrAvatar, HrButton, HrDrawer } from '@/shared/components/hr'
import { EmptyState } from '@/shared/components/EmptyState'
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { arr, txt } from './dc-runtime'
import { DatePicker } from './DatePicker'
import { SectionState } from './SectionState'
import { ShiftBar } from './ShiftBar'
import { SubTabs } from './SubTabs'
import './ShiftRoster.view.css'

export function ShiftRosterView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "grid", gap: "14px", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        <div style={{display: "flex", flexWrap: "wrap", alignItems: "center", justifyContent: "space-between", gap: "10px 16px"}}>
          <SubTabs items={v.chips} label="Show people on" />
          <label style={{position: "relative", flex: "0 1 280px", minWidth: "200px"}}>
            <span style={{position: "absolute", left: "12px", top: "50%", transform: "translateY(-50%)", display: "inline-flex", color: "#94a3b8"}}>
              {txt(v.icSearch)}
            </span>
            <input type="search" value={v.q} onChange={v.setQ} placeholder="Find a person or code" aria-label="Find a person" style={{width: "100%", boxSizing: "border-box", height: "38px", padding: "0 12px 0 36px", border: "1px solid #e2e8f0", borderRadius: "10px", background: "#fff", font: "inherit", fontSize: "13.5px", outline: "none"}} className="dc-shift-roster-0" />
          </label>
        </div>
        {v.isError ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", padding: "8px"}}>
              <SectionState kind="error" retry={v.retry} />
            </div>
          </>
        ) : null}
        {v.isEmpty ? (
          <>
            <div style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px"}}>
              <EmptyState icon={v.emptyIcon} title="No one on the roster yet" description="People appear here once they’re added to the workforce directory." />
            </div>
          </>
        ) : null}
        {v.isLoading ? (
          <>
            <SkeletonBlock {...(v.sk || {})} />
          </>
        ) : null}
        {v.showList ? (
          <>
            <section style={{background: "#fff", border: "1px solid #e2e8f0", borderRadius: "18px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", overflow: "hidden"}}>
              {v.isDesktop ? (
                <>
                  <div aria-hidden="true" style={{display: "grid", gridTemplateColumns: "minmax(0,1.7fr) minmax(0,1fr) minmax(0,1.7fr) minmax(0,1fr) 150px", gap: "16px", padding: "11px 18px", background: "#f8fafc", borderBottom: "1px solid #eef2f1", fontSize: "11px", fontWeight: "700", letterSpacing: ".06em", textTransform: "uppercase", color: "#64748b"}}>
                    <span>
                      {"Person"}
                    </span>
                    <span>
                      {"Department"}
                    </span>
                    <span>
                      {"Shift"}
                    </span>
                    <span>
                      {"Since"}
                    </span>
                    <span />
                  </div>
                </>
              ) : null}
              {arr(v.rows).map((r: any, $index: number) => (
                <Fragment key={$index}>
                  {v.isDesktop ? (
                    <>
                      <div style={{display: "grid", gridTemplateColumns: "minmax(0,1.7fr) minmax(0,1fr) minmax(0,1.7fr) minmax(0,1fr) 150px", gap: "16px", alignItems: "center", padding: "12px 18px", borderBottom: "1px solid #f1f5f9"}}>
                        <HrAvatar name={r?.name} sub={r?.code} />
                        <span style={{fontSize: "13.5px", color: "#475569"}}>
                          {txt(r?.dept)}
                        </span>
                        {r?.hasShift ? (
                          <>
                            <span style={{display: "inline-flex", alignItems: "center", gap: "10px", minWidth: "0"}}>
                              <span style={{position: "relative", flex: "0 0 32px", width: "32px", height: "32px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="32" height="32" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="31" height="31" rx="10" fill={r?.tint} stroke={r?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(r?.iconEl)}
                                </span>
                              </span>
                              <span style={{display: "grid", minWidth: "0"}}>
                                <strong style={{fontSize: "13.5px"}}>
                                  {txt(r?.shiftName)}
                                </strong>
                                <span style={{fontSize: "12px", color: "#64748b", fontVariantNumeric: "tabular-nums", whiteSpace: "nowrap"}}>
                                  {txt(r?.range)}
                                </span>
                              </span>
                            </span>
                          </>
                        ) : null}
                        {r?.noShift ? (
                          <>
                            <span style={{justifySelf: "start", display: "inline-flex", alignItems: "center", gap: "6px", padding: "4px 10px", borderRadius: "999px", background: "#fffbeb", border: "1px solid #fde68a", color: "#92400e", fontSize: "12.5px", fontWeight: "700"}}>
                              {txt(v.icAlert)}{" No shift yet"}
                            </span>
                          </>
                        ) : null}
                        <span style={{fontSize: "13px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                          {txt(r?.since)}
                        </span>
                        <span style={{justifySelf: "end"}}>
                          {r?.hasShift ? (
                            <>
                              <HrButton variant="ghost" size="sm" onClick={r?.onChange} data-tip={r?.tip}>
                                {txt(v.icSwap)}{" Change shift"}
                              </HrButton>
                            </>
                          ) : null}
                          {r?.noShift ? (
                            <>
                              <HrButton size="sm" onClick={r?.onChange} data-tip={r?.tip}>
                                {txt(v.icPlus)}{" Assign shift"}
                              </HrButton>
                            </>
                          ) : null}
                        </span>
                      </div>
                    </>
                  ) : null}
                  {v.isMobile ? (
                    <>
                      <div style={{display: "grid", gap: "10px", padding: "14px 16px", borderBottom: "1px solid #f1f5f9"}}>
                        <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px"}}>
                          <HrAvatar name={r?.name} sub={r?.dept} />
                          {r?.hasShift ? (
                            <>
                              <HrButton variant="ghost" size="sm" onClick={r?.onChange}>
                                {"Change"}
                              </HrButton>
                            </>
                          ) : null}
                          {r?.noShift ? (
                            <>
                              <HrButton size="sm" onClick={r?.onChange}>
                                {"Assign"}
                              </HrButton>
                            </>
                          ) : null}
                        </div>
                        {r?.hasShift ? (
                          <>
                            <span style={{display: "inline-flex", alignItems: "center", gap: "10px"}}>
                              <span style={{position: "relative", flex: "0 0 28px", width: "28px", height: "28px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="28" height="28" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="27" height="27" rx="9" fill={r?.tint} stroke={r?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(r?.iconEl)}
                                </span>
                              </span>
                              <span style={{fontSize: "13px"}}>
                                <strong>
                                  {txt(r?.shiftName)}
                                </strong>
                                <span style={{color: "#64748b"}}>
                                  {" · "}{txt(r?.range)}{" · since "}{txt(r?.since)}
                                </span>
                              </span>
                            </span>
                          </>
                        ) : null}
                        {r?.noShift ? (
                          <>
                            <span style={{justifySelf: "start", display: "inline-flex", alignItems: "center", gap: "6px", padding: "4px 10px", borderRadius: "999px", background: "#fffbeb", border: "1px solid #fde68a", color: "#92400e", fontSize: "12.5px", fontWeight: "700"}}>
                              {txt(v.icAlert)}{" No shift yet · "}{txt(r?.since)}
                            </span>
                          </>
                        ) : null}
                      </div>
                    </>
                  ) : null}
                </Fragment>
              ))}
              {v.noRows ? (
                <>
                  <p style={{margin: "0", padding: "28px", textAlign: "center", fontSize: "13.5px", color: "#64748b"}}>
                    {"No one matches. Try another name or shift."}
                  </p>
                </>
              ) : null}
              <div style={{display: "flex", flexWrap: "wrap", justifyContent: "space-between", gap: "6px 12px", padding: "12px 18px", background: "#fbfdfc", fontSize: "12.5px", color: "#64748b"}}>
                <span>
                  {txt(v.footLine)}
                </span>
                <span>
                  {"Changes start on the date you pick."}
                </span>
              </div>
            </section>
          </>
        ) : null}
      </div>
      {v.drawerOpen ? (
        <>
          <HrDrawer title={v.drawerTitle} onClose={v.close} footer={v.drawerFooter} width="max-w-md">
            <div style={{display: "grid", gap: "16px", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
              <div style={{display: "grid", gap: "8px", padding: "12px 14px", borderRadius: "12px", background: "#f8fafc", border: "1px solid #eef2f1"}}>
                <HrAvatar name={v.emp?.name} sub={v.emp?.sub} />
                <span style={{fontSize: "13px", color: "#475569"}}>
                  {txt(v.emp?.nowText)}
                </span>
              </div>
              <div style={{display: "grid", gap: "8px"}}>
                <span style={{fontSize: "13px", fontWeight: "600"}}>
                  {"Pick the new shift"}
                </span>
                <div role="radiogroup" aria-label="Shifts" style={{display: "grid", gap: "8px"}}>
                  {arr(v.opts).map((o: any, $index: number) => (
                    <Fragment key={$index}>
                      {o?.active ? (
                        <>
                          <button type="button" role="radio" aria-checked="true" onClick={o?.onClick} style={{display: "grid", gap: "10px", padding: "12px 14px", borderRadius: "14px", border: "2px solid #0f6e56", background: "#f0fdf8", textAlign: "left", font: "inherit", cursor: "pointer"}}>
                            <span style={{display: "flex", alignItems: "center", gap: "12px"}}>
                              <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={o?.tint} stroke={o?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(o?.iconEl)}
                                </span>
                              </span>
                              <span style={{flex: "1", display: "grid", minWidth: "0"}}>
                                <strong style={{fontSize: "14px"}}>
                                  {txt(o?.name)}
                                </strong>
                                <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                                  {txt(o?.range)}
                                </span>
                              </span>
                              <span style={{flex: "0 0 22px", width: "22px", height: "22px", borderRadius: "999px", background: "#0f6e56", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                {txt(v.icCheck)}
                              </span>
                            </span>
                            <ShiftBar start={o?.start} end={o?.end} color={o?.bar} />
                          </button>
                        </>
                      ) : null}
                      {o?.inactive ? (
                        <>
                          <button type="button" role="radio" aria-checked="false" onClick={o?.onClick} style={{display: "grid", gap: "10px", padding: "13px 15px", borderRadius: "14px", border: "1px solid #e2e8f0", background: "#fff", textAlign: "left", font: "inherit", cursor: "pointer"}} className="dc-shift-roster-1">
                            <span style={{display: "flex", alignItems: "center", gap: "12px"}}>
                              <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={o?.tint} stroke={o?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(o?.iconEl)}
                                </span>
                              </span>
                              <span style={{flex: "1", display: "grid", minWidth: "0"}}>
                                <strong style={{fontSize: "14px"}}>
                                  {txt(o?.name)}
                                </strong>
                                <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                                  {txt(o?.range)}
                                </span>
                              </span>
                              <span style={{flex: "0 0 22px", width: "22px", height: "22px", boxSizing: "border-box", borderRadius: "999px", border: "2px solid #cbd5e1"}} />
                            </span>
                            <ShiftBar start={o?.start} end={o?.end} color={o?.bar} />
                          </button>
                        </>
                      ) : null}
                      {o?.current ? (
                        <>
                          <div aria-disabled="true" style={{display: "grid", gap: "10px", padding: "13px 15px", borderRadius: "14px", border: "1px dashed #cbd5e1", background: "#f8fafc", opacity: ".7"}}>
                            <span style={{display: "flex", alignItems: "center", gap: "12px"}}>
                              <span style={{position: "relative", flex: "0 0 34px", width: "34px", height: "34px", display: "inline-flex", alignItems: "center", justifyContent: "center"}}>
                                <svg width="34" height="34" aria-hidden="true" style={{position: "absolute", left: "0", top: "0"}}>
                                  <rect x="0.5" y="0.5" width="33" height="33" rx="10" fill={o?.tint} stroke={o?.edge} />
                                </svg>
                                <span style={{position: "relative", display: "inline-flex"}}>
                                  {txt(o?.iconEl)}
                                </span>
                              </span>
                              <span style={{flex: "1", display: "grid", minWidth: "0"}}>
                                <strong style={{fontSize: "14px"}}>
                                  {txt(o?.name)}
                                </strong>
                                <span style={{fontSize: "12.5px", color: "#475569", fontVariantNumeric: "tabular-nums"}}>
                                  {txt(o?.range)}
                                </span>
                              </span>
                              <span style={{fontSize: "11px", fontWeight: "800", letterSpacing: ".06em", textTransform: "uppercase", color: "#64748b"}}>
                                {"Current"}
                              </span>
                            </span>
                            <ShiftBar start={o?.start} end={o?.end} color={o?.bar} />
                          </div>
                        </>
                      ) : null}
                    </Fragment>
                  ))}
                </div>
              </div>
              <div style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                <span>
                  {"Starts from *"}
                </span>
                <DatePicker value={v.f?.date} min={v.tomorrowMin} onChange={v.setDate} label="Starts from" />
                <span style={{fontSize: "12px", fontWeight: "400", color: "#64748b"}}>
                  {txt(v.dateHelp)}
                </span>
              </div>
              <label style={{display: "grid", gap: "6px", fontSize: "13px", fontWeight: "600"}}>
                {"Note (optional)"}
                <textarea value={v.f?.note} onChange={v.setNote} rows={2} maxLength={200} placeholder="e.g. Swapped with Vikram for the quarter" style={{font: "inherit", fontWeight: "400", padding: "9px 12px", border: "1px solid #cbd5e1", borderRadius: "8px", outline: "none", resize: "vertical"}} className="dc-shift-roster-0">

                </textarea>
              </label>
            </div>
          </HrDrawer>
        </>
      ) : null}
    </>
  )
}
