// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component GeofenceMap.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { SkeletonBlock } from '@/shared/components/SkeletonCard'
import { txt } from './dc-runtime'

export function GeofenceMapView({ v }: { v: any }) {
  return (
    <>
      <div style={{position: "relative", height: "240px", borderRadius: "14px", overflow: "hidden", border: "1px solid #e2e8f0", background: "#eef2f1"}}>
        <div ref={v.mapRef} aria-label="Geofence map. Click to move the pin." style={{position: "absolute", inset: "0"}} />
        {v.loading ? (
          <>
            <div style={{position: "absolute", inset: "0", zIndex: "600", padding: "12px", background: "#f8fafc"}}>
              <SkeletonBlock {...(v.sk || {})} />
            </div>
          </>
        ) : null}
        {v.failed ? (
          <>
            <div style={{position: "absolute", inset: "0", zIndex: "600", display: "flex", alignItems: "center", justifyContent: "center", textAlign: "center", padding: "16px", background: "repeating-linear-gradient(135deg,#f1f5f9 0 10px,#f8fafc 10px 20px)", fontFamily: "'JetBrains Mono',ui-monospace,monospace", fontSize: "12px", color: "#64748b"}}>
              {"Map tiles unavailable. Enter coordinates below."}
            </div>
          </>
        ) : null}
        {v.readOnly ? (
          <>
            <div style={{position: "absolute", inset: "0", zIndex: "650", cursor: "not-allowed"}} />
          </>
        ) : null}
        <span style={{position: "absolute", right: "10px", bottom: "10px", zIndex: "640", display: "inline-flex", alignItems: "center", gap: "6px", padding: "4px 10px", borderRadius: "999px", background: "#0f6e56", color: "#fff", fontFamily: "Inter,sans-serif", fontSize: "12px", fontWeight: "700", fontVariantNumeric: "tabular-nums", boxShadow: "0 4px 12px -4px rgba(10,82,64,.6)"}}>
          {txt(v.radiusLabel)}
        </span>
        {v.editable ? (
          <>
            <span style={{position: "absolute", left: "52px", top: "10px", zIndex: "640", padding: "4px 10px", borderRadius: "8px", background: "#fff", border: "1px solid #e2e8f0", fontFamily: "Inter,sans-serif", fontSize: "12px", color: "#475569", whiteSpace: "nowrap", boxShadow: "0 2px 6px rgba(15,23,42,.08)"}}>
              {"Click the map to move the pin"}
            </span>
          </>
        ) : null}
      </div>
    </>
  )
}
