// GENERATED by scripts/dc-to-tsx.mjs from the Claude Design component AttMine.dc.html.
// Do not edit by hand — change the design (or the view model) and regenerate.
import { Fragment } from 'react'
import { EmptyState } from '@/shared/components/EmptyState'
import { arr, txt } from './dc-runtime'
import { StatTile } from './StatTile'

export function AttMineView({ v }: { v: any }) {
  return (
    <>
      <div style={{display: "flex", flexWrap: "wrap", gap: "16px", alignItems: "flex-start", minWidth: "0", fontFamily: "Inter,-apple-system,sans-serif", color: "#0f172a"}}>
        <section style={{flex: "1 1 300px", minWidth: "0", display: "grid", gap: "10px"}}>
          <p style={{margin: "0", fontSize: "11px", fontWeight: "700", letterSpacing: ".08em", textTransform: "uppercase", color: "#64748b"}}>
            {txt(v.monthLabel)}{" summary"}
          </p>
          <div style={{display: "grid", gridTemplateColumns: "repeat(2,minmax(0,1fr))", gap: "10px"}}>
            {arr(v.tiles).map((t: any, $index: number) => (
              <Fragment key={$index}>
                <StatTile tile={t} />
              </Fragment>
            ))}
          </div>
        </section>
        <section style={{flex: "1.6 1 380px", minWidth: "0", background: "#fff", border: "1px solid #e2e8f0", borderRadius: "16px", boxShadow: "0 1px 2px rgba(15,23,42,.04)", padding: "18px 20px", display: "grid", gap: "12px"}}>
          <div style={{display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px"}}>
            <h3 style={{margin: "0", fontFamily: "'Plus Jakarta Sans',Inter,sans-serif", fontSize: "16px", fontWeight: "700"}}>
              {"Daily log"}
            </h3>
            <span style={{fontSize: "12px", color: "#475569", background: "#f8fafc", border: "1px solid #e2e8f0", borderRadius: "999px", padding: "3px 10px"}}>
              {txt(v.monthLabel)}
            </span>
          </div>
          {v.isError ? (
            <>
              <EmptyState icon={v.errIcon} title="Unable to load your attendance" description="This month's log didn't load." action={v.retry} />
            </>
          ) : null}
          {v.notError ? (
            <>
              <div style={{display: "grid", gridTemplateColumns: "repeat(7,minmax(0,1fr))", gap: "6px", fontSize: "11px", fontWeight: "700", letterSpacing: ".04em", textTransform: "uppercase", color: "#64748b", textAlign: "center"}}>
                <span>
                  {"Mon"}
                </span>
                <span>
                  {"Tue"}
                </span>
                <span>
                  {"Wed"}
                </span>
                <span>
                  {"Thu"}
                </span>
                <span>
                  {"Fri"}
                </span>
                <span>
                  {"Sat"}
                </span>
                <span>
                  {"Sun"}
                </span>
              </div>
              <div style={{display: "grid", gridTemplateColumns: "repeat(7,minmax(0,1fr))", gap: "6px"}}>
                {arr(v.cells).map((c: any, $index: number) => (
                  <Fragment key={$index}>
                    <svg viewBox="0 0 40 40" role="img" aria-label={c?.title} style={{display: "block", width: "100%", height: "auto"}}>
                      <title>
                        {txt(c?.title)}
                      </title>
                      <rect x="1" y="1" width="38" height="38" rx="8" fill={c?.bg} stroke={c?.stroke} strokeWidth="2" />
                      <text x="20" y="25" textAnchor="middle" fill={c?.fg} fontSize="13" fontWeight="700" fontFamily="Inter,sans-serif">
                        {txt(c?.day)}
                      </text>
                    </svg>
                  </Fragment>
                ))}
              </div>
              <div style={{display: "flex", flexWrap: "wrap", gap: "6px 14px", fontSize: "12px", color: "#475569"}}>
                {arr(v.legend).map((l: any, $index: number) => (
                  <Fragment key={$index}>
                    <span style={{display: "inline-flex", alignItems: "center", gap: "6px"}}>
                      <svg width="12" height="12" aria-hidden="true">
                        <rect width="12" height="12" rx="3" fill={l?.bg} />
                      </svg>
                      {txt(l?.label)}
                    </span>
                  </Fragment>
                ))}
              </div>
            </>
          ) : null}
        </section>
      </div>
    </>
  )
}
