/* eslint-disable */
// @ts-nocheck
// Generated by scripts/master-build.mjs from docs/Designs/UnifiedTree Master (offline).html — do not edit.
// The design's own components, verbatim apart from the PATCHES listed in the script.
import * as React from 'react'
import * as ReactDOM from 'react-dom'
import { portalHost, TODAY, TODAY_ISO, NEXT_MONTH, NONE, orNone, pl } from './masterRuntime'
const ICONS={
search:'<circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>',
settings:'<path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"/><circle cx="12" cy="12" r="3"/>',
'layout-grid':'<rect width="7" height="7" x="3" y="3" rx="1"/><rect width="7" height="7" x="14" y="3" rx="1"/><rect width="7" height="7" x="14" y="14" rx="1"/><rect width="7" height="7" x="3" y="14" rx="1"/>',
'layout-dashboard':'<rect width="7" height="9" x="3" y="3" rx="1"/><rect width="7" height="5" x="14" y="3" rx="1"/><rect width="7" height="9" x="14" y="12" rx="1"/><rect width="7" height="5" x="3" y="16" rx="1"/>',
bell:'<path d="M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9"/><path d="M10.3 21a1.94 1.94 0 0 0 3.4 0"/>',
'chevron-down':'<path d="m6 9 6 6 6-6"/>','chevron-up':'<path d="m18 15-6-6-6 6"/>','chevron-right':'<path d="m9 18 6-6-6-6"/>','chevron-left':'<path d="m15 18-6-6 6-6"/>',
'chevrons-up-down':'<path d="m7 15 5 5 5-5"/><path d="m7 9 5-5 5 5"/>','chevrons-down-up':'<path d="m7 20 5-5 5 5"/><path d="m7 4 5 5 5-5"/>',
'arrow-right':'<path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>',
users:'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>',
user:'<path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>',
'user-check':'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><polyline points="16 11 18 13 22 9"/>',
'user-plus':'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="19" x2="19" y1="8" y2="14"/><line x1="22" x2="16" y1="11" y2="11"/>',
'user-x':'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="17" x2="22" y1="8" y2="13"/><line x1="22" x2="17" y1="8" y2="13"/>',
'building-2':'<path d="M6 22V4a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v18Z"/><path d="M6 12H4a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h2"/><path d="M18 9h2a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2h-2"/><path d="M10 6h4"/><path d="M10 10h4"/><path d="M10 14h4"/><path d="M10 18h4"/>',
building:'<rect width="16" height="20" x="4" y="2" rx="2"/><path d="M9 22v-4h6v4"/><path d="M8 6h.01"/><path d="M16 6h.01"/><path d="M12 6h.01"/><path d="M12 10h.01"/><path d="M12 14h.01"/><path d="M16 10h.01"/><path d="M16 14h.01"/><path d="M8 10h.01"/><path d="M8 14h.01"/>',
landmark:'<line x1="3" x2="21" y1="22" y2="22"/><line x1="6" x2="6" y1="18" y2="11"/><line x1="10" x2="10" y1="18" y2="11"/><line x1="14" x2="14" y1="18" y2="11"/><line x1="18" x2="18" y1="18" y2="11"/><polygon points="12 2 20 7 4 7"/>',
'git-branch':'<line x1="6" x2="6" y1="3" y2="15"/><circle cx="18" cy="6" r="3"/><circle cx="6" cy="18" r="3"/><path d="M18 9a9 9 0 0 1-9 9"/>',
layers:'<path d="m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z"/><path d="m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65"/><path d="m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65"/>',
award:'<circle cx="12" cy="8" r="6"/><path d="M15.477 12.89 17 22l-5-3-5 3 1.523-9.11"/>',
'bar-chart-3':'<path d="M3 3v18h18"/><path d="M18 17V9"/><path d="M13 17V5"/><path d="M8 17v-3"/>',
briefcase:'<rect width="20" height="14" x="2" y="7" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/>',
clock:'<circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>',
timer:'<line x1="10" x2="14" y1="2" y2="2"/><line x1="12" x2="15" y1="14" y2="11"/><circle cx="12" cy="14" r="8"/>',
'clipboard-list':'<rect width="8" height="4" x="8" y="2" rx="1" ry="1"/><path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/><path d="M12 11h4"/><path d="M12 16h4"/><path d="M8 11h.01"/><path d="M8 16h.01"/>',
receipt:'<path d="M4 2v20l2-1 2 1 2-1 2 1 2-1 2 1 2-1 2 1V2l-2 1-2-1-2 1-2-1-2 1-2-1-2 1Z"/><path d="M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8"/><path d="M12 17.5v-11"/>',
'file-text':'<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v4a2 2 0 0 0 2 2h4"/><path d="M10 9H8"/><path d="M16 13H8"/><path d="M16 17H8"/>',
'file-bar-chart':'<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v4a2 2 0 0 0 2 2h4"/><path d="M8 18v-2"/><path d="M12 18v-4"/><path d="M16 18v-6"/>',
'scroll-text':'<path d="M15 12h-5"/><path d="M15 8h-5"/><path d="M19 17V5a2 2 0 0 0-2-2H4"/><path d="M8 21h12a2 2 0 0 0 2-2v-1a1 1 0 0 0-1-1H11a1 1 0 0 0-1 1v1a2 2 0 1 1-4 0V5a2 2 0 1 0-4 0v2a1 1 0 0 0 1 1h3"/>',
calendar:'<rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/>',
'calendar-check':'<rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/><path d="m9 16 2 2 4-4"/>',
plus:'<path d="M5 12h14"/><path d="M12 5v14"/>',
download:'<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/>',
upload:'<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" x2="12" y1="3" y2="15"/>',
'more-vertical':'<circle cx="12" cy="12" r="1"/><circle cx="12" cy="5" r="1"/><circle cx="12" cy="19" r="1"/>',
pencil:'<path d="M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"/><path d="m15 5 4 4"/>',
trash:'<path d="M3 6h18"/><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"/><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"/><line x1="10" x2="10" y1="11" y2="17"/><line x1="14" x2="14" y1="11" y2="17"/>',
x:'<path d="M18 6 6 18"/><path d="m6 6 12 12"/>',
check:'<path d="M20 6 9 17l-5-5"/>',
'check-circle':'<path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>',
'minus-circle':'<circle cx="12" cy="12" r="10"/><path d="M8 12h8"/>',
ban:'<circle cx="12" cy="12" r="10"/><path d="m4.9 4.9 14.2 14.2"/>',
'map-pin':'<path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/>',
phone:'<path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>',
mail:'<rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>',
copy:'<rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/>',
eye:'<path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"/><circle cx="12" cy="12" r="3"/>',
archive:'<rect width="20" height="5" x="2" y="3" rx="1"/><path d="M4 8v11a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8"/><path d="M10 12h4"/>',
sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2"/><path d="M12 20v2"/><path d="m4.93 4.93 1.41 1.41"/><path d="m17.66 17.66 1.41 1.41"/><path d="M2 12h2"/><path d="M20 12h2"/><path d="m6.34 17.66-1.41 1.41"/><path d="m19.07 4.93-1.41 1.41"/>',
moon:'<path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9Z"/>',
sunrise:'<path d="M12 2v8"/><path d="m4.93 10.93 1.41 1.41"/><path d="M2 18h2"/><path d="M20 18h2"/><path d="m19.07 10.93-1.41 1.41"/><path d="M22 22H2"/><path d="m8 6 4-4 4 4"/><path d="M16 18a4 4 0 0 0-8 0"/>',
sunset:'<path d="M12 10V2"/><path d="m4.93 10.93 1.41 1.41"/><path d="M2 18h2"/><path d="M20 18h2"/><path d="m19.07 10.93-1.41 1.41"/><path d="M22 22H2"/><path d="m16 6-4 4-4-4"/><path d="M16 18a4 4 0 0 0-8 0"/>',
info:'<circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/>',
'alert-triangle':'<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"/><path d="M12 9v4"/><path d="M12 17h.01"/>',
'shield-check':'<path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/><path d="m9 12 2 2 4-4"/>',
wallet:'<path d="M21 12V7H5a2 2 0 0 1 0-4h14v4"/><path d="M3 5v14a2 2 0 0 0 2 2h16v-5"/><path d="M18 12a2 2 0 0 0 0 4h4v-4Z"/>',
'credit-card':'<rect width="20" height="14" x="2" y="5" rx="2"/><line x1="2" x2="22" y1="10" y2="10"/>',
rupee:'<path d="M6 3h12"/><path d="M6 8h12"/><path d="m6 13 8.5 8"/><path d="M6 13h3"/><path d="M9 13c6.667 0 6.667-10 0-10"/>',
code:'<polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/>',
palette:'<circle cx="13.5" cy="6.5" r=".5" fill="currentColor"/><circle cx="17.5" cy="10.5" r=".5" fill="currentColor"/><circle cx="8.5" cy="7.5" r=".5" fill="currentColor"/><circle cx="6.5" cy="12.5" r=".5" fill="currentColor"/><path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.926 0 1.648-.746 1.648-1.688 0-.437-.18-.835-.437-1.125-.29-.289-.438-.652-.438-1.125a1.64 1.64 0 0 1 1.668-1.668h1.996c3.051 0 5.555-2.503 5.555-5.554C21.965 6.012 17.461 2 12 2z"/>',
'trending-up':'<polyline points="22 7 13.5 15.5 8.5 10.5 2 17"/><polyline points="16 7 22 7 22 13"/>',
factory:'<path d="M2 20a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V8l-7 5V8l-7 5V4a2 2 0 0 0-2-2H4a2 2 0 0 0-2 2Z"/><path d="M17 18h1"/><path d="M12 18h1"/><path d="M7 18h1"/>',
warehouse:'<path d="M22 8.35V20a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V8.35A2 2 0 0 1 3.26 6.5l8-3.2a2 2 0 0 1 1.48 0l8 3.2A2 2 0 0 1 22 8.35Z"/><path d="M6 18h12"/><path d="M6 14h12"/><rect width="12" height="12" x="6" y="10"/>',
headphones:'<path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7a9 9 0 0 1 18 0v7a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2h3"/>',
laptop:'<path d="M20 16V7a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v9m16 0H4m16 0 1.28 2.55a1 1 0 0 1-.9 1.45H3.62a1 1 0 0 1-.9-1.45L4 16"/>',
lightbulb:'<path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/>',
truck:'<path d="M14 18V6a2 2 0 0 0-2-2H4a2 2 0 0 0-2 2v11a1 1 0 0 0 1 1h2"/><path d="M15 18H9"/><path d="M19 18h2a1 1 0 0 0 1-1v-3.65a1 1 0 0 0-.22-.624l-3.48-4.35A1 1 0 0 0 17.52 8H14"/><circle cx="17" cy="18" r="2"/><circle cx="7" cy="18" r="2"/>',
target:'<circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/>',
database:'<ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5V19A9 3 0 0 0 21 19V5"/><path d="M3 12A9 3 0 0 0 21 12"/>',
hourglass:'<path d="M5 22h14"/><path d="M5 2h14"/><path d="M17 22v-4.172a2 2 0 0 0-.586-1.414L12 12l-4.414 4.414A2 2 0 0 0 7 17.828V22"/><path d="M7 2v4.172a2 2 0 0 0 .586 1.414L12 12l4.414-4.414A2 2 0 0 0 17 6.172V2"/>',
send:'<path d="m22 2-7 20-4-9-9-4Z"/><path d="M22 2 11 13"/>',
percent:'<line x1="19" x2="5" y1="5" y2="19"/><circle cx="6.5" cy="6.5" r="2.5"/><circle cx="17.5" cy="17.5" r="2.5"/>',
calculator:'<rect width="16" height="20" x="4" y="2" rx="2"/><line x1="8" x2="16" y1="6" y2="6"/><line x1="16" x2="16" y1="14" y2="18"/><path d="M16 10h.01"/><path d="M12 10h.01"/><path d="M8 10h.01"/><path d="M12 14h.01"/><path d="M8 14h.01"/><path d="M12 18h.01"/><path d="M8 18h.01"/>',
'heart-pulse':'<path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/><path d="M3.22 12H9.5l.5-1 2 4.5 2-7 1.5 3.5h5.27"/>',
plane:'<path d="M17.8 19.2 16 11l3.5-3.5C21 6 21.5 4 21 3c-1-.5-3 0-4.5 1.5L13 8 4.8 6.2c-.5-.1-.9.1-1.1.5l-.3.5c-.2.5-.1 1 .3 1.3L9 12l-2 3H4l-1 1 3 2 2 3 1-1v-3l3-2 3.5 5.3c.3.4.8.5 1.3.3l.5-.2c.4-.3.6-.7.5-1.2z"/>',
baby:'<path d="M9 12h.01"/><path d="M15 12h.01"/><path d="M10 16c.5.3 1.2.5 2 .5s1.5-.2 2-.5"/><path d="M19 6.3a9 9 0 0 1 1.8 3.9 2 2 0 0 1 0 3.6 9 9 0 0 1-17.6 0 2 2 0 0 1 0-3.6A9 9 0 0 1 12 3c2 0 3.5 1.1 3.5 2.5s-.9 2.5-2 2.5c-.8 0-1.5-.4-1.5-1"/>',
coffee:'<path d="M17 8h1a4 4 0 1 1 0 8h-1"/><path d="M3 8h14v9a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4Z"/><line x1="6" x2="6" y1="2" y2="4"/><line x1="10" x2="10" y1="2" y2="4"/><line x1="14" x2="14" y1="2" y2="4"/>',
repeat:'<path d="m17 2 4 4-4 4"/><path d="M3 11v-1a4 4 0 0 1 4-4h14"/><path d="m7 22-4-4 4-4"/><path d="M21 13v1a4 4 0 0 1-4 4H3"/>',
'refresh-cw':'<path d="M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8"/><path d="M21 3v5h-5"/><path d="M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16"/><path d="M8 16H3v5"/>',
list:'<line x1="8" x2="21" y1="6" y2="6"/><line x1="8" x2="21" y1="12" y2="12"/><line x1="8" x2="21" y1="18" y2="18"/><line x1="3" x2="3.01" y1="6" y2="6"/><line x1="3" x2="3.01" y1="12" y2="12"/><line x1="3" x2="3.01" y1="18" y2="18"/>',
'log-out':'<path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" x2="9" y1="12" y2="12"/>',
'hard-hat':'<path d="M2 18a1 1 0 0 0 1 1h18a1 1 0 0 0 1-1v-2a1 1 0 0 0-1-1H3a1 1 0 0 0-1 1v2z"/><path d="M10 10V5a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v5"/><path d="M4 15v-3a6 6 0 0 1 6-6"/><path d="M14 6a6 6 0 0 1 6 6v3"/>',
tags:'<path d="m15 5 6.3 6.3a2.4 2.4 0 0 1 0 3.4L17 19"/><path d="M9.586 5.586A2 2 0 0 0 8.172 5H3a1 1 0 0 0-1 1v5.172a2 2 0 0 0 .586 1.414L8.29 18.29a2.426 2.426 0 0 0 3.42 0l3.58-3.58a2.426 2.426 0 0 0 0-3.42z"/><circle cx="6.5" cy="9.5" r=".5" fill="currentColor"/>',
'id-card':'<rect width="20" height="14" x="2" y="5" rx="2"/><path d="M16 10h2"/><path d="M16 14h2"/><path d="M6.17 15a3 3 0 0 1 5.66 0"/><circle cx="9" cy="11" r="2"/>',
'graduation-cap':'<path d="M21.42 10.922a1 1 0 0 0-.019-1.838L12.83 5.18a2 2 0 0 0-1.66 0L2.6 9.08a1 1 0 0 0 0 1.832l8.57 3.908a2 2 0 0 0 1.66 0z"/><path d="M22 10v6"/><path d="M6 12.5V16a6 3 0 0 0 12 0v-3.5"/>',
network:'<rect x="16" y="16" width="6" height="6" rx="1"/><rect x="2" y="16" width="6" height="6" rx="1"/><rect x="9" y="2" width="6" height="6" rx="1"/><path d="M5 16v-3a1 1 0 0 1 1-1h12a1 1 0 0 1 1 1v3"/><path d="M12 12V8"/>',
umbrella:'<path d="M22 12a10.06 10.06 1 0 0-20 0Z"/><path d="M12 12v8a2 2 0 0 0 4 0"/><path d="M12 2v1"/>',
lock:'<rect width="18" height="11" x="3" y="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/>',
sigma:'<path d="M18 7V5a1 1 0 0 0-1-1H6.5a.5.5 0 0 0-.4.8l4.5 6a2 2 0 0 1 0 2.4l-4.5 6a.5.5 0 0 0 .4.8H17a1 1 0 0 0 1-1v-2"/>',
home:'<path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>',
dot:'<circle cx="12" cy="12" r="1.5" fill="currentColor"/>'
};
function Icon({name,size=18,stroke=2,className,style}){return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={stroke} strokeLinecap="round" strokeLinejoin="round" className={className} style={style} aria-hidden="true" dangerouslySetInnerHTML={{__html:ICONS[name]||ICONS.dot}}></svg>}
const TONES={green:['#ecfdf5','#047857','#a7f3d0','#10b981'],blue:['#eff6ff','#1d4ed8','#bfdbfe','#3b82f6'],amber:['#fffbeb','#b45309','#fde68a','#f59e0b'],orange:['#fff7ed','#c2410c','#fed7aa','#f97316'],red:['#fef2f2','#b91c1c','#fecaca','#ef4444'],rose:['#fff1f2','#be123c','#fecdd3','#f43f5e'],violet:['#f5f3ff','#6d28d9','#ddd6fe','#8b5cf6'],indigo:['#eef2ff','#4338ca','#c7d2fe','#6366f1'],teal:['#f0fdfa','#0f766e','#99f6e4','#14b8a6'],cyan:['#ecfeff','#0e7490','#a5f3fc','#06b6d4'],pink:['#fdf2f8','#be185d','#fbcfe8','#ec4899'],slate:['#f1f5f9','#475569','#e2e8f0','#94a3b8'],brand:['#ecfdf5','#0f6e56','#a7f3d0','#0f6e56']};
function tone(t,x){const v=TONES[t]||TONES.slate;return Object.assign({'--tb':v[0],'--tf':v[1],'--tl':v[2],'--ts':v[3]},x||{})}
const STATUS_TONE={Active:'green',Probation:'amber','On notice':'orange',Exited:'slate',Inactive:'slate',Draft:'amber',Archived:'slate'};
const AppCtx=React.createContext(null);
function useApp(){return React.useContext(AppCtx)}
const asDate=s=>new Date(String(s).length===10?s+'T12:00:00':s);
const fmtINR=n=>'₹'+Math.round(n).toLocaleString('en-IN');
function fmtL(n){if(n>=1e7){const c=n/1e7;return '₹'+(Number.isInteger(c)?c:c.toFixed(2))+' Cr'}const l=n/1e5;return '₹'+(Number.isInteger(l)?l:l.toFixed(1))+'L'}
const fmtDate=s=>s?asDate(s).toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'}):'—';
function tenure(s){if(!s)return '—';const m=Math.floor((TODAY-asDate(s))/2629800000);if(m<1)return 'Joined this month';if(m<12)return m+' mo';const y=Math.floor(m/12),r=m%12;return y+' yr'+(r?' '+r+' mo':'')}
const initials=n=>String(n).replace(/[^A-Za-z ]/g,' ').split(' ').filter(Boolean).map(x=>x[0]).slice(0,2).join('').toUpperCase();
const hm=m=>String(Math.floor(m/60)%24).padStart(2,'0')+':'+String(m%60).padStart(2,'0');
const toMin=s=>{const p=String(s||'0:0').split(':').map(Number);return (p[0]||0)*60+(p[1]||0)};
const pct=(a,b)=>b?Math.round(a/b*100):0;
const daysTo=s=>Math.round((asDate(s)-TODAY)/864e5);
function Pill({s,t,children}){return <span className="pill" style={tone(t||STATUS_TONE[s]||'slate')}><i></i>{children||s}</span>}
const AV_TONES=['green','blue','violet','amber','teal','rose','indigo','cyan','orange','pink'];
function Av({name,size}){let h=0;for(const c of String(name))h=(h*31+c.charCodeAt(0))>>>0;return <span className={'av'+(size?' '+size:'')} style={tone(AV_TONES[h%AV_TONES.length])}>{initials(name)}</span>}
function Who({name,sub,sm}){return <div className={'who'+(sm?' sm':'')}><Av name={name} size={sm?'sm':''}/><div><b>{name}</b>{sub&&<small>{sub}</small>}</div></div>}
function Meter({v,wide}){return <div className={'meter'+(wide?' wide':'')}><span style={{width:Math.max(0,Math.min(100,v||0))+'%'}}></span></div>}
function Spark({data,w=100,h=38}){const mx=Math.max(...data),mn=Math.min(...data),r=(mx-mn)||1;const pts=data.map((v,i)=>[i*w/(data.length-1),h-4-(v-mn)/r*(h-10)]);const line=pts.map((p,i)=>(i?'L':'M')+p[0].toFixed(1)+' '+p[1].toFixed(1)).join(' ');const l=pts[pts.length-1];return <svg width={w} height={h} viewBox={'0 0 '+w+' '+h} aria-hidden="true" style={{display:'block',overflow:'visible'}}><path d={line+' L'+w+' '+h+' L0 '+h+' Z'} fill="var(--tb)"></path><path d={line} fill="none" stroke="var(--ts)" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"></path><circle cx={l[0]} cy={l[1]} r="3.4" fill="#fff" stroke="var(--ts)" strokeWidth="2"></circle></svg>}
function Stat({icon,label,value,unit,sub,t='blue',on,onClick,z}){return <div className={'stat'+(onClick?' click':'')+(on?' on':'')} style={tone(t)} onClick={onClick} role={onClick?'button':undefined} tabIndex={onClick?0:undefined} onKeyDown={onClick?e=>{if(e.key==='Enter')onClick()}:undefined}><span className="stat-ico"><Icon name={icon} size={20}/></span><div className="stat-m"><div className="stat-l">{label}</div><div className="stat-v">{value}{unit&&<small>{unit}</small>}</div>{sub&&<div className="stat-s">{sub}</div>}</div>{z&&<div className="stat-z">{z}</div>}</div>}
function Popover({anchor,open,onClose,align,width,children}){
const [pos,setPos]=React.useState(null);const pop=React.useRef(null);
React.useLayoutEffect(()=>{if(!open||!anchor.current)return;const r=anchor.current.getBoundingClientRect();const w=width||Math.max(r.width,210);let left=align==='right'?r.right-w:r.left;left=Math.max(8,Math.min(left,window.innerWidth-w-8));const below=window.innerHeight-r.bottom;setPos(below<300&&r.top>below?{left,bottom:window.innerHeight-r.top+6,width:w}:{left,top:r.bottom+6,width:w})},[open]);
React.useEffect(()=>{if(!open)return;const inPop=e=>pop.current&&pop.current.contains(e.target);const md=e=>{if(!inPop(e)&&!(anchor.current&&anchor.current.contains(e.target)))onClose()};const key=e=>{if(e.key==='Escape')onClose()};const sc=e=>{if(!inPop(e))onClose()};document.addEventListener('mousedown',md);document.addEventListener('keydown',key);window.addEventListener('scroll',sc,true);window.addEventListener('resize',onClose);return()=>{document.removeEventListener('mousedown',md);document.removeEventListener('keydown',key);window.removeEventListener('scroll',sc,true);window.removeEventListener('resize',onClose)}},[open]);
if(!open||!pos)return null;
return ReactDOM.createPortal(<div ref={pop} className="pop" style={Object.assign({position:'fixed'},pos)}>{children}</div>,portalHost());
}
function Dropdown({label,value,options,onChange,all,placeholder,field,sm,search,icon,width,align,disabled}){
const ref=React.useRef(null);const [open,setOpen]=React.useState(false);const [q,setQ]=React.useState('');
const opts=(options||[]).map(o=>typeof o==='string'?{v:o,l:o}:o);const list=all?[{v:'',l:all}].concat(opts):opts;
const cur=list.find(o=>o.v===value);const shown=q?list.filter(o=>o.l.toLowerCase().includes(q.toLowerCase())):list;
const isSet=!field&&!!value;const close=()=>{setOpen(false);setQ('')};
return <div className={'dd'+(field?' field':'')+(sm?' sm':'')}>
<button type="button" ref={ref} disabled={disabled} className={'ddb'+(open?' open':'')+(isSet?' set':'')} onClick={()=>{if(!disabled)setOpen(o=>!o)}}>{icon&&<Icon name={icon} size={16}/>}<span>{field?(cur?cur.l:<span className="ph">{placeholder||'Select…'}</span>):(isSet?<>{label}: <b>{cur?cur.l:value}</b></>:(all||label))}</span><Icon name="chevron-down" size={16} className="car"/></button>
<Popover anchor={ref} open={open} onClose={close} width={width} align={align}>
{search&&<div className="pop-s"><Icon name="search" size={15}/><input autoFocus placeholder="Search…" value={q} onChange={e=>setQ(e.target.value)}/></div>}
<div className="pop-list">{shown.length?shown.map(o=><div key={o.v||'_all'} className={'opt'+(o.v===value?' on':'')} onClick={()=>{onChange(o.v);close()}}>{o.t&&<i className="dot" style={tone(o.t)}></i>}<span>{o.l}</span>{o.sub&&<small>{o.sub}</small>}{o.v===value&&<Icon name="check" size={15} className="ck"/>}</div>):<div className="opt muted">No matches</div>}</div>
</Popover></div>
}
function Menu({items,label='More actions',children,btn='btn sm',align='right'}){
const ref=React.useRef(null);const [open,setOpen]=React.useState(false);const tg=e=>{e.stopPropagation();setOpen(o=>!o)};
return <>{children?<button type="button" ref={ref} className={btn} onClick={tg}>{children}</button>:<button type="button" ref={ref} className={'ib'+(open?' on':'')} aria-label={label} onClick={tg}><Icon name="more-vertical" size={17}/></button>}
<Popover anchor={ref} open={open} onClose={()=>setOpen(false)} align={align} width={224}><div className="pop-list" onClick={e=>e.stopPropagation()}>{items.filter(Boolean).map((it,i)=>it==='-'?<div key={i} className="sep"></div>:<div key={i} className={'opt'+(it.danger&&!it.soon?' danger':'')} title={it.soon?(it.tip||'Coming soon'):undefined} style={it.soon?{cursor:'not-allowed',color:'var(--text-disabled)'}:undefined} onClick={()=>{if(it.soon)return;setOpen(false);it.on&&it.on()}}>{it.icon&&<Icon name={it.icon} size={16}/>}<span>{it.label}</span>{it.soon&&<small>{it.soonLabel||'Coming soon'}</small>}</div>)}</div></Popover></>
}
const noLayerOpen=()=>!document.querySelector('#utm-portal>.pop');
function Drawer({title,sub,icon,t='brand',lead,onClose,children,footer,width}){
React.useEffect(()=>{const k=e=>{if(e.key==='Escape'&&noLayerOpen()&&!document.querySelector('.modal'))onClose()};document.addEventListener('keydown',k);return()=>document.removeEventListener('keydown',k)},[]);
return ReactDOM.createPortal(<><div className="scrim" onClick={onClose}></div><aside className="drawer" role="dialog" aria-label={title} style={width?{width:'min('+width+'px,100vw)'}:undefined}><div className="dr-h">{lead||(icon&&<span className="tile" style={tone(t)}><Icon name={icon} size={19}/></span>)}<div><h3>{title}</h3>{sub&&<p>{sub}</p>}</div><button className="ib" onClick={onClose} aria-label="Close"><Icon name="x" size={18}/></button></div><div className="dr-b">{children}</div>{footer&&<div className="dr-f">{footer}</div>}</aside></>,portalHost());
}
function Modal({title,body,icon='info',t='blue',onClose,children,cta='Confirm',danger,onOk,okDisabled}){
React.useEffect(()=>{const k=e=>{if(e.key==='Escape'&&noLayerOpen())onClose()};document.addEventListener('keydown',k);return()=>document.removeEventListener('keydown',k)},[]);
return ReactDOM.createPortal(<><div className="scrim" style={{zIndex:210}} onClick={onClose}></div><div className="modal" style={{zIndex:211}} role="dialog" aria-label={title}><div className="md-b"><span className="tile" style={tone(t)}><Icon name={icon} size={19}/></span><div><h3>{title}</h3>{body&&<p>{body}</p>}</div></div>{children&&<div className="md-x">{children}</div>}<div className="md-f"><button className="btn" onClick={onClose}>Cancel</button><button className={'btn '+(danger?'danger':'pri')} disabled={okDisabled} onClick={onOk}>{cta}</button></div></div></>,portalHost());
}
function Toasts({items}){return <div className="toasts">{items.map(x=><div key={x.id} className="toast"><span className={'ti'+(x.kind==='info'?' info':x.kind==='error'?' err':'')}><Icon name={x.kind==='info'?'info':x.kind==='error'?'alert-triangle':'check'} size={15} stroke={2.6}/></span>{x.msg}</div>)}</div>}
function Switch({on,onChange,sm,label,disabled,title}){return <button type="button" role="switch" aria-checked={!!on} aria-label={label} disabled={disabled} title={title} className={'sw'+(on?' on':'')+(sm?' sm':'')} onClick={e=>{e.stopPropagation();if(!disabled)onChange(!on)}}></button>}
function Seg({value,options,onChange,full,disabled}){return <div className={'seg'+(full?' full':'')}>{options.map(o=>{const x=typeof o==='string'?{v:o,l:o}:o;return <button type="button" key={x.v} disabled={disabled||x.disabled} title={x.disabled?(x.tip||'Coming soon'):undefined} className={value===x.v?'on':''} onClick={()=>onChange(x.v)}>{x.icon&&<Icon name={x.icon} size={15}/>}{x.l}{x.n!=null&&<span className="n">{x.n}</span>}</button>})}</div>}
function Field({label,req,hint,err,span2,children}){return <div className={'field'+(span2?' span2':'')+(err?' err':'')}><label>{label}{req&&<em>*</em>}</label>{children}{(err||hint)&&<div className={'hint'+(err?' e':'')}>{err||hint}</div>}</div>}
function FormField({f,v,set,err}){
const val=v[f.k];const opts=typeof f.options==='function'?f.options(v):f.options;const ph=typeof f.placeholder==='function'?f.placeholder(v):f.placeholder;
if(f.type==='custom')return f.render(v,set,err);
if(f.type==='toggle')return <div className={'tgl'+(f.half?'':' span2')}><div><b>{f.label}</b>{f.hint&&<small>{f.hint}</small>}</div><Switch on={!!val} disabled={f.disabled} onChange={x=>set(f.k,x)} label={f.label}/></div>;
let c;
if(f.type==='select')c=<Dropdown field disabled={f.disabled} value={val==null?'':val} options={opts} onChange={x=>set(f.k,x)} placeholder={ph} search={f.search}/>;
else if(f.type==='seg')c=<Seg full disabled={f.disabled} value={val} options={opts} onChange={x=>set(f.k,x)}/>;
else if(f.type==='multi'){const arr=val||[];c=<div className="seg full">{opts.map(o=>{const x=typeof o==='string'?{v:o,l:o}:o;const on=arr.includes(x.v);return <button type="button" key={x.v} disabled={f.disabled} className={on?'on':''} aria-pressed={on} onClick={()=>set(f.k,on?arr.filter(y=>y!==x.v):arr.concat([x.v]))}>{on&&<Icon name="check" size={14} stroke={2.6}/>}{x.l}</button>})}</div>}
else if(f.type==='radio')c=<div className="rgrid">{opts.map(o=><button type="button" key={o.v} disabled={f.disabled} className={'rc'+(val===o.v?' on':'')} style={tone(o.t)} onClick={()=>set(f.k,o.v)}><i></i>{o.l}</button>)}</div>;
else if(f.type==='textarea')c=<textarea className="input" disabled={f.disabled} rows={f.rows||5} value={val||''} placeholder={ph} onChange={e=>set(f.k,e.target.value)}></textarea>;
else{const inp=<input className="input" disabled={f.disabled} type={f.type||'text'} value={val==null?'':val} placeholder={ph} min={f.min} max={f.max} step={f.step} style={f.suffix?{paddingRight:76}:undefined} onChange={e=>{let x=e.target.value;if(f.type==='number')x=x===''?'':Number(x);else if(f.upper)x=x.toUpperCase().replace(/\s+/g,'_');set(f.k,x)}}/>;c=(f.suffix||f.prefix)?<div className={'iw'+(f.prefix?' has-pre':'')}>{f.prefix&&<span className="pre">{f.prefix}</span>}{inp}{f.suffix&&<span className="suf">{f.suffix}</span>}</div>:inp}
return <Field label={f.label} req={f.req} hint={f.hint} err={err} span2={f.span2}>{c}</Field>
}
function RecordForm({title,sub,icon,t,sections,initial,onSave,onClose,preview,cta='Save',secondary,danger}){
const [v,setV]=React.useState(()=>Object.assign({},initial));const [errs,setErrs]=React.useState({});
const all=sections.flatMap(s=>s.fields);
const set=(k,x)=>{const f=all.find(y=>y.k===k);setV(o=>{const n=Object.assign({},o,{[k]:x});((f&&f.clears)||[]).forEach(c=>{n[c]=''});return n});setErrs(e=>Object.assign({},e,{[k]:null}))};
const submit=mode=>{const e={};all.forEach(f=>{if(f.when&&!f.when(v))return;if(f.disabled)return;const x=v[f.k];if(f.req&&(x===undefined||x===null||String(x).trim()===''))e[f.k]='Required';else if(f.validate){const m=f.validate(x,v);if(m)e[f.k]=m}});setErrs(e);if(Object.keys(e).length)return;onSave(v,mode)};
return <Drawer title={title} sub={sub} icon={icon} t={t} onClose={onClose} footer={<>{danger}<span className="sp"></span><button className="btn" onClick={onClose}>Cancel</button>{secondary&&<button className="btn" onClick={()=>submit(secondary.mode)}>{secondary.label}</button>}<button className="btn pri" onClick={()=>submit('save')}><Icon name="check" size={17}/>{cta}</button></>}>
{sections.map((s,i)=><div className="fsec" key={i}>{s.title&&<h4>{s.title}</h4>}<div className="fgrid">{s.fields.filter(f=>!f.when||f.when(v)).map(f=><FormField key={f.k} f={f} v={v} set={set} err={errs[f.k]}/>)}</div></div>)}
{preview&&preview(v)}
</Drawer>
}
function Tip({children}){return <span className="tip" tabIndex={0}><Icon name="info" size={14}/><span className="tip-b">{children}</span></span>}
function Hero({title,sub,actions}){
const {route,go,t,group}=useApp();
return <section className="hero"><div className="hero-row"><div className="hero-txt"><div className="crumbs eyebrow"><span onClick={()=>go('overview')}>Master</span>{group&&<><span className="sl">/</span><span onClick={()=>go(group.items[0].id)}>{group.l}</span></>}</div><h1>{title}</h1>{sub&&<p>{sub}</p>}</div>{actions&&<div className="hero-act">{actions}</div>}</div>
{t.nav==='Top tabs'&&group&&<div className="hero-tabs"><Seg value={route.p} options={group.items.map(i=>({v:i.id,l:i.l}))} onChange={go}/></div>}</section>
}
function Pager({page,pages,onPage}){const nums=[];for(let i=1;i<=pages;i++){if(i===1||i===pages||Math.abs(i-page)<=1)nums.push(i);else if(nums[nums.length-1]!=='…')nums.push('…')}return <div className="pager"><button disabled={page<=1} onClick={()=>onPage(page-1)} aria-label="Previous page"><Icon name="chevron-left" size={16}/></button>{nums.map((n,i)=>n==='…'?<span key={i} className="gap">…</span>:<button key={i} className={n===page?'on':''} onClick={()=>onPage(n)}>{n}</button>)}<button disabled={page>=pages} onClick={()=>onPage(page+1)} aria-label="Next page"><Icon name="chevron-right" size={16}/></button></div>}
function SortTh({k,sort,setSort,children,className}){const on=sort.k===k;return <th className={className}><button className={'thb'+(on?' on':'')} onClick={()=>setSort(on?{k,d:-sort.d}:{k,d:1})}>{children}<Icon name={on?(sort.d>0?'chevron-up':'chevron-down'):'chevrons-up-down'} size={13}/></button></th>}
function sortRows(rows,sort,get){if(!sort.k)return rows;const g=get||((r,k)=>r[k]);return rows.slice().sort((a,b)=>{const x=g(a,sort.k),y=g(b,sort.k);return (x>y?1:x<y?-1:0)*sort.d})}
function usePaged(rows,ps,deps){const [page,setPage]=React.useState(1);React.useEffect(()=>setPage(1),deps);const pages=Math.max(1,Math.ceil(rows.length/ps));const pg=Math.min(page,pages);return {page:pg,pages,setPage,slice:rows.slice((pg-1)*ps,pg*ps)}}
function TableFoot({shown,of,P,ps,noun='records'}){const from=shown?(P.page-1)*ps+1:0,to=Math.min(P.page*ps,shown);return <div className="tfoot"><div className="tf-l"><span>Showing <b>{from}–{to}</b> of <b>{shown}</b> {noun}{of!=null&&of!==shown?' · filtered from '+of:''}</span></div>{P.pages>1&&<Pager page={P.page} pages={P.pages} onPage={P.setPage}/>}</div>}
function TSearch({value,onChange,placeholder}){return <label className="tsearch"><Icon name="search" size={17}/><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder||'Search…'}/>{value&&<button className="ib xs" onClick={()=>onChange('')} aria-label="Clear search"><Icon name="x" size={14}/></button>}</label>}
function Empty({icon='search',title,body,action}){return <div className="empty"><span className="tile lg" style={tone('slate')}><Icon name={icon} size={24}/></span><b>{title}</b>{body&&<p>{body}</p>}{action}</div>}
function DayBar({start,end,core,t}){const seg=(a,b)=>b>a?[[a,b]]:[[a,1440],[0,b]];const main=seg(start,end);return <div className="tl" style={t?tone(t):undefined}>{[360,720,1080].map(g=><span key={g} className="g" style={{left:g/14.4+'%'}}></span>)}{main.map((s,i)=><span key={i} className={'s'+(core?' soft':'')} style={{left:s[0]/14.4+'%',width:(s[1]-s[0])/14.4+'%'}}></span>)}{core&&seg(core[0],core[1]).map((s,i)=><span key={'c'+i} className="s" style={{left:s[0]/14.4+'%',width:(s[1]-s[0])/14.4+'%'}}></span>)}</div>}
function DayAxis(){return <div className="axis">{['00','06','12','18','24'].map((h,i)=><span key={h} style={{left:i*25+'%'}}>{h}</span>)}</div>}
function YesNo({v,yes='Yes',no='—'}){return v?<span className="yes"><Icon name="check" size={15} stroke={2.6}/>{yes}</span>:<span className="no">{no}</span>}
function useMaps(){const {db}=useApp();return React.useMemo(()=>{const m=(a,k)=>Object.fromEntries(a.map(x=>[x[k||'id'],x]));return {dept:orNone(m(db.depts),NONE.dept),desig:orNone(m(db.desigs),NONE.desig),branch:orNone(m(db.branches),NONE.branch),co:m(db.companies),grade:m(db.grades),shift:m(db.shifts),byName:m(db.employees,'name'),emp:m(db.employees)}},[db])}
function deptOptions(db){return db.depts.filter(x=>!x.parent).flatMap(p=>[{v:p.id,l:p.name}].concat(db.depts.filter(c=>c.parent===p.id).map(c=>({v:c.id,l:c.name,sub:'in '+p.name}))))}
function copyText(s){try{navigator.clipboard&&navigator.clipboard.writeText(s)}catch(e){}}
const BRANCH_KIND_TONE={'Head office':'brand','Branch':'cyan','Store':'violet','Other':'slate','Regional office':'blue','Office':'cyan','Sales office':'teal','Plant':'orange','Warehouse':'amber'};
const METHODS={formula:{l:'Formula',d:c=>c.note||'Set on each salary structure'},statutory:{l:'Statutory',d:c=>c.note||'From Statutory Settings'},pct_ctc:{l:'% of CTC',d:c=>c.val+'% of CTC'},pct_basic:{l:'% of Basic',d:c=>c.val+'% of Basic'},pct_gross:{l:'% of Gross',d:c=>c.val+'% of Gross'},fixed:{l:'Fixed amount',d:c=>c.note||(c.val!=null?fmtINR(c.val)+' / month':'Set on each salary structure')},balance:{l:'Formula',d:()=>'CTC − other earnings'},slab:{l:'State slab',d:()=>'Per state PT slab'},variable:{l:'Variable',d:()=>'Per employee schedule'},capped:{l:'Against bills',d:c=>'Up to '+fmtINR(c.val)+' / month'}};
const CAT_META={Earning:{l:'Earnings',one:'Earning',t:'green',icon:'trending-up',d:'Paid to the employee'},Deduction:{l:'Deductions',one:'Deduction',t:'red',icon:'minus-circle',d:'Withheld from gross pay'},Employer:{l:'Employer contributions',one:'Employer contribution',t:'blue',icon:'building-2',d:'Part of CTC, not take-home'},Reimbursement:{l:'Reimbursements',one:'Reimbursement',t:'violet',icon:'receipt',d:'Paid against bills'}};
function deriveDB(db){
const live=db.employees.filter(e=>e.status!=='Exited');
const cnt=k=>live.reduce((m,e)=>{m[e[k]]=(m[e[k]]||0)+1;return m},{});
const dept=cnt('dept'),deptTotal={};
db.depts.forEach(x=>{deptTotal[x.id]=(dept[x.id]||0)+db.depts.filter(c=>c.parent===x.id).reduce((s,c)=>s+(dept[c.id]||0),0)});
return {live,headcount:live.length,dept,deptTotal,branch:cnt('branch'),desig:cnt('desig'),grade:cnt('grade'),co:cnt('co'),type:cnt('type'),shift:cnt('shift')};
}
const NAV=[
{id:'workforce',l:'Workforce Directory',icon:'users',t:'blue',desc:'Employees, contractor agencies and the rules that classify every worker.',items:[{id:'employees',l:'Employee Master',icon:'id-card'},{id:'contractors',l:'Contractor Master',icon:'hard-hat'},{id:'classes',l:'Classification Rules',icon:'tags'}]},
{id:'org',l:'Organization Setup',icon:'building-2',t:'teal',desc:'Legal entities, locations and the reporting structure people sit in.',items:[{id:'companies',l:'Companies',icon:'landmark'},{id:'branches',l:'Branches',icon:'map-pin'},{id:'departments',l:'Departments',icon:'network'},{id:'designations',l:'Designations',icon:'award'},{id:'grades',l:'Grades & Bands',icon:'layers'}]},
{id:'rules',l:'Rules & Policies',icon:'scroll-text',t:'amber',desc:'Shift timings, leave entitlements and the documents employees acknowledge.',items:[{id:'shifts',l:'Shift Rules',icon:'clock'},{id:'leaves',l:'Leave Rules',icon:'calendar-check'},{id:'policies',l:'Policy Documents',icon:'file-text'}]},
{id:'payroll',l:'Payroll Configuration',icon:'wallet',t:'violet',desc:'The earnings, deductions and statutory contributions payroll computes.',items:[{id:'components',l:'Salary Components',icon:'rupee'},{id:'statutory',l:'Statutory Settings',icon:'shield-check'}]}];
function TopTabs(){const {route,go,group,nav}=useApp();return <div className="stabs" role="navigation" aria-label="Master sections"><button className={'stab'+(route.p==='overview'?' on':'')} onClick={()=>go('overview')}><Icon name="layout-grid" size={16}/>Overview</button>{nav.map(g=><button key={g.id} className={'stab'+(group&&group.id===g.id?' on':'')} onClick={()=>go(g.items[0].id)}><Icon name={g.icon} size={16}/>{g.l}</button>)}</div>}
const TYPE_TONE={'Full-time':'green','Part-time':'blue','Intern':'violet'};
function EmployeesPage(){
const {db,d,update,toast,route,t,act:ax}=useApp();const M=useMaps();
const [q,setQ]=React.useState(route.q||'');const [status,setStatus]=React.useState(route.status||'');
const [dept,setDept]=React.useState(route.dept||'');const [branch,setBranch]=React.useState(route.branch||'');const [type,setType]=React.useState('');
const [sort,setSort]=React.useState({k:'code',d:1});const [sel,setSel]=React.useState([]);
const [view,setView]=React.useState(null);const [form,setForm]=React.useState(null);const [exit,setExit]=React.useState(null);
const ftNotice=(db.classes.find(c=>c.code==='FULL_TIME')||db.classes[0]||{}).notice||60;
const [lwd,setLwd]=React.useState(new Date(TODAY.getTime()+ftNotice*864e5).toISOString().slice(0,10));
const E=db.employees;const n=s=>E.filter(e=>e.status===s).length;const hc=d.headcount;
const ids=dept?[dept].concat(db.depts.filter(x=>x.parent===dept).map(x=>x.id)):null;const ql=q.trim().toLowerCase();
const rows=sortRows(E.filter(e=>(!status||e.status===status)&&(!ids||(dept==='__none'?!e.dept:ids.includes(e.dept)))&&(!ax.milestone.value||!!(ax.milestone.ids&&ax.milestone.ids.has(e.id)))&&(!branch||e.branch===branch)&&(!type||e.type===type)&&(!ql||(e.name+' '+e.code+' '+(e.email||'')+' '+M.desig[e.desig].name).toLowerCase().includes(ql))),sort,(e,k)=>k==='name'?e.name:k==='joined'?e.joined:k==='dept'?M.dept[e.dept].name:e.code);
const ps=+t.pageSize||10;const P=usePaged(rows,ps,[status,dept,branch,type,ql,sort.k,sort.d,ax.milestone.value,ax.milestone.ids]);
const any=!!(status||dept||branch||type||ql||ax.milestone.value);const clear=()=>{setStatus('');setDept('');setBranch('');setType('');setQ('');ax.milestone.set('')};
const spark=[...Array(12)].map((_,i)=>{const iso=new Date(TODAY.getFullYear(),TODAY.getMonth()-10+i,0).toISOString().slice(0,10);return E.filter(e=>e.joined<=iso&&!(e.exitOn&&e.exitOn<=iso)).length});
spark[11]=hc;
const YR=TODAY_ISO.slice(0,4);const joinedYr=E.filter(e=>e.joined>=YR+'-01-01').length;const exitedYr=E.filter(e=>e.status==='Exited'&&(e.exitOn||'')>=YR+'-01-01').length;
const pageIds=P.slice.map(e=>e.id);const allOn=pageIds.length>0&&pageIds.every(id=>sel.includes(id));const someOn=pageIds.some(id=>sel.includes(id));
const togglePage=()=>setSel(s=>allOn?s.filter(id=>!pageIds.includes(id)):Array.from(new Set(s.concat(pageIds))));
const toggle=id=>setSel(s=>s.includes(id)?s.filter(x=>x!==id):s.concat([id]));
const setStat=s=>setStatus(x=>x===s?'':s);
const viewing=view&&E.find(e=>e.id===view);
const doExit=()=>{const st=lwd<=TODAY_ISO?'Exited':'On notice';update('employees',L=>L.map(e=>e.id===exit.id?Object.assign({},e,st==='Exited'?{status:st,exitOn:lwd}:{status:st,lwd}):e));toast(st==='Exited'?exit.name+' marked as exited':exit.name+' is now serving notice · last day '+fmtDate(lwd));setExit(null)};
const act=n('Active');
return <>
<Hero title="Employee Master" sub={pl(hc,'person','people')+' across '+pl(db.companies.length,'company','companies')+' and '+pl(db.branches.length,'branch','branches')+'. Click anyone to see their full record.'} actions={<><button className="btn" onClick={()=>ax.importEmployees()}><Icon name="upload" size={17}/>Import</button><button className="btn" onClick={()=>ax.exportEmployees(rows)}><Icon name="download" size={17}/>Export</button><button className="btn pri" onClick={()=>setForm({})}><Icon name="user-plus" size={17}/>Add employee</button></>}/>
<div className="stats">
<Stat icon="users" t="blue" label="Headcount" value={hc} sub={'+'+joinedYr+' joined in '+YR} on={false} onClick={()=>setStatus('')} z={<Spark data={spark}/>}/>
<Stat icon="user-check" t="green" label="Active" value={act} sub={pct(act,hc)+'% of headcount'} on={status==='Active'} onClick={()=>setStat('Active')} z={<Meter v={pct(act,hc)}/>}/>
<Stat icon="hourglass" t="amber" label="On probation" value={n('Probation')} sub="Not yet confirmed" on={status==='Probation'} onClick={()=>setStat('Probation')} z={<Meter v={pct(n('Probation'),hc)}/>}/>
<Stat icon="clock" t="orange" label="Serving notice" value={n('On notice')} sub={exitedYr+' exited this year'} on={status==='On notice'} onClick={()=>setStat('On notice')} z={<Meter v={pct(n('On notice'),hc)}/>}/>
</div>
<div className="card tcard">
<div className="tbar"><TSearch value={q} onChange={setQ} placeholder="Search name, code, email or role…"/>
<Dropdown label="Department" all="All departments" value={dept} options={deptOptions(db).concat([{v:'__none',l:'No department',sub:'People without one'}])} onChange={setDept} search icon="layers"/>
<Dropdown label="Branch" all="All branches" value={branch} options={db.branches.map(b=>({v:b.id,l:b.name,sub:b.city}))} onChange={setBranch} icon="map-pin"/>
<Dropdown label="Type" all="All types" value={type} options={Array.from(new Set(['Full-time','Part-time','Intern'].concat(E.map(e=>e.type)))).map(x=>({v:x,l:x,t:TYPE_TONE[x]}))} onChange={setType}/>
<Dropdown label="Status" all="All statuses" value={status} options={Array.from(new Set(['Active','Probation','On notice','Exited'].concat(E.map(e=>e.status)))).map(x=>({v:x,l:x,t:STATUS_TONE[x],sub:String(n(x))}))} onChange={setStatus}/>
<Dropdown label="Milestone" all="All people" value={ax.milestone.value} options={ax.milestone.options} onChange={ax.milestone.set} icon="calendar"/>
{any&&<button className="btn sm ghost" onClick={clear}><Icon name="x" size={15}/>Clear</button>}<span className="sp"></span><span className="meta">{rows.length} of {E.length}</span></div>
{rows.length?<div className="twrap"><table className="t"><thead><tr><th className="cbx"><input type="checkbox" className={'cb'+(someOn&&!allOn?' mixed':'')} checked={allOn} onChange={togglePage} aria-label="Select this page"/></th><SortTh k="name" sort={sort} setSort={setSort}>Employee</SortTh><SortTh k="code" sort={sort} setSort={setSort}>Code</SortTh><SortTh k="dept" sort={sort} setSort={setSort}>Department</SortTh><th>Type</th><th>Status</th><SortTh k="joined" sort={sort} setSort={setSort}>Joined</SortTh><th className="act"></th></tr></thead>
<tbody>{P.slice.map(e=>{const dp=M.dept[e.dept],br=M.branch[e.branch],on=sel.includes(e.id),leaving=e.status==='Exited'||e.status==='On notice';return <tr key={e.id} className={'click'+(on?' sel':'')+(e.status==='Exited'?' off':'')} onClick={()=>setView(e.id)}>
<td className="cbx" onClick={ev=>ev.stopPropagation()}><input type="checkbox" className="cb" checked={on} onChange={()=>toggle(e.id)} aria-label={'Select '+e.name}/></td>
<td><Who name={e.name} sub={M.desig[e.desig].name}/></td>
<td><span className="code">{e.code}</span></td>
<td><div className="ic-row"><span className="tile xs" style={tone(dp.t)}><Icon name={dp.icon} size={14}/></span><div><span className="t1">{dp.name}</span><span className="t2">{br.name}</span></div></div></td>
<td><span className="ttag" style={tone(TYPE_TONE[e.type])}><i></i>{e.type}</span></td>
<td><Pill s={e.status}>{e.statusLabel}</Pill>{e.status==='On notice'&&<span className="t2">Last day {fmtDate(e.lwd)}</span>}</td>
<td><span className="t1 num">{fmtDate(e.joined)}</span><span className="t2">{tenure(e.joined)}</span></td>
<td className="act"><Menu items={[{icon:'eye',label:'View profile',on:()=>setView(e.id)},{icon:'pencil',label:'Edit details',on:()=>setForm({emp:e})},!leaving&&'-',!leaving&&{icon:'log-out',label:'Start exit',danger:true,on:()=>setExit(e)}]}/></td></tr>})}</tbody></table></div>
:<Empty title="No one matches these filters" body="Try another name, or clear the filters to see everyone." action={<button className="btn sm" onClick={clear}>Clear filters</button>}/>}
<TableFoot shown={rows.length} of={E.length} P={P} ps={ps} noun="employees"/>
</div>
{sel.length>0&&<div className="bulk"><b>{sel.length} selected</b>
<Menu btn="btn" align="left" items={['Active','Probation'].map(s=>({icon:'check-circle',label:'Mark as '+s.toLowerCase(),on:()=>{const ch=E.filter(e=>sel.includes(e.id)&&e.status!=='Exited'&&e.status!==s&&!(s==='Probation'&&e.status==='On notice'));if(!ch.length){toast('No one selected can be marked '+s.toLowerCase(),'info');return}const ids=ch.map(e=>e.id);update('employees',L=>L.map(e=>ids.includes(e.id)?Object.assign({},e,{status:s}):e));toast(pl(ch.length,'employee','employees')+' marked '+s.toLowerCase());setSel([])}}))}><Icon name="refresh-cw" size={16}/>Change status</Menu>
<button className="btn" onClick={()=>{ax.exportEmployees(E.filter(e=>sel.includes(e.id)));setSel([])}}><Icon name="download" size={16}/>Export</button><span className="vr"></span><button className="btn" onClick={()=>setSel([])} aria-label="Clear selection"><Icon name="x" size={16}/></button></div>}
{viewing&&!form&&!exit&&<EmpProfile e={viewing} onClose={()=>setView(null)} onEdit={()=>setForm({emp:viewing})} onExit={()=>setExit(viewing)}/>}
{form&&<EmpForm emp={form.emp} onClose={()=>setForm(null)}/>}
{exit&&<Modal title={'Start exit for '+exit.name+'?'} body="They move to notice period until their last working day, then to Exited. Full and final settlement runs on that date." icon="log-out" t="orange" cta="Start exit" onClose={()=>setExit(null)} onOk={doExit}><Field label="Last working day" hint={'Notice period for '+exit.type.toLowerCase()+' employees is '+((db.classes.find(c=>c.type===exit.type)||{}).notice||ftNotice)+' days'}><input className="input" type="date" value={lwd} onChange={e=>setLwd(e.target.value)}/></Field></Modal>}
</>
}
function EmpProfile({e,onClose,onEdit,onExit}){
const {db,go,act}=useApp();const M=useMaps();
const dp=M.dept[e.dept],ds=M.desig[e.desig],br=M.branch[e.branch],co=M.co[e.co],gr=M.grade[e.grade],sh=M.shift[e.shift]||null;
const parent=dp.parent?M.dept[dp.parent]:null;const mgr=e.mgrId?M.emp[e.mgrId]:null;
const cls=db.classes.find(c=>c.type===e.type)||db.classes[0]||NONE.cls;
const leaves=db.leaves.filter(l=>l.status==='Active'&&(!l.applies||l.applies.includes(cls.id))).length;
const I=(l,v)=><div><small>{l}</small><b title={v}>{v}</b></div>;const leaving=e.status==='Exited'||e.status==='On notice';
return <Drawer title="Employee profile" sub={e.code+' · on record since '+fmtDate(e.joined)} icon="id-card" onClose={onClose} footer={<>{!leaving&&<button className="btn ghost" onClick={onExit}><Icon name="log-out" size={16}/>Start exit</button>}<span className="sp"></span><button className="btn" onClick={()=>act.openRecord(e.id)}><Icon name="id-card" size={16}/>Full record</button><button className="btn" onClick={onClose}>Close</button><button className="btn pri" onClick={onEdit}><Icon name="pencil" size={16}/>Edit details</button></>}>
<div className="prof"><Av name={e.name} size="lg"/><div><h3>{e.name}</h3><div className="muted" style={{marginTop:3}}>{ds.name} · {dp.name}</div><div className="prof-meta"><Pill s={e.status}>{e.statusLabel}</Pill><span className="ttag" style={tone(TYPE_TONE[e.type])}><i></i>{e.type}</span>{gr&&<span className="chip">{gr.id} · {gr.name}</span>}</div></div></div>
{e.status==='On notice'&&<div className="note" style={tone('orange')}><Icon name="clock" size={16}/><span>Serving notice — last working day is <b>{fmtDate(e.lwd)}</b>, {daysTo(e.lwd)} days from now.</span></div>}
{e.status==='Probation'&&<div className="note" style={tone('amber')}><Icon name="hourglass" size={16}/><span>{e.probEnd?<>On probation until <b>{fmtDate(e.probEnd)}</b>.</>:'On probation — no confirmation date is set yet.'}</span></div>}
{e.status==='Exited'&&<div className="note" style={tone('slate')}><Icon name="archive" size={16}/><span>Exited on <b>{fmtDate(e.exitOn)}</b>. The record is kept for statutory and payroll history.</span></div>}
<div className="fsec"><h4>Employment</h4><div className="igrid">{I('Department',dp.name+(parent?' · '+parent.name:''))}{I('Designation',ds.name)}{I('Company',co.name)}{I('Branch',br.name+(br.city?', '+br.city:''))}{I('Shift',sh?sh.name+' · '+(sh.kind==='Flexible'&&sh.core?'core '+hm(sh.core[0])+'–'+hm(sh.core[1]):hm(sh.start)+'–'+hm(sh.end)):'—')}{I('Tenure',tenure(e.joined))}</div></div>
<div className="fsec"><h4>Contact</h4><div className="igrid">{I('Work email',e.email||'—')}{I('Mobile',e.phone||'—')}</div></div>
<div className="fsec"><h4>Reports to</h4>{mgr?<Who name={mgr.name} sub={M.desig[mgr.desig].name+' · '+M.dept[mgr.dept].name}/>:dp.id&&!dp.headId?<span className="warn-link" onClick={()=>go('departments')}><Icon name="alert-triangle" size={14}/>{dp.name} has no head yet — assign one</span>:<span className="muted">No reporting manager set</span>}</div>
<div className="fsec"><h4>Rules that apply</h4><div className="chips"><span className="chip"><Icon name="hourglass" size={14}/>Probation · {cls.probation}</span><span className="chip"><Icon name="clock" size={14}/>Notice · {cls.notice?cls.notice+' days':'—'}</span>{cls.pf===true&&<span className="chip"><Icon name="landmark" size={14}/>PF & ESI eligible</span>}<span className="chip"><Icon name="calendar-check" size={14}/>{leaves} leave types</span></div></div>
</Drawer>
}
function EmpForm({emp,onClose}){
const {db,update,toast,act}=useApp();const M=useMaps();const isEdit=!!emp;
const next=act.nextCode||'Assigned when saved';
const initial=isEdit?Object.assign({},emp):{co:act.defaultCo,branch:(db.branches.find(b=>b.co===act.defaultCo&&b.kind==='Head office')||{}).id||'',type:'Full-time',joined:TODAY_ISO,shift:''};
const sections=[
{title:'Personal',fields:[{k:'first',label:'First name',req:true,placeholder:'e.g. Ananya'},{k:'last',label:'Last name',req:true,placeholder:'e.g. Sharma'},{k:'email',label:'Work email',req:true,type:'email',placeholder:'name@company.com',validate:x=>/^\S+@\S+\.\S+$/.test(x)?null:'Enter a valid email address'},{k:'phone',label:'Mobile',placeholder:'+91 98xxx xxxxx'}]},
{title:'Employment',fields:[
{k:'co',label:'Company',type:'select',req:true,disabled:isEdit,hint:isEdit?'Moving someone to another company isn’t supported yet':undefined,options:db.companies.map(c=>({v:c.id,l:c.name})),clears:['branch']},
{k:'branch',label:'Branch',type:'select',req:true,options:v=>db.branches.filter(b=>b.co===v.co).map(b=>({v:b.id,l:b.name,sub:b.city}))},
{k:'dept',label:'Department',type:'select',req:true,search:true,options:v=>deptOptions(db).filter(o=>(db.depts.find(x=>x.id===o.v)||{}).co===v.co),clears:['desig']},
{k:'desig',label:'Designation',type:'select',req:true,options:v=>db.desigs.filter(x=>x.co===v.co&&(x.dept===v.dept||!x.dept)).map(x=>({v:x.id,l:x.name,sub:x.grade||undefined})),placeholder:v=>v.dept?'Select…':'Pick a department first'},
{k:'type',label:'Employment type',type:'seg',span2:true,options:v=>act.typeOptions(v.type)},
{k:'joined',label:'Date of joining',type:'date',req:true},
{k:'shift',label:'Shift',type:'select',disabled:!act.canAssignShift,hint:act.canAssignShift?undefined:'You don’t have access to assign shifts',options:db.shifts.filter(s=>s.status==='Active').map(s=>({v:s.id,l:s.name,sub:s.kind==='Flexible'?'Flexible':hm(s.start)+'–'+hm(s.end)}))},{k:'agency',label:'Staffing agency',type:'select',span2:true,when:v=>v.type==='Contract'&&act.showAgency,disabled:!act.canAgency,hint:act.canAgency?'The agency that supplies this contract worker. Its worker count on Contractor Master is counted from these links.':'You don’t have access to change the agency',options:v=>[{v:'',l:'No agency'}].concat(db.agencies.filter(a=>a.co===v.co&&(a.status==='Active'||a.id===v.agency)).map(a=>({v:a.id,l:a.name,sub:a.service||undefined})))}]}];
const preview=v=>{const cls=db.classes.find(c=>c.type===v.type);const ds=v.desig&&M.desig[v.desig];const g=ds&&M.grade[ds.grade];return <div className="preview"><div className="preview-h">{isEdit?'Employee code':'Employee code will be'}<span>{isEdit?'Codes never change':'Auto-generated'}</span></div><div className="ic-row" style={{flexWrap:'wrap'}}><span className="code" style={{fontSize:14,padding:'5px 10px'}}>{isEdit?emp.code:next}</span>{g&&<span className="chip">{g.id} · {g.name}{g.min!=null?' · '+fmtL(g.min)+'–'+fmtL(g.max):''}</span>}</div>{cls&&<div className="note" style={tone('blue')}><Icon name="info" size={16}/><span><b>{cls.name} rules apply</b> — probation {cls.probation.toLowerCase()}, notice {cls.notice?cls.notice+' days':'—'}{cls.pf===true?', PF & ESI eligible':''}.</span></div>}</div>};
const onSave=v=>{const first=v.first.trim(),last=v.last.trim(),name=first+' '+last;const ds=M.desig[v.desig];const rec=Object.assign({},isEdit?emp:{},v,{name,first,last,grade:ds.grade,co:M.branch[v.branch].co,id:isEdit?emp.id:next,status:isEdit?emp.status:'Probation',agency:v.type==='Contract'?(v.agency||''):''});update('employees',L=>isEdit?L.map(x=>x.id===emp.id?rec:x):L.concat([rec]));toast(isEdit?'Saved changes to '+name:name+(act.nextCode?' added as '+next:' added'));onClose()};
return <RecordForm title={isEdit?'Edit '+emp.name:'Add employee'} sub={isEdit?emp.code+' · changes apply from the next payroll run':'They get an employee code, login invite and the rules for their classification.'} icon={isEdit?'pencil':'user-plus'} sections={sections} initial={initial} onSave={onSave} onClose={onClose} preview={preview} cta={isEdit?'Save changes':'Add employee'}/>
}
function ContractorsPage(){
const {db,update,toast,act}=useApp();const M=useMaps();const [form,setForm]=React.useState(null);const [end,setEnd]=React.useState(null);
const A=db.agencies;const total=A.filter(a=>a.status==='Active').reduce((s,a)=>s+a.workers,0);
const expiring=A.filter(a=>a.status==='Active'&&daysTo(a.licence)<=30);
const renew=a=>{if(!a.licence){setForm(a);return}const dt=asDate(a.licence);dt.setFullYear(dt.getFullYear()+1);update('agencies',L=>L.map(x=>x.id===a.id?Object.assign({},x,{licence:dt.toISOString().slice(0,10)}):x));toast(a.name+' licence renewed to '+fmtDate(dt.toISOString().slice(0,10)))};
const setStatus=(a,s)=>{update('agencies',L=>L.map(x=>x.id===a.id?Object.assign({},x,{status:s,workers:s==='Active'?x.workers:0}):x));toast(s==='Active'?a.name+' reactivated':'Contract with '+a.name+' ended');setEnd(null)};
return <>
<Hero title="Contractor Master" sub="Staffing agencies and the contract workers they deploy across your sites." actions={<><button className="btn" onClick={()=>act.exportAgencies(A)}><Icon name="download" size={17}/>Export</button><button className="btn pri" onClick={()=>setForm({})}><Icon name="plus" size={17}/>Add agency</button></>}/>
<div className="stats">
<Stat icon="briefcase" t="blue" label="Agencies" value={A.length} sub={A.filter(a=>a.status==='Active').length+' with active contracts'}/>
<Stat icon="hard-hat" t="orange" label="Contract workers" value={total} sub={'Deployed across '+pl(new Set(A.filter(a=>a.status==='Active').flatMap(a=>a.sites||[])).size,'site','sites')}/>
<Stat icon="alert-triangle" t={expiring.length?'red':'green'} label="Licences expiring" value={expiring.length} sub={expiring.length?expiring[0].name+' · '+(daysTo(expiring[0].licence)<0?'expired':daysTo(expiring[0].licence)+' days'):A.some(a=>a.status==='Active'&&a.licence)?'All valid for 30+ days':'No licence dates added yet'}/>
</div>
<div className="cgrid wide">{A.map(a=>{const dd=daysTo(a.licence),share=pct(a.workers,total),on=a.status==='Active';return <div key={a.id} className={'ccard'+(on?'':' off')}>
<div className="cc-h"><span className="tile lg" style={tone(a.t)}>{initials(a.name)}</span><div className="cc-t"><b className="lg">{a.name}</b><small>{a.service?a.service+' · ':''}Reg. {a.reg||'—'}</small></div><Pill s={a.status}/><Menu items={[{icon:'pencil',label:'Edit agency',on:()=>setForm(a)},on&&{icon:'refresh-cw',label:'Renew licence',on:()=>renew(a)},'-',on?{icon:'ban',label:'End contract',danger:true,on:()=>setEnd(a)}:{icon:'check-circle',label:'Reactivate',on:()=>setStatus(a,'Active')}]}/></div>
<div className="cc-n"><div><b className="num">{a.workers==null?'—':a.workers}</b><span>active workers</span></div><div className="cc-share"><div className="ack-top"><span className="muted">Share of contract workforce</span><b>{a.workers==null?'—':share+'%'}</b></div><Meter v={share} wide/></div></div>
<dl className="kv"><dt><Icon name="user" size={15}/>Contact</dt><dd>{a.contact||'—'}</dd><dt><Icon name="phone" size={15}/>Phone</dt><dd className="num">{a.phone||'—'}</dd><dt><Icon name="mail" size={15}/>Email</dt><dd>{a.email||'—'}</dd><dt><Icon name="map-pin" size={15}/>Deployed at</dt><dd>{a.sites&&a.sites.length?a.sites.map(s=>M.branch[s].name).join(', '):'—'}</dd></dl>
<div className="cc-f"><Icon name="shield-check" size={16}/><span>{a.licence?'Licence '+(a.licenceNo?a.licenceNo+' ':'')+(dd<0?'expired':'valid till')+' '+fmtDate(a.licence):'No licence date added'}</span><span className="sp"></span>{on&&dd<=30?<span className="warn-link" onClick={()=>renew(a)}><Icon name="alert-triangle" size={14}/>{dd<0?'Expired':'Expires in '+dd+' days'} · Renew</span>:<span className="muted">{a.since?'Since '+a.since:''}</span>}</div>
</div>})}
<button className="add-card" onClick={()=>setForm({})}><span className="tile lg"><Icon name="plus" size={22}/></span>Add a staffing agency</button></div>
{form&&<AgencyForm a={form.id?form:null} onClose={()=>setForm(null)}/>}
{end&&<Modal title={'End contract with '+end.name+'?'} body={'The agency is marked inactive. '+pl(end.workers||0,'contract worker stays','contract workers stay')+' linked on their employee records, and you can reactivate the agency later.'} icon="ban" t="red" danger cta="End contract" onClose={()=>setEnd(null)} onOk={()=>setStatus(end,'Inactive')}/>}
</>
}
function AgencyForm({a,onClose}){
const {db,update,toast}=useApp();const isEdit=!!a;
const sections=[{title:'Agency',fields:[{k:'name',label:'Agency name',req:true,span2:true,placeholder:'e.g. Apex Staffing Solutions'},{k:'reg',label:'Registration no.',req:true,upper:true,placeholder:'e.g. APX-9981'},{k:'service',label:'Service provided',placeholder:'e.g. Security & front desk'}]},
{title:'Point of contact',fields:[{k:'contact',label:'Contact person',req:true},{k:'phone',label:'Phone',placeholder:'+91 98xxx xxxxx'},{k:'email',label:'Email',span2:true,type:'email',validate:x=>!x||/^\S+@\S+\.\S+$/.test(x)?null:'Enter a valid email address'}]},
{title:'Deployment',fields:[{k:'sites',label:'Deployed at',type:'multi',span2:true,options:db.branches.filter(b=>b.status==='Active'&&b.co===((a&&a.co)||(db.companies[0]||{}).id)).map(b=>({v:b.id,l:b.city||b.name})),validate:x=>!db.branches.length||x&&x.length?null:'Pick at least one site'},{k:'workers',label:'Active workers',type:'number',min:0,suffix:'people',disabled:true,hint:'Counted from the contract workers linked to this agency on their employee record'},{k:'licenceNo',label:'Licence number',placeholder:'e.g. CLRA/KA/2021/0077'},{k:'licence',label:'Licence valid till',type:'date',hint:'CLRA licence — flagged on this page and the overview 30 days before it expires'}]}];
const onSave=v=>{const rec=Object.assign({t:'teal',status:'Active',since:''},a||{},v,{id:isEdit?a.id:'A'+Date.now(),workers:isEdit?a.workers:0});update('agencies',L=>isEdit?L.map(x=>x.id===a.id?rec:x):L.concat([rec]));toast(isEdit?'Saved '+rec.name:rec.name+' added');onClose()};
return <RecordForm title={isEdit?'Edit '+a.name:'Add staffing agency'} sub="Contract workers from this agency are tracked for attendance and CLRA compliance." icon="hard-hat" t="orange" sections={sections} initial={a||{sites:[],workers:0}} onSave={onSave} onClose={onClose} cta={isEdit?'Save changes':'Add agency'}/>
}
function ClassesPage(){
const {db,d,update,toast}=useApp();const [form,setForm]=React.useState(null);
const hc=c=>c.id==='CT'?db.agencies.filter(a=>a.status==='Active').reduce((s,a)=>s+a.workers,0):d.live.filter(e=>e.type===c.type).length;
const rows=db.classes.map(c=>Object.assign({},c,{hc:hc(c)}));const total=rows.reduce((s,r)=>s+r.hc,0);
const Ben=({l,v})=>v===true?<span className="pill" style={tone('green')}><Icon name="check" size={13} stroke={2.6}/>{l}</span>:v==='agency'?<span className="pill" style={tone('amber')}>{l} · via agency</span>:<span className="chip" style={{textDecoration:'line-through',opacity:.65}}>{l}</span>;
return <>
<Hero title="Classification Rules" sub="How each kind of worker is treated — probation, notice period and statutory eligibility follow from their classification." actions={<button className="btn pri" onClick={()=>setForm({})}><Icon name="plus" size={17}/>Add classification</button>}/>
<div className="card mixcard"><div className="mix-h"><div><h3>Workforce mix</h3><p>Everyone who works for you today, by classification</p></div><div className="mix-total"><b className="num">{total}</b><small>people</small></div></div>
<div className="mix">{rows.filter(r=>r.hc>0).map(r=><span key={r.id} style={Object.assign({flex:r.hc},tone(r.t))} title={r.name+': '+r.hc}></span>)}</div>
<div className="legend">{rows.map(r=><div className="lg" key={r.id} style={tone(r.t)}><i></i>{r.name}<b className="num">{r.hc}</b><span className="muted">{pct(r.hc,total)}%</span></div>)}</div></div>
<div className="card tcard"><div className="twrap"><table className="t"><thead><tr><th>Classification</th><th className="r">Headcount</th><th>Probation</th><th>Notice period</th><th>Statutory benefits<Tip>PF and ESI rates come from Statutory Settings. Gratuity becomes payable after 5 years of continuous service.</Tip></th><th>Leave policy</th><th>Status</th><th className="act"></th></tr></thead>
<tbody>{rows.map(c=><tr key={c.id} className={'click'+(c.status!=='Active'?' off':'')} onClick={()=>setForm(c)}>
<td><div className="ic-row"><span className="tile" style={tone(c.t)}><Icon name={c.icon} size={18}/></span><div><span className="t1">{c.name}<span className="code" style={{marginLeft:8}}>{c.code}</span></span><span className="t2">{c.desc||c.sub}</span></div></div></td>
<td className="r"><b className="num" style={{fontSize:15,color:'var(--text-primary)'}}>{c.hc}</b></td><td>{c.probation}</td><td>{c.notice?c.notice+' days':'—'}</td>
<td>{c.pf==null?<span className="no">Set per employee</span>:<div className="chips"><Ben l="PF" v={c.pf}/><Ben l="ESI" v={c.esi}/><Ben l="Gratuity" v={c.gratuity}/></div>}</td><td>{c.leave}</td><td><Pill s={c.status}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit rules',soon:c.system,soonLabel:'Built-in',tip:'Built-in types can’t be changed',on:()=>setForm(c)},'-',c.status==='Active'?{icon:'ban',label:'Deactivate',danger:true,soon:c.system,soonLabel:'Built-in',tip:'Built-in types can’t be changed',on:()=>{update('classes',L=>L.map(x=>x.id===c.id?Object.assign({},x,{status:'Inactive'}):x));toast(c.name+' deactivated')}}:{icon:'check-circle',label:'Activate',on:()=>{update('classes',L=>L.map(x=>x.id===c.id?Object.assign({},x,{status:'Active'}):x));toast(c.name+' activated')}}]}/></td></tr>)}</tbody></table></div></div>
{form&&<ClassForm c={form.id?form:null} onClose={()=>setForm(null)}/>}
</>
}
function ClassForm({c,onClose}){
const {update,toast}=useApp();const isEdit=!!c;const sys=!!(c&&c.system);
const sections=[{title:'Classification',fields:[{k:'name',label:'Name',req:true,disabled:sys,hint:sys?'Built-in types can’t be changed':undefined,placeholder:'e.g. Apprentice'},{k:'code',label:'Code',req:true,upper:true,disabled:isEdit,placeholder:'e.g. PART_TIME',hint:isEdit?'Codes are locked — employee records link to them':'Employees link to it by code: FULL_TIME, PART_TIME, INTERN, CONTRACT or CONSULTANT',validate:x=>String(x).length<=30?null:'Use up to 30 characters'},{k:'desc',label:'Description',span2:true,disabled:true,hint:'Coming soon — not saved yet',placeholder:'Who falls into this classification?'}]},
{title:'Rules',fields:[{k:'probation',label:'Probation',type:'select',disabled:true,hint:'Company-wide — set in HR configuration',options:v=>Array.from(new Set(['None','1 month','3 months','6 months',v.probation].filter(Boolean)))},{k:'notice',label:'Notice period',type:'number',min:0,suffix:'days',disabled:true,hint:'Company-wide — set in HR configuration'},{k:'leave',label:'Leave policy',type:'select',span2:true,disabled:true,hint:'Coming soon — every leave type applies to everyone today',options:v=>Array.from(new Set(['Standard','Pro-rated','Casual only','Per agency','None',v.leave].filter(Boolean)))}]},
{title:'Statutory benefits',fields:[{k:'pf',label:'Provident Fund',half:true,type:'toggle',disabled:true,hint:'Set per employee on their salary structure'},{k:'esi',label:'ESI',half:true,type:'toggle',disabled:true,hint:'Set per employee on their salary structure'},{k:'gratuity',label:'Gratuity',type:'toggle',disabled:true,hint:'Coming soon'},{k:'active',label:'Active',type:'toggle',disabled:sys,hint:'Inactive classifications can’t be assigned to new people'}]}];
const onSave=v=>{const rec=Object.assign({t:'teal',icon:'tags',type:v.name},c||{},v,{status:v.active?'Active':'Inactive'});delete rec.active;delete rec.hc;update('classes',L=>isEdit?L.map(x=>x.id===c.id?rec:x):L.concat([rec]));toast(isEdit?'Saved '+rec.name+' rules':rec.name+' classification added');onClose()};
return <RecordForm title={isEdit?c.name+' rules':'Add classification'} sub="These rules apply automatically to everyone with this classification." icon="tags" t="blue" sections={sections} initial={Object.assign({probation:'None',notice:30,leave:'Standard',pf:false,esi:false,gratuity:false},c||{},{active:c?c.status==='Active':true})} onSave={onSave} onClose={onClose} cta={isEdit?'Save rules':'Add classification'}/>
}
const DEPT_ICONS=['code','laptop','database','shield-check','lightbulb','trending-up','phone','factory','truck','users','calculator','headphones','target','briefcase','palette','graduation-cap'];
const IN_STATES=['Andaman and Nicobar Islands','Andhra Pradesh','Arunachal Pradesh','Assam','Bihar','Chandigarh','Chhattisgarh','Dadra and Nagar Haveli and Daman and Diu','Delhi','Goa','Gujarat','Haryana','Himachal Pradesh','Jammu and Kashmir','Jharkhand','Karnataka','Kerala','Ladakh','Lakshadweep','Madhya Pradesh','Maharashtra','Manipur','Meghalaya','Mizoram','Nagaland','Odisha','Puducherry','Punjab','Rajasthan','Sikkim','Tamil Nadu','Telangana','Tripura','Uttar Pradesh','Uttarakhand','West Bengal'];
function CompaniesPage(){
const {db,d,toast,go}=useApp();const [form,setForm]=React.useState(null);
return <>
<Hero title="Companies" sub="The legal entities you run payroll for. Each one carries its own statutory registrations." actions={<button className="btn pri" onClick={()=>setForm({})}><Icon name="plus" size={17}/>Add company</button>}/>
<div className="cgrid wide">{db.companies.map(c=>{const br=db.branches.filter(b=>b.co===c.id);const depts=new Set(d.live.filter(e=>e.co===c.id&&e.dept).map(e=>e.dept)).size;return <div key={c.id} className={'ccard'+(c.status==='Active'?'':' off')}>
<div className="cc-h"><span className="tile lg" style={tone(c.t)}>{initials(c.name)}</span><div className="cc-t"><b className="lg">{c.name}</b><small>{[c.legal,c.industry].filter(Boolean).join(' · ')||'Legal name not set'}</small>{c.desc&&<small>{c.desc}</small>}</div><Pill s={c.status}/><Menu items={[{icon:'pencil',label:'Edit company',on:()=>setForm(c)},{icon:'map-pin',label:'View branches',on:()=>go('branches',{co:c.id})}]}/></div>
<div className="co-stats"><div><small>Branches</small><b>{br.length}</b></div><div><small>Departments</small><b>{depts}</b></div><div><small>Employees</small><b>{d.co[c.id]||0}</b></div></div>
<div><div className="mth" style={{marginBottom:6}}>Statutory registrations</div><div className="ids">{Object.entries(c.ids).map(([k,v])=><div className="idr" key={k}><small>{k}</small><span className="mono">{v||'—'}</span>{v&&<button className="ib xs" aria-label={'Copy '+k} onClick={()=>{copyText(v);toast(k+' copied to clipboard')}}><Icon name="copy" size={14}/></button>}</div>)}</div></div>
<div className="cc-f"><Icon name="map-pin" size={16}/><span>{c.hq||'No head office branch yet'}{c.since?' · since '+c.since:''}</span><span className="sp"></span><a style={{cursor:'pointer'}} onClick={()=>go('branches',{co:c.id})}>View branches</a></div>
</div>})}
<button className="add-card" onClick={()=>setForm({})}><span className="tile lg"><Icon name="plus" size={22}/></span>Add a company</button></div>
{form&&<CompanyForm c={form.id?form:null} onClose={()=>setForm(null)}/>}
</>
}
function CompanyForm({c,onClose}){
const {update,toast}=useApp();const isEdit=!!c;
const sections=[{title:'Company',fields:[{k:'name',label:'Display name',req:true,placeholder:'e.g. Acme Retail'},{k:'legal',label:'Registered legal name',req:true,placeholder:'e.g. Acme Retail Pvt. Ltd.'},{k:'industry',label:'Industry',type:'select',options:v=>Array.from(new Set(['Software & IT services','Industrial manufacturing','Retail','Healthcare','Financial services','Logistics',v.industry].filter(Boolean)))},{k:'hq',label:'Registered office',placeholder:'City, State',disabled:true,hint:'Comes from the branch marked Head office'},{k:'inc',label:'Incorporated on',type:'date',max:TODAY_ISO,hint:'Shown as “since” on the company card',validate:x=>!x||x<=TODAY_ISO?null:'Can’t be in the future'},{k:'desc',label:'Description',type:'textarea',rows:3,span2:true,placeholder:'What this company does, in a line or two',validate:x=>!x||String(x).length<=2000?null:'Keep it under 2,000 characters'}]},
{title:'Statutory registrations',fields:[{k:'CIN',label:'CIN',upper:true,span2:true,placeholder:'21 characters',validate:x=>!x||String(x).length===21?null:'CIN has 21 characters'},{k:'PAN',label:'PAN',upper:true,placeholder:'AAAAA0000A',validate:x=>!x||/^[A-Z]{5}[0-9]{4}[A-Z]$/.test(x)?null:'Format: AAAAA0000A'},{k:'TAN',label:'TAN',upper:true,placeholder:'AAAA00000A',validate:x=>!x||/^[A-Z]{4}[0-9]{5}[A-Z]$/.test(x)?null:'Format: AAAA00000A'},{k:'GSTIN',label:'GSTIN',upper:true,span2:true,placeholder:'15 characters'}]}];
const init=c?Object.assign({},c,c.ids):{industry:'Software & IT services'};
const onSave=v=>{const rec=Object.assign({t:'teal',status:'Active',since:''},c||{},{id:isEdit?c.id:'co'+Date.now(),name:v.name,legal:v.legal,industry:v.industry,hq:v.hq||'—',inc:v.inc||'',desc:v.desc||'',since:v.inc?String(v.inc).slice(0,4):'',ids:{CIN:v.CIN||'',PAN:v.PAN||'',TAN:v.TAN||'',GSTIN:v.GSTIN||''}});update('companies',L=>isEdit?L.map(x=>x.id===c.id?rec:x):L.concat([rec]));toast(isEdit?'Saved '+rec.name:rec.name+' added');onClose()};
return <RecordForm title={isEdit?'Edit '+c.name:'Add company'} sub="Registrations print on payslips, Form 16 and statutory challans." icon="landmark" t="teal" sections={sections} initial={init} onSave={onSave} onClose={onClose} cta={isEdit?'Save changes':'Add company'}/>
}
function BranchesPage(){
const {db,d,update,toast,route,act}=useApp();const M=useMaps();const [co,setCo]=React.useState(route.co||'');const [q,setQ]=React.useState('');const [form,setForm]=React.useState(null);
const ql=q.trim().toLowerCase();const rows=db.branches.filter(b=>(!co||b.co===co)&&(!ql||(b.name+' '+(b.city||'')+' '+(b.state||'')+' '+(b.code||'')).toLowerCase().includes(ql)));
const max=Math.max(1,...db.branches.map(b=>d.branch[b.id]||0));
const setSt=(b,s)=>{update('branches',L=>L.map(x=>x.id===b.id?Object.assign({},x,{status:s}):x));toast(b.name+(s==='Active'?' activated':' deactivated'))};
return <>
<Hero title="Branches" sub="Offices, plants and warehouses — where people report to work, and which state’s labour rules apply." actions={<button className="btn pri" onClick={()=>setForm({co:co||act.defaultCo,kind:'Branch',status:'Active'})}><Icon name="plus" size={17}/>Add branch</button>}/>
<div className="card tcard"><div className="tbar"><TSearch value={q} onChange={setQ} placeholder="Search branch, city or state…"/><Dropdown label="Company" all="All companies" value={co} options={db.companies.map(c=>({v:c.id,l:c.name}))} onChange={setCo} icon="landmark"/><Dropdown label="Status" all="Active branches" value={route.archived?'all':''} options={[{v:'all',l:'Include inactive'}]} onChange={x=>act.showArchivedBranches(!!x,co)} icon="archive"/><span className="sp"></span><span className="meta">{rows.length} branches</span></div>
{rows.length?<div className="twrap"><table className="t"><thead><tr><th>Branch</th><th>Type</th><th>Company</th><th>People</th><th>Status</th><th className="act"></th></tr></thead><tbody>
{rows.map(b=>{const n=d.branch[b.id]||0,bt=BRANCH_KIND_TONE[b.kind]||'slate';return <tr key={b.id} className={'click'+(b.status==='Active'?'':' off')} onClick={()=>setForm(b)}>
<td><div className="ic-row"><span className="tile" style={tone(bt)}><Icon name={b.icon||'building'} size={18}/></span><div><span className="t1">{b.name}</span><span className="t2"><span className="mono" style={{fontSize:11.5}}>{b.code||'—'}</span> · {[b.city,b.state].filter(Boolean).join(', ')||'—'}</span></div></div></td>
<td><span className="chip">{b.kind}</span></td><td><span className="t1">{M.co[b.co]?M.co[b.co].name:'—'}</span></td>
<td><div className="ic-row"><b className="num" style={{minWidth:28,color:'var(--text-primary)'}}>{n}</b><div className="meter" style={tone(bt,{width:120})}><span style={{width:pct(n,max)+'%'}}></span></div></div></td>
<td><Pill s={b.status}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit branch',on:()=>setForm(b)},'-',b.status==='Active'?{icon:'ban',label:'Deactivate',danger:true,on:()=>{if(n){toast('Move the '+n+' people at '+b.name+' before deactivating it','info');return}setSt(b,'Inactive')}}:{icon:'check-circle',label:'Activate',on:()=>setSt(b,'Active')}]}/></td></tr>})}
</tbody></table></div>:<Empty title="No branches match" body="Try another search or company." action={<button className="btn sm" onClick={()=>{setQ('');setCo('')}}>Clear filters</button>}/>}</div>
{form&&<BranchForm b={form.name?form:null} init={form} onClose={()=>setForm(null)}/>}
</>
}
function BranchForm({b,init,onClose}){
const {db,update,toast}=useApp();const isEdit=!!b;
const icons={'Head office':'building-2','Branch':'building','Regional office':'building','Office':'building','Sales office':'building','Plant':'factory','Warehouse':'warehouse'};
const sections=[{title:'Branch',fields:[{k:'name',label:'Branch name',req:true,placeholder:'e.g. Kochi Office'},{k:'code',label:'Code',req:true,upper:true,placeholder:'e.g. KOC',validate:x=>db.branches.some(y=>y.code===x&&(!isEdit||y.id!==b.id))?'Code already in use':null},{k:'kind',label:'Type',type:'select',options:['Head office','Branch','Plant','Warehouse','Office','Store','Other'],hint:'Head office makes this the company’s head office in place of the current one'},{k:'co',label:'Company',type:'select',req:true,disabled:isEdit,options:db.companies.map(c=>({v:c.id,l:c.name}))}]},
{title:'Location',fields:[{k:'city',label:'City',req:true},{k:'state',label:'State',type:'select',req:true,search:true,options:v=>Array.from(new Set(IN_STATES.concat(v.state?[v.state]:[]))),hint:'Professional Tax uses one state for now, set in Statutory Settings'}]}];
const onSave=v=>{const rec=Object.assign({status:'Active'},b||{},v,{icon:icons[v.kind]||'building'});update('branches',L=>isEdit?L.map(x=>x.id===b.id?rec:x):L.concat([rec]));toast(isEdit?'Saved '+rec.name:rec.name+' added');onClose()};
return <RecordForm title={isEdit?'Edit '+b.name:'Add branch'} sub="People are assigned to a branch for attendance and holidays." icon="map-pin" t="teal" sections={sections} initial={init} onSave={onSave} onClose={onClose} cta={isEdit?'Save changes':'Add branch'}/>
}
function DepartmentsPage(){
const {db,d,update,toast}=useApp();const M=useMaps();
const tops=db.depts.filter(x=>!x.parent);const kids=id=>db.depts.filter(x=>x.parent===id);
const [open,setOpen]=React.useState(()=>Object.fromEntries(tops.map(x=>[x.id,true])));
const [q,setQ]=React.useState('');const [form,setForm]=React.useState(null);const [assign,setAssign]=React.useState(null);const [pick,setPick]=React.useState('');
const ql=q.trim().toLowerCase();const max=Math.max(1,...tops.map(x=>d.deptTotal[x.id]||0));
const noHead=db.depts.filter(x=>!x.head&&x.status==='Active');const withKids=tops.filter(x=>kids(x.id).length);const allOpen=withKids.every(x=>open[x.id]);
const openAssign=x=>{setAssign(x);setPick(x.headId||'')};
const row=(x,o)=>{const k=kids(x.id),kid=!!o.kid,n=kid||o.flat?(x.parent?d.dept[x.id]||0:d.deptTotal[x.id]||0):(d.deptTotal[x.id]||0),head=x.headId&&M.emp[x.headId];return <tr key={x.id} className={'click'+(x.status==='Active'?'':' off')} onClick={()=>setForm(x)}>
<td className={kid?'kid'+(o.last?'':' mid'):''}><div className="tree">{!kid&&!o.flat&&<button className={'tw'+(k.length?'':' none')+(open[x.id]?' open':'')} onClick={e=>{e.stopPropagation();setOpen(s=>Object.assign({},s,{[x.id]:!s[x.id]}))}} aria-label="Show sub-teams"><Icon name="chevron-right" size={16}/></button>}<span className={'tile'+(kid?' sm':'')} style={tone(x.t)}><Icon name={x.icon} size={kid?15:18}/></span><div><span className="t1">{x.name}</span><span className="t2"><span className="mono" style={{fontSize:11.5}}>{x.code||'—'}</span>{x.parent?' · in '+M.dept[x.parent].name:k.length?' · '+k.length+' sub-team'+(k.length>1?'s':''):''}</span></div></div></td>
<td onClick={e=>{if(!head){e.stopPropagation();openAssign(x)}}}>{head?<Who sm name={head.name} sub={M.desig[head.desig].name}/>:<span className="warn-link"><Icon name="user-plus" size={14}/>Assign head</span>}</td>
<td><div className="ic-row"><b className="num" style={{minWidth:28,color:'var(--text-primary)'}}>{n}</b>{!x.parent&&<div className="meter" style={tone(x.t,{width:110})}><span style={{width:pct(n,max)+'%'}}></span></div>}</div></td>
<td className="num">{db.desigs.filter(y=>y.dept===x.id).length}</td><td><Pill s={x.status}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit department',on:()=>setForm(x)},{icon:'user-check',label:head?'Change head':'Assign head',on:()=>openAssign(x)},!x.parent&&{icon:'plus',label:'Add sub-team',on:()=>setForm({parent:x.id,t:x.t,icon:x.icon})}]}/></td></tr>};
const body=ql?db.depts.filter(x=>(x.name+' '+(x.code||'')+' '+(x.head||'')).toLowerCase().includes(ql)).map(x=>row(x,{flat:true})):tops.flatMap(x=>{const k=kids(x.id);return [row(x,{})].concat(open[x.id]?k.map((c,i)=>row(c,{kid:true,last:i===k.length-1})):[])});
const cand=assign?d.live.filter(e=>[assign.id].concat(kids(assign.id).map(k=>k.id)).includes(e.dept)).sort((a,b)=>b.grade.localeCompare(a.grade)||a.name.localeCompare(b.name)):[];
return <>
<Hero title="Departments" sub="Your reporting structure. Sub-teams roll up into their parent, and each head approves leave and attendance for their people." actions={<>{!ql&&withKids.length>0&&<button className="btn" onClick={()=>setOpen(Object.fromEntries(tops.map(x=>[x.id,!allOpen])))}><Icon name={allOpen?'chevrons-down-up':'chevrons-up-down'} size={17}/>{allOpen?'Collapse all':'Expand all'}</button>}<button className="btn pri" onClick={()=>setForm({t:'teal',icon:'briefcase'})}><Icon name="plus" size={17}/>Add department</button></>}/>
{noHead.length>0&&<div className="note" style={tone('amber')}><Icon name="alert-triangle" size={16}/><span><b>{noHead.length} {noHead.length>1?'departments have':'department has'} no head</b> — {noHead.map(x=>x.name).join(', ')}. Approvals for their people fall back to HR until you assign one.</span></div>}
<div className="card tcard"><div className="tbar"><TSearch value={q} onChange={setQ} placeholder="Search department, code or head…"/><span className="sp"></span><span className="meta">{pl(tops.length,'department','departments')} · {pl(db.depts.length-tops.length,'sub-team','sub-teams')}</span></div>
{body.length?<div className="twrap"><table className="t"><thead><tr><th>Department</th><th>Head</th><th>People</th><th>Designations</th><th>Status</th><th className="act"></th></tr></thead><tbody>{body}</tbody></table></div>:<Empty title="No departments match" body="Try a different name or code."/>}</div>
{form&&<DeptForm x={form.name?form:null} init={form} onClose={()=>setForm(null)}/>}
{assign&&<Modal title={(assign.head?'Change head of ':'Assign a head to ')+assign.name} body="The head approves leave, attendance regularisation and expenses for everyone in this department." icon="user-check" t="brand" cta="Save head" okDisabled={!pick} onClose={()=>setAssign(null)} onOk={()=>{const e=cand.find(c=>c.id===pick);update('depts',L=>L.map(x=>x.id===assign.id?Object.assign({},x,{head:e.name,headId:e.id}):x));toast(e.name+' is now head of '+assign.name);setAssign(null)}}><Field label="Department head" hint={cand.length+' people in '+assign.name+', most senior first'}><Dropdown field search value={pick} placeholder={cand.length?'Choose a person…':'No one in this department yet'} options={cand.map(e=>({v:e.id,l:e.name,sub:e.grade+' · '+M.desig[e.desig].name}))} onChange={setPick}/></Field></Modal>}
</>
}
function DeptForm({x,init,onClose}){
const {db,update,toast,act}=useApp();const isEdit=!!x;
const sections=[{title:'Department',fields:[{k:'name',label:'Name',req:true,placeholder:'e.g. Data Science'},{k:'code',label:'Code',req:true,upper:true,placeholder:'e.g. DSC',validate:v=>db.depts.some(y=>y.code===v&&(!isEdit||y.id!==x.id))?'Code already in use':null},{k:'parent',label:'Parent department',type:'select',span2:true,options:[{v:'',l:'None — top-level department'}].concat(db.depts.filter(y=>!y.parent&&(!x||y.id!==x.id)).map(y=>({v:y.id,l:y.name}))),disabled:isEdit&&db.depts.some(y=>y.parent===x.id),hint:isEdit&&db.depts.some(y=>y.parent===x.id)?'It has sub-teams of its own, so it stays top-level. Move its sub-teams out first.':'Sub-teams roll their headcount up into the parent'},
{k:'branches',label:'Branches',type:'multi',span2:true,options:v=>{const co=v.parent?(db.depts.find(y=>y.id===v.parent)||{}).co:(x?x.co:act.defaultCo);return db.branches.filter(b=>b.co===co&&(b.status==='Active'||(v.branches||[]).includes(b.id))).map(b=>({v:b.id,l:b.name}))},hint:'Where this department works. Leave all off if it works at every branch.'},
{k:'icon',type:'custom',render:(v,set)=><Field key="icon" label="Icon" span2><div className="icons">{DEPT_ICONS.map(ic=><button type="button" key={ic} className={v.icon===ic?'on':''} onClick={()=>set('icon',ic)} aria-label={ic}><Icon name={ic} size={18}/></button>)}</div></Field>},
{k:'active',label:'Active',type:'toggle',hint:'Inactive departments can’t receive new people'}]}];
const onSave=v=>{const p=v.parent?db.depts.find(y=>y.id===v.parent):null;const rec=Object.assign({t:'teal',head:null},x||{},v,{parent:v.parent||null,t:p?p.t:(v.t||'teal'),status:v.active?'Active':'Inactive'});delete rec.active;update('depts',L=>isEdit?L.map(y=>y.id===x.id?rec:y):L.concat([rec]));toast(isEdit?'Saved '+rec.name:rec.name+' added'+(p?' under '+p.name:''));onClose()};
return <RecordForm title={isEdit?'Edit '+x.name:(init.parent?'Add sub-team':'Add department')} sub="Assign a head from the Departments table once people join." icon="network" t="teal" sections={sections} initial={Object.assign({parent:''},init,{active:x?x.status==='Active':true})} onSave={onSave} onClose={onClose} cta={isEdit?'Save changes':'Add department'}/>
}
function GradeBadge({g,sub}){return <div className="ic-row"><span className="lv" style={tone(g.t,{background:'var(--tb)',color:'var(--tf)'})}>{g.id}</span><div><span className="t1">{g.name}</span>{sub&&<span className="t2">{sub}</span>}</div></div>}
function DesignationsPage(){
const {db,d,update,toast,t}=useApp();const M=useMaps();const [q,setQ]=React.useState('');const [dept,setDept]=React.useState('');const [grade,setGrade]=React.useState('');const [sort,setSort]=React.useState({k:'grade',d:-1});const [form,setForm]=React.useState(null);
const ids=dept?[dept].concat(db.depts.filter(x=>x.parent===dept).map(x=>x.id)):null;const ql=q.trim().toLowerCase();
const all=db.desigs;const rows=sortRows(all.filter(x=>(!ids||ids.includes(x.dept))&&(!grade||x.grade===grade)&&(!ql||x.name.toLowerCase().includes(ql))),sort,(x,k)=>k==='name'?x.name:k==='people'?(d.desig[x.id]||0):k==='dept'?M.dept[x.dept].name:x.grade+x.name);
const ps=+t.pageSize||10;const P=usePaged(rows,ps,[dept,grade,ql,sort.k,sort.d]);const empty=all.filter(x=>!d.desig[x.id]).length;
return <>
<Hero title="Designations" sub="Job titles, the department they sit in and the grade they map to — the grade decides the pay band." actions={<button className="btn pri" onClick={()=>setForm({dept:dept||'',grade:''})}><Icon name="plus" size={17}/>Add designation</button>}/>
<div className="card tcard"><div className="tbar"><TSearch value={q} onChange={setQ} placeholder="Search job titles…"/><Dropdown label="Department" all="All departments" value={dept} options={deptOptions(db)} onChange={setDept} search icon="layers"/><Dropdown label="Grade" all="All grades" value={grade} options={db.grades.map(g=>({v:g.id,l:g.id+' · '+g.name,t:g.t}))} onChange={setGrade}/><span className="sp"></span><span className="meta">{rows.length} titles{empty?' · '+empty+' unfilled':''}</span></div>
{rows.length?<div className="twrap"><table className="t"><thead><tr><SortTh k="name" sort={sort} setSort={setSort}>Designation</SortTh><SortTh k="dept" sort={sort} setSort={setSort}>Department</SortTh><SortTh k="grade" sort={sort} setSort={setSort}>Grade & band</SortTh><SortTh k="people" sort={sort} setSort={setSort} className="r">People</SortTh><th>Status</th><th className="act"></th></tr></thead><tbody>
{P.slice.map(x=>{const dp=M.dept[x.dept],g=M.grade[x.grade],n=d.desig[x.id]||0;return <tr key={x.id} className={'click'+(x.status==='Active'?'':' off')} onClick={()=>setForm(x)}>
<td><span className="t1">{x.name}</span><span className="t2 mono" style={{fontSize:11.5}}>{x.code||''}</span></td>
<td><div className="ic-row"><span className="tile xs" style={tone(dp.t)}><Icon name={dp.icon} size={14}/></span><div><span className="t1">{dp.name}</span>{dp.parent&&<span className="t2">in {M.dept[dp.parent].name}</span>}</div></div></td>
<td>{g?<GradeBadge g={g} sub={g.min!=null?fmtL(g.min)+' – '+fmtL(g.max)+' a year':'Pay band not set'}/>:x.grade?<span className="chip">{x.grade}</span>:<span className="no">—</span>}</td>
<td className="r">{n?<b className="num" style={{color:'var(--text-primary)'}}>{n}</b>:<span className="no">Unfilled</span>}</td><td><Pill s={x.status}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit designation',on:()=>setForm(x)},'-',x.status==='Active'?{icon:'ban',label:'Deactivate',danger:true,on:()=>{if(n){toast(n+' people hold this title — move them first','info');return}update('desigs',L=>L.map(y=>y.id===x.id?Object.assign({},y,{status:'Inactive'}):y));toast(x.name+' deactivated')}}:{icon:'check-circle',label:'Activate',on:()=>{update('desigs',L=>L.map(y=>y.id===x.id?Object.assign({},y,{status:'Active'}):y));toast(x.name+' activated')}}]}/></td></tr>})}
</tbody></table></div>:<Empty title="No designations match" body="Try another title, department or grade." action={<button className="btn sm" onClick={()=>{setQ('');setDept('');setGrade('')}}>Clear filters</button>}/>}
<TableFoot shown={rows.length} of={all.length} P={P} ps={ps} noun="designations"/></div>
{form&&<DesigForm x={form.name?form:null} init={form} onClose={()=>setForm(null)}/>}
</>
}
function DesigForm({x,init,onClose}){
const {db,update,toast}=useApp();const isEdit=!!x;
const sections=[{title:'Designation',fields:[{k:'name',label:'Job title',req:true,span2:true,placeholder:'e.g. Data Analyst'},{k:'code',label:'Code',upper:true,placeholder:'e.g. SSE',hint:'Optional short code, unique in the company',validate:c=>c&&db.desigs.some(y=>(y.code||'')===c&&(!isEdit||y.id!==x.id))?'Code already in use':null},{k:'dept',label:'Department',type:'select',req:true,search:true,options:deptOptions(db)},{k:'grade',label:'Grade',type:'select',req:true,options:db.grades.map(g=>({v:g.id,l:g.id+' · '+g.name,sub:g.min!=null?fmtL(g.min)+'–'+fmtL(g.max):undefined,t:g.t}))},{k:'active',label:'Active',type:'toggle',hint:'Inactive titles can’t be given to new hires'}]}];
const preview=v=>{const g=db.grades.find(y=>y.id===v.grade);return g&&<div className="preview"><div className="preview-h">Pay band for this title<span>From Grades & Bands</span></div><GradeBadge g={g} sub={g.min!=null?fmtL(g.min)+' – '+fmtL(g.max)+' annual CTC · midpoint '+fmtL((g.min+g.max)/2):'Pay band not set yet'}/></div>};
const onSave=v=>{const rec=Object.assign({},x||{},v,{id:isEdit?x.id:'DS'+Date.now().toString().slice(-4),status:v.active?'Active':'Inactive'});delete rec.active;update('desigs',L=>isEdit?L.map(y=>y.id===x.id?rec:y):L.concat([rec]));toast(isEdit?'Saved '+rec.name:rec.name+' added');onClose()};
return <RecordForm title={isEdit?'Edit '+x.name:'Add designation'} sub="Titles appear on offer letters, payslips and the org chart." icon="award" t="teal" sections={sections} initial={Object.assign({},init,{active:x?x.status==='Active':true})} onSave={onSave} onClose={onClose} preview={preview} cta={isEdit?'Save changes':'Add designation'}/>
}
function Ladder({n}){return <span className="lvl" aria-hidden="true">{[1,2,3,4,5,6,7].map(i=><span key={i} className={i<=n?'on':''} style={{height:4+i*1.7}}></span>)}</span>}
function GradesPage(){
const {db,d,update,toast}=useApp();const [form,setForm]=React.useState(null);const HI=10000000;
const G=db.grades.slice().sort((a,b)=>((a.level||0)-(b.level||0))||a.id.localeCompare(b.id,undefined,{numeric:true}));
return <>
<Hero title="Grades & Bands" sub="Your career ladder and the annual CTC range each level pays. Designations inherit their band from the grade." actions={<button className="btn pri" onClick={()=>setForm({})}><Icon name="plus" size={17}/>Add grade</button>}/>
<div className="card tcard"><div className="twrap"><table className="t"><thead><tr><th>Grade</th><th style={{width:'38%'}}><div className="th2"><span>Annual CTC band</span><div className="axis">{['₹0','25L','50L','75L','1Cr'].map((h,i)=><span key={h} style={{left:i*25+'%'}}>{h}</span>)}</div></div></th><th>Range</th><th className="r">Titles</th><th className="r">People</th><th className="act"></th></tr></thead><tbody>
{G.map((g,i)=>{const prev=G[i-1];const ov=prev&&prev.max>g.min?prev.max-g.min:0;return <tr key={g.id} className="click" onClick={()=>setForm(g)}>
<td><div className="ic-row"><GradeBadge g={g}/><Ladder n={i+1}/></div></td>
<td><div className="band" style={tone(g.t)}>{[25,50,75].map(x=><span key={x} className="g" style={{left:x+'%'}}></span>)}<span className="base"></span>{g.min!=null&&<span className="s" style={{left:Math.min(100,g.min/HI*100)+'%',width:Math.max(1,Math.min(100,(g.max-g.min)/HI*100))+'%'}}></span>}</div></td>
<td>{g.min==null?<><span className="no">—</span><span className="t2">{g.hidden?'Pay band hidden':'Band not set yet'}</span></>:<><span className="t1 num">{fmtL(g.min)} – {fmtL(g.max)}</span><span className="t2">{ov?'Overlaps '+prev.id+' by '+fmtL(ov):'Midpoint '+fmtL((g.min+g.max)/2)}</span></>}</td>
<td className="r num">{db.desigs.filter(x=>x.grade===g.id).length}</td><td className="r"><b className="num" style={{color:'var(--text-primary)'}}>{d.grade[g.id]||0}</b></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit band',on:()=>setForm(g)},'-',{icon:'ban',label:'Deactivate',danger:true,on:()=>{const n=d.grade[g.id]||0,tl=db.desigs.filter(x=>x.grade===g.id).length;if(n||tl){toast((tl?pl(tl,'title uses','titles use'):pl(n,'person is','people are')+' on')+' '+g.id+' — move them first','info');return}update('grades',L=>L.map(x=>x.id===g.id?Object.assign({},x,{status:'Inactive'}):x));toast(g.id+' · '+g.name+' deactivated')}}]}/></td></tr>})}
</tbody></table></div></div>
{form&&<GradeForm g={form.id?form:null} onClose={()=>setForm(null)}/>}
</>
}
function GradeForm({g,onClose}){
const {db,update,toast,act}=useApp();const isEdit=!!g;const nextId='L'+(db.grades.length+1);
const sections=[{title:'Grade',fields:[{k:'id',label:'Level',req:true,upper:true,placeholder:nextId},{k:'name',label:'Name',req:true,placeholder:'e.g. Principal'}]},{title:'Annual CTC band',fields:[{k:'min',label:'Minimum',type:'number',prefix:'₹',min:0,step:50000,disabled:!act.canBands,hint:act.canBands?'Leave both empty if this grade has no band':'You don’t have access to pay bands',validate:(x,v)=>(x===''||x==null)===(v.max===''||v.max==null)?null:'Enter both amounts, or neither'},{k:'max',label:'Maximum',type:'number',prefix:'₹',min:0,step:50000,disabled:!act.canBands,validate:(x,v)=>x===''||x==null||+x>+v.min?null:'Must be more than the minimum'}]}];
const preview=v=>{const mn=+v.min||0,mx=+v.max||0,HI=10000000;return <div className="preview"><div className="preview-h">Band preview<span>{mn&&mx?fmtL(mn)+' – '+fmtL(mx):'Enter a range'}</span></div><div className="band" style={tone(g?g.t:'teal')}>{[25,50,75].map(x=><span key={x} className="g" style={{left:x+'%'}}></span>)}<span className="base"></span>{mx>mn&&<span className="s" style={{left:Math.min(100,mn/HI*100)+'%',width:Math.min(100,(mx-mn)/HI*100)+'%'}}></span>}</div><div className="axis">{['₹0','25L','50L','75L','1Cr'].map((h,i)=><span key={h} style={{left:i*25+'%'}}>{h}</span>)}</div></div>};
const onSave=v=>{const rec=Object.assign({t:'teal',status:'Active'},g||{},v,act.canBands?{min:v.min===''||v.min==null?null:+v.min,max:v.max===''||v.max==null?null:+v.max}:{min:g?g.min:null,max:g?g.max:null});update('grades',L=>isEdit?L.map(x=>x.id===g.id?rec:x):L.concat([rec]));toast(isEdit?rec.id+' band updated':rec.id+' · '+rec.name+' added');onClose()};
return <RecordForm title={isEdit?g.id+' · '+g.name:'Add grade'} sub="Bands guide offers and revisions — Salary Structure warns when someone’s pay falls outside the band." icon="layers" t="teal" sections={sections} initial={g||{id:nextId,min:'',max:''}} onSave={onSave} onClose={onClose} preview={preview} cta={isEdit?'Save band':'Add grade'}/>
}
const WEEK=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'];
const offsLabel=o=>!o||!o.length?'No weekly off':o.length===2&&o[0]==='Sat'&&o[1]==='Sun'?'Sat–Sun off':o.join(', ')+' off';
function ShiftsPage(){
const {db,d,update,toast}=useApp();const [form,setForm]=React.useState(null);
const setSt=(s,st)=>{const n=d.shift[s.id]||0;if(st==='Inactive'&&n){toast('Move the '+n+' people on '+s.name+' to another shift first','info');return}update('shifts',L=>L.map(x=>x.id===s.id?Object.assign({},x,{status:st}):x));toast(s.name+' shift '+(st==='Active'?'activated':'deactivated'))};
const dup=s=>setForm({copy:Object.assign({},s,{name:s.name+' (copy)',code:''})});
return <>
<Hero title="Shift Rules" sub="Working hours, grace windows and overtime for every shift. Attendance marks late check-ins and overtime from these rules." actions={<button className="btn pri" onClick={()=>setForm({})}><Icon name="plus" size={17}/>Add shift</button>}/>
<div className="card tcard"><div className="twrap"><table className="t"><thead><tr><th>Shift</th><th>Timing</th><th style={{width:'28%'}}><div className="th2"><span>Day coverage</span><DayAxis/></div></th><th>Grace<Tip>Minutes after the shift starts during which a check-in still counts as on time. Anything later is marked Late.</Tip></th><th>Overtime<Tip>The multiplier recorded for hours beyond the daily target. Overtime is approved in Attendance; payroll doesn’t pay it automatically yet.</Tip></th><th className="r">People</th><th>Status</th><th className="act"></th></tr></thead><tbody>
{db.shifts.map(s=><tr key={s.id} className={'click'+(s.status==='Active'?'':' off')} onClick={()=>setForm(s)}>
<td><div className="ic-row"><span className="tile" style={tone(s.t)}><Icon name={s.icon} size={18}/></span><div><span className="t1">{s.name}</span><span className="t2">{s.kind}{s.offs?' · '+offsLabel(s.offs):''}</span></div></div></td>
<td><span className="mono" style={{color:'var(--text-primary)',fontWeight:600}}>{hm(s.start)} – {hm(s.end)}</span><span className="t2">{s.kind==='Flexible'&&s.core?'Core '+hm(s.core[0])+'–'+hm(s.core[1]):s.hours+'h daily target'}{s.end<s.start?' · overnight':''}</span></td>
<td><DayBar start={s.start} end={s.end} core={s.kind==='Flexible'?s.core:null} t={s.t}/></td>
<td>{s.grace!=null?<><span className="t1">{s.grace} min</span><span className="t2">Late after {hm(s.start+s.grace)}</span></>:<span className="no">Not tracked</span>}</td>
<td>{s.ot?<><span className="yes"><Icon name="check" size={15} stroke={2.6}/>{s.rate}</span><span className="t2">beyond {s.hours}h</span></>:<span className="no">—</span>}</td>
<td className="r"><b className="num" style={{color:'var(--text-primary)'}}>{d.shift[s.id]||0}</b></td><td><Pill s={s.status}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit shift',on:()=>setForm(s)},{icon:'copy',label:'Duplicate',on:()=>dup(s)},'-',s.status==='Active'?{icon:'ban',label:'Deactivate',danger:true,on:()=>setSt(s,'Inactive')}:{icon:'check-circle',label:'Activate',on:()=>setSt(s,'Active')}]}/></td></tr>)}
</tbody></table></div></div>
{form&&<ShiftForm s={form.id?form:null} copy={form.copy} onClose={()=>setForm(null)}/>}
</>
}
function ShiftForm({s,copy,onClose}){
const {db,update,toast}=useApp();const isEdit=!!s;const src=s||copy;
const init=src?Object.assign({},src,{_key:s?src._key:undefined,id:s?src.id:'',startS:hm(src.start),endS:hm(src.end),coreS:src.core?hm(src.core[0]):'11:00',coreE:src.core?hm(src.core[1]):'16:00',grace:src.grace==null?15:src.grace,rate:src.rate||'1.5×',active:s?s.status==='Active':true}):{kind:'Fixed',startS:'09:00',endS:'18:00',coreS:'11:00',coreE:'16:00',grace:15,hours:8,offs:[],ot:false,rate:'1.5×',active:true};
const sections=[
{title:'Shift',fields:[{k:'name',label:'Shift name',req:true,placeholder:'e.g. General'},{k:'code',label:'Code',upper:true,placeholder:'e.g. GEN',hint:'Optional. A short code for rosters and reports',validate:x=>x&&!/^[A-Z0-9_-]{1,20}$/.test(x)?'Up to 20 letters, digits, - or _':x&&db.shifts.some(y=>y.status==='Active'&&y.code===x&&(!isEdit||y.id!==s.id))?'Code already in use':null},{k:'kind',label:'Shift type',type:'seg',span2:true,options:v=>[{v:'Fixed',l:'Fixed hours',icon:'clock'},{v:'Flexible',l:'Flexible with core hours',icon:'timer'}].concat(v.kind==='Rotational'?[{v:'Rotational',l:'Rotational',icon:'refresh-cw'}]:[])}]},
{title:'Timing',fields:[{k:'startS',label:'Starts',type:'time',req:true},{k:'endS',label:'Ends',type:'time',req:true,hint:'Ending before the start time runs overnight'},{k:'coreS',label:'Core hours from',type:'time',req:true,when:v=>v.kind==='Flexible',hint:'Check-ins after this are marked Late'},{k:'coreE',label:'Core hours until',type:'time',req:true,when:v=>v.kind==='Flexible',validate:(x,v)=>toMin(x)<=toMin(v.coreS)?'Must end after core hours start':toMin(v.startS)<toMin(v.endS)&&(toMin(v.coreS)<toMin(v.startS)||toMin(x)>toMin(v.endS))?'Keep core hours inside the shift':null},{k:'grace',label:'Grace period',type:'number',min:0,max:120,suffix:'minutes',when:v=>v.kind==='Fixed',hint:'0–120. Check-ins inside this window aren’t marked Late.',validate:x=>x===''||x==null||(x>=0&&x<=120)?null:'Between 0 and 120'},{k:'hours',label:'Working hours per day',type:'number',min:0.5,max:24,step:0.5,suffix:'hours',req:true,hint:'0.5–24. The daily target for this shift.',validate:x=>x>=0.5&&x<=24?null:'Between 0.5 and 24'},{k:'offs',label:'Weekly off',type:'multi',span2:true,options:WEEK,hint:'Used for people on this shift who have no weekly offs on their own record. Leave empty for Saturday and Sunday.',validate:x=>x&&x.length>=7?'Leave at least one working day':null}]},
{title:'Overtime & status',fields:[{k:'ot',label:'Overtime applicable',type:'toggle',hint:'Records a rate for hours beyond the daily target. Payroll doesn’t pay it automatically yet.'},{k:'rate',label:'Overtime rate',type:'seg',span2:true,when:v=>v.ot,options:v=>Array.from(new Set(['1.25×','1.5×','2×'].concat(v.rate?[v.rate]:[])))},{k:'active',label:'Active',type:'toggle',when:()=>isEdit,hint:'Inactive shifts can’t be assigned to people'}]}];
const preview=v=>{const a=toMin(v.startS),b=toMin(v.endS),span=((b-a+1440)%1440||1440)/60;const core=v.kind==='Flexible'?[toMin(v.coreS),toMin(v.coreE)]:null;return <div className="preview"><div className="preview-h">Preview<span>{hm(a)} – {hm(b)} · {+span.toFixed(2)}h span{b<a?' · overnight':''}</span></div><div><DayBar start={a} end={b} core={core} t={s?s.t:'teal'}/><div style={{marginTop:6}}><DayAxis/></div></div><div className="hint">{v.kind==='Flexible'?'Everyone is expected between '+(v.coreS||'—')+' and '+(v.coreE||'—')+'; a check-in after '+(v.coreS||'—')+' is marked Late. The rest of the window is flexible.':'Check-ins after '+hm(a+(+v.grace||0))+' are marked Late'+(v.ot?'; overtime past '+(v.hours||8)+'h is recorded at '+v.rate+'.':'.')}</div></div>};
const onSave=v=>{const rec=Object.assign({icon:'clock',t:'teal'},s||{},{id:v.id,code:v.code||'',name:v.name,kind:v.kind,start:toMin(v.startS),end:toMin(v.endS),core:v.kind==='Flexible'?[toMin(v.coreS),toMin(v.coreE)]:null,grace:v.kind==='Fixed'?(+v.grace||0):null,hours:+v.hours,offs:v.offs||[],ot:!!v.ot,rate:v.ot?v.rate:null,status:v.active?'Active':'Inactive'});update('shifts',L=>isEdit?L.map(x=>x.id===s.id?rec:x):L.concat([rec]));toast(isEdit?rec.name+' shift saved':rec.name+' shift added');onClose()};
return <RecordForm title={isEdit?s.name+' shift':'Add shift'} sub="Changes apply to new check-ins; past days keep the marks they already have." icon="clock" t="amber" sections={sections} initial={init} onSave={onSave} onClose={onClose} preview={preview} cta={isEdit?'Save shift':'Add shift'}/>
}
const LEAVE_CATS=['Casual','Sick','Earned','Maternity','Paternity','Bereavement','Comp-off','Unpaid','Study','Sabbatical'];
function leaveEnt(l){if(l.quota==null)return l.cat==='Comp-off'?['Earned as you go','Expires after '+(l.expiry||60)+' days']:['No limit','Deducted from pay'];const per=l.quota/12;return [l.quota+' days / year',l.accrual==='Monthly'?(+per.toFixed(2))+(per===1?' day':' days')+' accrue monthly':l.accrual==='Quarterly'?(+(l.quota/4).toFixed(2))+' days each quarter':'Credited upfront']}
function LeavesPage(){
const {db,update,toast}=useApp();const [form,setForm]=React.useState(null);
const allow=id=>db.leaves.filter(l=>l.status==='Active'&&l.paid&&l.quota&&(!l.applies||l.applies.includes(id))&&l.cat!=='Maternity'&&l.cat!=='Paternity'&&l.cat!=='Comp-off').reduce((s,l)=>s+l.quota,0);
const C=db.classes.filter(c=>['Full-time','Part-time','Intern'].includes(c.type));const top=Math.max(1,...C.map(c=>allow(c.id)));
return <>
<Hero title="Leave Rules" sub="Entitlement, accrual and carry-forward for each leave type, and which classifications get it." actions={<button className="btn pri" onClick={()=>setForm({})}><Icon name="plus" size={17}/>Add leave type</button>}/>
<div className="stats">{C.map(c=><Stat key={c.id} icon={c.icon} t={c.t} label={c.name+' allowance'} value={allow(c.id)} unit="days / yr" sub="Paid leave, before parental and comp-off" z={<Meter v={pct(allow(c.id),top)}/>}/>)}</div>
<div className="card tcard"><div className="twrap"><table className="t"><thead><tr><th>Leave type</th><th>Category</th><th>Entitlement</th><th>Carry forward<Tip>Unused days that move into next year, up to the cap. Anything above the cap lapses on 31 December.</Tip></th><th>Applies to</th><th>Status</th><th className="act"></th></tr></thead><tbody>
{db.leaves.map(l=>{const e=leaveEnt(l);return <tr key={l.id} className={'click'+(l.status==='Active'?'':' off')} onClick={()=>setForm(l)}>
<td><div className="ic-row"><span className="tile" style={tone(l.t)}><Icon name={l.icon} size={18}/></span><div><span className="t1">{l.name}<span className="code" style={{marginLeft:8}}>{l.code}</span></span><span className="t2">{l.paid?'Paid':'Unpaid'}</span></div></div></td>
<td><span className="chip">{l.cat}</span></td><td><span className="t1">{e[0]}</span><span className="t2">{e[1]}</span></td>
<td>{l.carry?<><span className="t1">Up to {l.carry} days</span><span className="t2">{l.encash?'Encashable'+(l.encashMax?' · up to '+l.encashMax+' days a year':''):'Not encashable'}</span></>:<span className="no">No carry-forward</span>}</td>
<td><div className="chips">{l.applies?db.classes.filter(c=>l.applies.includes(c.id)).map(c=><Pill key={c.id} t={c.t}>{c.code}</Pill>):<Pill t="slate">{l.gender==='FEMALE'?'Women':l.gender==='MALE'?'Men':'Everyone'}</Pill>}</div></td><td><Pill s={l.status}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit rules',on:()=>setForm(l)},'-',l.status==='Active'?{icon:'ban',label:'Deactivate',danger:true,on:()=>{update('leaves',L=>L.map(x=>x.id===l.id?Object.assign({},x,{status:'Inactive'}):x));toast(l.name+' deactivated — existing balances stay')}}:{icon:'check-circle',label:'Activate',on:()=>{update('leaves',L=>L.map(x=>x.id===l.id?Object.assign({},x,{status:'Active'}):x));toast(l.name+' activated')}}]}/></td></tr>})}
</tbody></table></div></div>
{form&&<LeaveForm l={form.id?form:null} onClose={()=>setForm(null)}/>}
</>
}
function LeaveForm({l,onClose}){
const {db,update,toast}=useApp();const isEdit=!!l;
const init=l?Object.assign({},l,{paidS:l.paid?'Paid':'Unpaid',carryOn:!!l.carry,carry:l.carry||30,quota:l.quota==null?'':l.quota,active:l.status==='Active'}):{cat:'Casual',paidS:'Paid',quota:12,accrual:'Upfront',carryOn:false,carry:30,encash:false,applies:[],active:true};
const sections=[
{title:'Leave type',fields:[{k:'name',label:'Name',req:true,placeholder:'e.g. Marriage Leave'},{k:'code',label:'Code',req:true,upper:true,placeholder:'e.g. MRL',validate:x=>db.leaves.some(y=>y.code===x&&(!isEdit||y.id!==l.id))?'Code already in use':null},{k:'cat',label:'Category',type:'select',options:LEAVE_CATS},{k:'paidS',label:'Pay',type:'seg',options:['Paid','Unpaid']}]},
{title:'Entitlement',fields:[{k:'quota',label:'Annual quota',type:'number',min:0,suffix:'days / year',req:true,hint:'Days a year, more than 0',validate:x=>+x>0?null:'Must be more than 0'},{k:'accrual',label:'Credited',type:'seg',options:['Monthly','Quarterly','Upfront'],hint:'Monthly: a twelfth of the quota on the 1st of each month. Quarterly: a quarter on 1 Jan, Apr, Jul and Oct. Upfront: the whole quota at the start of the year. Monthly and quarterly credits start from the month someone joins.'},{k:'carryOn',label:'Carry forward unused days',type:'toggle',hint:'Balances above the cap lapse on 31 December'},{k:'carry',label:'Carry-forward cap',type:'number',min:1,suffix:'days max',when:v=>v.carryOn},{k:'encash',label:'Encashable',type:'toggle',hint:'Employees can ask to cash in unused days. Once HR approves, the days come off the balance and are paid in the next payroll run at one day’s Basic each.',when:v=>v.carryOn},{k:'encashMax',label:'Most days encashed a year',type:'number',min:1,max:365,suffix:'days max',when:v=>v.carryOn&&v.encash,hint:'Per person. Leave empty for no yearly limit (the balance is still the limit).',validate:x=>x===''||x==null||(+x>=1&&+x<=365)?null:'Between 1 and 365'}]},
{title:'Eligibility',fields:[{k:'applies',label:'Applies to',type:'multi',span2:true,options:db.classes.map(c=>({v:c.id,l:c.name})),disabled:true,hint:'Coming soon — every leave type applies to everyone today'},{k:'active',label:'Active',type:'toggle',when:()=>isEdit,hint:'Inactive types can’t be applied for; balances are kept'}]}];
const preview=v=>{const q=+v.quota;if(!q)return null;const nm=+NEXT_MONTH.slice(5,7);const got=v.accrual==='Monthly'?q/12*(13-nm):v.accrual==='Quarterly'?q/4*(4-Math.floor((nm-1)/3)):q;return <div className="preview"><div className="preview-h">What a new joiner gets<span>{'Joining '+fmtDate(NEXT_MONTH)}</span></div><div className="ic-row"><span className="tile" style={tone('teal')}><Icon name="calendar-check" size={18}/></span><div><span className="t1">{+got.toFixed(2)} days before the year resets</span><span className="t2">{v.accrual==='Upfront'?'Full quota credited on joining':v.accrual==='Monthly'?'A twelfth credited on the 1st of each month':'A quarter credited at the start of each quarter'}{v.carryOn?' · up to '+v.carry+' days carry into '+(+TODAY_ISO.slice(0,4)+1):''}</span></div></div></div>};
const onSave=v=>{const q=v.quota===''||v.quota==null?null:+v.quota;const rec=Object.assign({icon:'calendar',t:'teal'},l||{},{id:v.id,code:v.code,name:v.name,cat:v.cat,paid:v.paidS==='Paid',quota:q,accrual:v.accrual,carry:v.carryOn?+v.carry:0,encash:!!(v.carryOn&&v.encash),encashMax:v.carryOn&&v.encash&&v.encashMax!==''&&v.encashMax!=null?+v.encashMax:null,applies:v.applies,status:v.active?'Active':'Inactive'});update('leaves',L=>isEdit?L.map(x=>x.id===l.id?rec:x):L.concat([rec]));toast(isEdit?rec.name+' rules saved':rec.name+' added');onClose()};
return <RecordForm title={isEdit?l.name:'Add leave type'} sub="Monthly and quarterly credits follow these rules from the next accrual run; days already credited stay." icon="calendar-check" t="amber" sections={sections} initial={init} onSave={onSave} onClose={onClose} preview={preview} cta={isEdit?'Save rules':'Add leave type'}/>
}
const POLICY_CATS=['Workplace','HR operations','Compliance','Finance','IT'];
function PoliciesPage(){
const {db,d,update,toast,act:ax,route}=useApp();const [del,setDel]=React.useState(null);const [tab,setTab]=React.useState(['Active','Draft','Archived'].includes(route.status)?route.status:'Active');const [q,setQ]=React.useState(route.q||'');const [cat,setCat]=React.useState('');const [form,setForm]=React.useState(null);
const P=db.policies,hc=d.headcount,cnt=s=>P.filter(p=>p.status===s).length;const ql=q.trim().toLowerCase();
const rows=P.filter(p=>p.status===tab&&(!cat||p.cat===cat)&&(!ql||(p.title+' '+p.cat).toLowerCase().includes(ql)));
const act=P.filter(p=>p.status==='Active'&&p.ackReq);const pending=act.reduce((s,p)=>s+Math.max(0,hc-p.ack),0);const rate=act.length?Math.round(act.reduce((s,p)=>s+Math.min(1,p.ack/hc),0)/act.length*100):100;
const set=(p,ch,msg)=>{update('policies',L=>L.map(x=>x.id===p.id?Object.assign({},x,ch):x));toast(msg)};
const bump=v=>{const [a,b]=String(v).split('.').map(Number);return a+'.'+((b||0)+1)};
const newVer=p=>{const eff=new Date(TODAY.getTime()+14*864e5).toISOString().slice(0,10);update('policies',L=>L.concat([Object.assign({},p,{_key:undefined,id:'P'+Date.now(),ver:bump(p.ver),status:'Draft',ack:0,eff})]));setTab('Draft');toast('Draft v'+bump(p.ver)+' of '+p.title+' created')};
return <>
<Hero title="Policy Documents" sub="Publish the policies employees must read, and see who has acknowledged each version." actions={<button className="btn pri" onClick={()=>setForm({})}><Icon name="send" size={17}/>Publish a policy</button>}/>
<div className="stats">
<Stat icon="file-text" t="blue" label="Live policies" value={cnt('Active')} sub={cnt('Draft')+' drafts waiting to publish'}/>
<Stat icon="check-circle" t={rate>=90?'green':'amber'} label="Acknowledgement rate" value={rate} unit="%" sub="Average across live policies" z={<Meter v={rate}/>}/>
<Stat icon="hourglass" t="orange" label="Signatures pending" value={pending} sub={'From '+hc+' people on the roster'}/>
</div>
<div className="card tcard"><div className="ttabs">{[['Active','Live','green'],['Draft','Drafts','amber'],['Archived','Archived','slate']].map(([s,l,tn])=><button key={s} className={'ttab'+(tab===s?' on':'')} style={tone(tn)} onClick={()=>setTab(s)}><i className="tdot"></i>{l}<span className="n">{cnt(s)}</span></button>)}</div>
<div className="tbar"><TSearch value={q} onChange={setQ} placeholder="Search policies…"/><Dropdown label="Category" all="All categories" value={cat} options={Array.from(new Set(POLICY_CATS.concat(P.map(p=>p.cat).filter(Boolean))))} onChange={setCat}/><span className="sp"></span><span className="meta">{rows.length} shown</span></div>
{rows.length?<div className="twrap"><table className="t"><thead><tr><th>Policy</th><th>Version</th><th>Effective</th><th>Acknowledged</th><th>Status</th><th className="act"></th></tr></thead><tbody>
{rows.map(p=>{const pc=pct(p.ack,hc),tn=pc>=95?'green':pc>=75?'amber':'red';return <tr key={p.id} className={'click'+(p.status==='Archived'?' off':'')} onClick={()=>setForm(p)}>
<td><div className="ic-row"><span className="tile" style={tone(p.status==='Draft'?'amber':p.status==='Archived'?'slate':'blue')}><Icon name="file-text" size={18}/></span><div><span className="t1">{p.title}</span><span className="t2">{p.cat}</span></div></div></td>
<td><span className="code">v{p.ver}</span></td>
<td><span className="t1 num">{fmtDate(p.eff)}</span><span className="t2">{!p.eff?'':p.eff>TODAY_ISO?'In '+daysTo(p.eff)+' days':tenure(p.eff).replace('Joined this month','This month')+' ago'}</span></td>
<td>{p.status==='Draft'?<button className="btn sm" onClick={e=>{e.stopPropagation();set(p,{status:'Active',ack:0},p.title+' published — '+hc+' people asked to acknowledge')}}><Icon name="send" size={15}/>Publish now</button>:p.ackReq?<div className="ack" style={tone(tn)}><div className="ack-top"><span><b className="num">{Math.min(p.ack,hc)}</b> / {hc}</span><span className="muted">{Math.min(100,pc)}%</span></div><Meter v={pc} wide/></div>:<span className="no">Not required</span>}</td>
<td><Pill s={p.status}/></td>
<td className="act"><Menu items={p.status==='Active'?[{icon:'pencil',label:'Edit details',on:()=>setForm(p)},p.ackReq&&p.ack<hc&&{icon:'bell',label:'Remind '+(hc-p.ack)+' people',on:()=>ax.remindPolicy(p)},{icon:'copy',label:'Start new version',on:()=>newVer(p)},'-',{icon:'archive',label:'Archive',danger:true,on:()=>set(p,{status:'Archived'},p.title+' archived')}]:p.status==='Draft'?[{icon:'pencil',label:'Edit draft',on:()=>setForm(p)},{icon:'send',label:'Publish now',on:()=>set(p,{status:'Active',ack:0},p.title+' published')},'-',{icon:'trash',label:'Discard draft',danger:true,on:()=>setDel(p)}]:[{icon:'refresh-cw',label:'Restore as live',on:()=>set(p,{status:'Active'},p.title+' restored')}]}/></td></tr>})}
</tbody></table></div>:<Empty icon="file-text" title={'No '+(tab==='Active'?'live':tab.toLowerCase())+' policies'+(q||cat?' match':'')} body={tab==='Draft'?'Drafts you save while writing a policy show up here.':'Try another search or category.'}/>}</div>
{form&&<PolicyForm p={form.id?form:null} onClose={()=>setForm(null)} onDone={s=>setTab(s)}/>}{del&&<Modal title={'Discard '+del.title+'?'} body="The draft is deleted for good. It was never published, so nobody has seen it." icon="trash" t="red" danger cta="Discard draft" onClose={()=>setDel(null)} onOk={()=>{const x=del;setDel(null);update('policies',L=>L.filter(y=>y.id!==x.id));toast('Draft discarded')}}/>}
</>
}
function PolicyForm({p,onClose,onDone}){
const {d,update,toast}=useApp();const isEdit=!!p;
const sections=[{title:'Policy',fields:[{k:'title',label:'Title',req:true,span2:true,placeholder:'e.g. Remote Work Policy'},{k:'cat',label:'Category',type:'select',options:v=>Array.from(new Set(POLICY_CATS.concat(v.cat?[v.cat]:[])))},{k:'ver',label:'Version',placeholder:'e.g. v1.0'},{k:'eff',label:'Effective date',type:'date',req:true},{k:'content',label:'Content',type:'textarea',span2:true,rows:6,placeholder:'The full policy text employees will read and acknowledge…'}]},
{title:'Distribution',fields:[{k:'ackReq',label:'Require acknowledgement',type:'toggle',hint:'Employees are asked to confirm they’ve read it, and can be reminded. Off: published for reading only.'},{k:'remindAfter',label:'Remind automatically after',type:'number',min:1,max:90,suffix:'days',when:v=>v.ackReq,hint:'One reminder goes to anyone who hasn’t acknowledged by then. Leave empty for none.',validate:x=>x===''||x==null||(+x>=1&&+x<=90)?null:'Between 1 and 90 days'},{k:'notify',label:'Email everyone when published',type:'toggle',disabled:!!(isEdit&&p.status!=='Draft'),hint:isEdit&&p.status!=='Draft'?'Already published. Use Remind to nudge people who haven’t acknowledged.':'Everyone on the roster ('+pl(d.headcount,'person','people')+') gets an email and an in-app notice when it’s published.'}]}];
const save=(v,mode)=>{const st=isEdit?(mode==='draft'?'Draft':p.status==='Draft'?'Active':p.status):(mode==='draft'?'Draft':'Active');const rec=Object.assign({ack:0},p||{},v,{id:isEdit?p.id:'P'+Date.now(),ver:String(v.ver||'1.0').replace(/^v/i,''),status:st});update('policies',L=>isEdit?L.map(x=>x.id===p.id?rec:x):L.concat([rec]));toast(st==='Draft'?rec.title+' saved as draft':isEdit&&p.status!=='Draft'?rec.title+' updated':rec.title+' published'+(v.notify&&(!isEdit||p.status==='Draft')?' · emailing '+pl(d.headcount,'person','people')+' now':''));onDone&&onDone(st);onClose()};
return <RecordForm title={isEdit?p.title:'Publish a policy'} sub={isEdit?'Version '+p.ver+' · '+p.status:'Draft it here, then publish when it’s ready.'} icon="file-text" t="blue" sections={sections} initial={Object.assign({cat:'Workplace',ver:'1.0',eff:TODAY_ISO,ackReq:true,notify:false},p||{})} onSave={save} onClose={onClose} secondary={!isEdit||p.status==='Draft'?{label:'Save as draft',mode:'draft'}:null} cta={isEdit&&p.status!=='Draft'?'Save changes':'Publish policy'}/>
}
const METHOD_BY_CAT={Earning:['fixed','pct_basic','pct_gross','formula'],Deduction:['fixed','pct_basic','pct_gross','statutory'],Employer:['pct_basic','pct_gross','statutory'],Reimbursement:['fixed']};
function ctcSplit(ctc,comps){const SP=comps.find(x=>x.code==='SPECIAL'&&x.status==='Active');const m=ctc/12,b=Math.round(m*0.5),h=Math.round(b*0.4),cv=m>=20000?1600:0,rest=Math.max(0,m-b-h-cv);return [{k:'BASIC',l:'Basic',v:b*12,t:'brand'},{k:'HRA',l:'HRA',v:h*12,t:'teal'},{k:'SPECIAL',l:SP?'Special allowance':'Unallocated',v:rest*12,t:SP?'blue':'red'},{k:'CONVEYANCE',l:'Conveyance',v:cv*12,t:'amber'}].filter(x=>x.v>0)}
function ComponentsPage(){
const {db,update,toast,go}=useApp();const [del,setDel]=React.useState(null);const [off,setOff]=React.useState(null);const [cat,setCat]=React.useState('');const [q,setQ]=React.useState('');const [form,setForm]=React.useState(null);const [ctc,setCtc]=React.useState(1200000);
const C=db.components;const ql=q.trim().toLowerCase();const rows=C.filter(c=>(!cat||c.cat===cat)&&(!ql||(c.name+' '+c.code).toLowerCase().includes(ql)));
const split=ctcSplit(ctc,C);const pfEr=(split.find(s=>s.k==='PF_EMPLOYER')||{v:0}).v;const gross=ctc-pfEr;
const setSt=(c,st)=>{update('components',L=>L.map(x=>x.code===c.code?Object.assign({},x,{status:st}):x));toast(c.name+(st==='Active'?' switched on — paid from the next run':' switched off — skipped from the next run'))};
return <>
<Hero title="Salary Components" sub="The earnings, deductions and employer contributions payroll computes every month." actions={<button className="btn pri" onClick={()=>setForm({cat:cat||'Earning'})}><Icon name="plus" size={17}/>Add component</button>}/>
<div className="catgrid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(185px,1fr))'}}>
<button className={'cat'+(!cat?' on':'')} style={tone('brand')} onClick={()=>setCat('')}><span className="tile"><Icon name="layers" size={18}/></span><div style={{minWidth:0}}><b>All components</b><small>Everything payroll runs</small></div><span className="cn">{C.length}</span></button>
{Object.entries(CAT_META).map(([k,m])=><button key={k} className={'cat'+(cat===k?' on':'')} style={tone(m.t)} onClick={()=>setCat(cat===k?'':k)}><span className="tile"><Icon name={m.icon} size={18}/></span><div style={{minWidth:0}}><b>{m.l}</b><small>{m.d}</small></div><span className="cn">{C.filter(c=>c.cat===k).length}</span></button>)}</div>
<div className="card mixcard"><div className="mix-h" style={{flexWrap:'wrap'}}><div><h3>How a CTC splits</h3><p>The split new salary structures use · PF, ESI and PT follow Statutory Settings</p></div><div style={{display:'flex',alignItems:'flex-end',gap:20,flexWrap:'wrap'}}><div className="field" style={{width:190}}><label>Annual CTC</label><div className="iw has-pre"><span className="pre">₹</span><input className="input num" inputMode="numeric" value={ctc?ctc.toLocaleString('en-IN'):''} onChange={e=>setCtc(Math.min(99999999,+e.target.value.replace(/\D/g,'')||0))}/></div></div><div className="mix-total"><b className="num">{fmtINR(gross/12)}</b><small>gross / month</small></div></div></div>
{ctc>0?<><div className="mix">{split.map(s=><span key={s.k} style={Object.assign({flex:s.v},tone(s.t))} title={s.l+': '+fmtINR(s.v)+' a year'}></span>)}</div><div className="legend">{split.map(s=><div className="lg" key={s.k} style={tone(s.t)}><i></i>{s.l}<b className="num">{fmtINR(s.v/12)}</b><span className="muted">{pct(s.v,ctc)}%</span></div>)}</div></>:<div className="hint">Enter a CTC to preview the split.</div>}</div>
<div className="card tcard"><div className="tbar"><TSearch value={q} onChange={setQ} placeholder="Search name or code…"/><span className="sp"></span><span className="meta">{rows.length} of {C.length} components</span></div>
{rows.length?<div className="twrap"><table className="t"><thead><tr><th>Component</th><th>Category</th><th>Computation</th><th>Income tax</th><th>Statutory</th><th>On payslip<Tip>Hidden components still count toward pay — on the payslip and its PDF they’re added into one “Other earnings” or “Other deductions” line.</Tip></th><th className="act"></th></tr></thead><tbody>
{rows.map(c=>{const m=CAT_META[c.cat],md=METHODS[c.method]||{l:'—',d:()=>'—'},taxed=c.cat==='Earning'||c.cat==='Reimbursement';return <tr key={c.code} className={'click'+(c.status==='Active'?'':' off')} onClick={()=>setForm(c)}>
<td><span className="t1">{c.name}{c.status!=='Active'&&<span className="chip" style={{marginLeft:8,height:22}}>Inactive</span>}</span><span className="t2 mono" style={{fontSize:11.5}}>{c.code}</span></td>
<td><span className="pill" style={tone(m.t)}><i></i>{m.one}</span></td>
<td><div className="calc"><span className="mth">{md.l}</span><span className="t1" style={{fontWeight:500}}>{md.d(c)}</span></div></td>
<td>{!taxed?<span className="no">—</span>:c.taxable==='Yes'?<span className="yes"><Icon name="check" size={15} stroke={2.6}/>Taxable</span>:c.taxable==='Partly'?<span className="ttag" style={tone('amber')}><i></i>Partly exempt</span>:<span className="ttag" style={tone('slate')}><i></i>Exempt</span>}</td>
<td>{c.stat?<span className="pill" style={tone('amber')}><Icon name="shield-check" size={13}/>Statutory</span>:<span className="no">—</span>}</td>
<td onClick={e=>e.stopPropagation()}><Switch sm on={c.payslip} label={'Show '+c.name+' on payslip'} onChange={x=>{update('components',L=>L.map(y=>y.code===c.code?Object.assign({},y,{payslip:x}):y));toast(c.name+(x?' now prints as its own line on payslips':' now prints inside “Other” on payslips'))}}/></td>
<td className="act"><Menu items={[{icon:'pencil',label:'Edit component',on:()=>setForm(c)},c.stat&&{icon:'shield-check',label:'Open statutory settings',on:()=>go('statutory')},'-',c.status==='Active'?{icon:'ban',label:'Deactivate',danger:true,soon:c.system,soonLabel:'Built-in',tip:'Built-in components are always on',on:()=>setOff(c)}:{icon:'check-circle',label:'Activate',on:()=>setSt(c,'Active')},!c.system&&{icon:'trash',label:'Delete component',danger:true,on:()=>setDel(c)}]}/></td></tr>})}
</tbody></table></div>:<Empty title="No components match" body="Try another name or code, or pick a different category." action={<button className="btn sm" onClick={()=>{setQ('');setCat('')}}>Show all</button>}/>}</div>
{form&&<ComponentForm c={form.code?form:null} init={form} onClose={()=>setForm(null)}/>}{del&&<Modal title={'Delete '+del.name+'?'} body="It’s removed for good. Components used on a salary structure or payslip can’t be deleted." icon="trash" t="red" danger cta="Delete" onClose={()=>setDel(null)} onOk={()=>{const c=del;setDel(null);update('components',L=>L.filter(x=>x.code!==c.code));toast(c.name+' deleted')}}/>}{off&&<Modal title={'Switch off '+off.name+'?'} body={(off.cat==='Deduction'?'Payroll stops deducting it':'Payroll stops paying it')+' from the next run, for everyone whose salary structure includes it. Runs already processed don’t change, and you can switch it back on at any time.'} icon="ban" t="amber" danger cta="Switch off" onClose={()=>setOff(null)} onOk={()=>{const c=off;setOff(null);setSt(c,'Inactive')}}/>}
</>
}
function ComponentForm({c,init,onClose}){
const {db,update,toast}=useApp();const isEdit=!!c;
const isPct=v=>/^pct_/.test(v.method||''),isAmt=v=>v.method==='fixed'||v.method==='capped';
const initial=Object.assign({cat:'Earning',method:'fixed',taxable:'Yes',payslip:true},init||{},c?{valPct:c.val,valAmt:c.val}:{},{active:c?c.status==='Active':true});
const sections=[
{title:'Component',fields:[{k:'name',label:'Name',req:true,placeholder:'e.g. Meal Allowance'},{k:'code',label:'Code',req:true,upper:true,placeholder:'e.g. MEAL',disabled:isEdit,hint:isEdit?'Codes can’t change once created':'Used in formulas and payroll exports',validate:x=>!isEdit&&db.components.some(y=>y.code===x)?'Code already in use':null},{k:'cat',label:'Category',type:'radio',span2:true,options:Object.entries(CAT_META).map(([k,m])=>({v:k,l:m.one,t:m.t})),clears:['method']}]},
{title:'Computation',fields:[{k:'method',label:'Calculated as',type:'select',req:true,span2:true,options:v=>(METHOD_BY_CAT[v.cat]||[]).map(k=>({v:k,l:METHODS[k].l}))},{k:'valPct',label:'Percentage',type:'number',req:true,min:0,max:100,step:0.25,suffix:'%',when:isPct},{k:'valAmt',label:'Amount per month',type:'number',min:0,prefix:'₹',when:isAmt,disabled:!!(c&&c.managed),hint:c&&c.managed?'Worked out by payroll — there’s no amount to set':'Optional. An earning is paid every month to everyone whose salary structure doesn’t list it (less for unpaid days). A deduction is taken in full from everyone unless their salary structure sets its own amount. Leave empty to set amounts on each salary structure.'},{k:'taxable',label:'Income tax',type:'seg',span2:true,when:v=>v.cat==='Earning'||v.cat==='Reimbursement',options:[{v:'Yes',l:'Taxable'},{v:'Partly',l:'Partly exempt',disabled:true},{v:'No',l:'Exempt'}]}]},
{title:'Display',fields:[{k:'payslip',label:'Show on payslip',type:'toggle',hint:'Off: it still counts toward pay, and prints inside one “Other earnings” or “Other deductions” line'},{k:'active',label:'Active',type:'toggle',disabled:!!(c&&c.system),hint:c&&c.system?'Built-in components are always on':'Off: payroll stops paying or deducting it from the next run. Runs already processed don’t change.'}]}];
const preview=v=>{const ex=1200000,basic=ex*0.5,gross=ex,val=isPct(v)?+v.valPct:+v.valAmt;const amt=v.method==='pct_ctc'?ex*val/100/12:v.method==='pct_basic'?basic*val/100/12:v.method==='pct_gross'?gross*val/100/12:isAmt(v)?val:null;const m=CAT_META[v.cat]||{};
const main=amt!=null&&!isNaN(amt)&&v[isPct(v)?'valPct':'valAmt']!==''&&v[isPct(v)?'valPct':'valAmt']!=null?(v.method==='capped'?'Up to ':'')+fmtINR(amt)+' / month':v.method==='balance'?'Whatever CTC is left after other earnings':v.method==='slab'?'Per the state slab — ₹200 / month in Karnataka':v.method==='variable'?'Set per employee on their salary record':v.method==='fixed'||v.method==='formula'?'Set on each salary structure':v.method==='statutory'?'Worked out from Statutory Settings':'Pick how it’s calculated';
return <div className="preview"><div className="preview-h">On a ₹12,00,000 CTC<span>Basic at 50% of gross</span></div><div className="ic-row"><span className="tile" style={tone(m.t)}><Icon name={m.icon||'rupee'} size={18}/></span><div><span className="t1">{main}</span><span className="t2">{v.cat==='Deduction'?'Withheld from gross pay':v.cat==='Employer'?'Added to CTC, not paid out':v.cat==='Reimbursement'?'Paid only against submitted bills':'Paid as part of gross salary'}</span></div></div>{c&&c.stat&&<div className="note" style={tone('amber')}><Icon name="shield-check" size={16}/><span>Statutory component — its rates also follow <b>Statutory Settings</b>.</span></div>}</div>};
const onSave=v=>{const val=isPct(v)?+v.valPct:isAmt(v)?+v.valAmt:undefined;const rec=Object.assign({stat:false},c||{},{code:v.code,name:v.name,cat:v.cat,method:v.method,val,taxable:v.cat==='Earning'||v.cat==='Reimbursement'?v.taxable:'No',payslip:!!v.payslip,status:v.active?'Active':'Inactive'});update('components',L=>isEdit?L.map(x=>x.code===c.code?rec:x):L.concat([rec]));toast(isEdit?rec.name+' saved':rec.name+' added to '+CAT_META[rec.cat].l.toLowerCase());onClose()};
return <RecordForm title={isEdit?c.name:'Add salary component'} sub={isEdit?c.code+' · changes apply from the next payroll run':'It becomes available to every salary structure.'} icon="rupee" t="violet" sections={sections} initial={initial} onSave={onSave} onClose={onClose} preview={preview} cta={isEdit?'Save component':'Add component'}/>
}
function StatutoryPage(){
const {db,update,toast,go}=useApp();
const toggle=s=>{const on=!s.on;update('statutory',L=>L.map(x=>x.id===s.id?Object.assign({},x,{on}):x));toast(s.name+(on?' switched on':' switched off'))};
return <>
<Hero title="Statutory Settings" sub="Registrations and contribution rules payroll applies by law. Switching a scheme off stops payroll working it out from the next run."/>
<div className="cgrid wide">{db.statutory.map(s=><div key={s.id} className={'ccard'+(s.on?'':' off')}>
<div className="cc-h"><span className="tile lg" style={tone(s.t)}><Icon name={s.icon} size={24}/></span><div className="cc-t"><b className="lg">{s.name}</b><small>Reg. no. <span className="mono">{s.reg}</span></small></div><Switch on={s.on} onChange={()=>toggle(s)} label={s.name}/></div>
<dl className="kv">{s.rows.map(([k,v])=><React.Fragment key={k}><dt>{k}</dt><dd>{v}</dd></React.Fragment>)}</dl>
<div className="cc-f"><span className="muted">Linked components</span><span className="sp"></span>{s.comps.length?<div className="chips">{s.comps.map(c=><span key={c} className="code" style={{cursor:'pointer'}} onClick={()=>go('components')}>{c}</span>)}</div>:s.on?<span className="muted" style={{fontWeight:500}}>{s.note}</span>:<span className="warn-link" onClick={()=>toggle(s)}><Icon name="alert-triangle" size={14}/>Switched off · Turn on</span>}</div>
</div>)}</div>
</>
}
function OverviewPage(){
const {db,d,go,counts,toast,nav,act}=useApp();const hc=d.headcount;
const onNotice=db.employees.filter(e=>e.status==='On notice').length;
const lic=db.agencies.filter(a=>a.status==='Active'&&daysTo(a.licence)<=30);
const noHead=db.depts.filter(x=>!x.head&&x.status==='Active');
const drafts=db.policies.filter(p=>p.status==='Draft').length;
const pending=db.policies.filter(p=>p.status==='Active'&&p.ackReq).reduce((s,p)=>s+Math.max(0,hc-p.ack),0);
const unfilled=db.desigs.filter(x=>!d.desig[x.id]).length;
const workers=db.agencies.filter(a=>a.status==='Active').reduce((s,a)=>s+a.workers,0);
const flags={
workforce:[onNotice&&{t:'orange',icon:'clock',txt:pl(onNotice,'employee','employees')+' serving notice',go:()=>go('employees',{status:'On notice'})},lic.length&&{t:'red',icon:'alert-triangle',txt:lic[0].name+' licence expires in '+daysTo(lic[0].licence)+' days',go:()=>go('contractors')}],
org:[noHead.length&&{t:'amber',icon:'user-plus',txt:pl(noHead.length,'department has','departments have')+' no head',go:()=>go('departments')},unfilled&&{t:'slate',icon:'award',txt:pl(unfilled,'designation is','designations are')+' unfilled',go:()=>go('designations')}],
rules:[drafts&&{t:'amber',icon:'file-text',txt:pl(drafts,'policy draft','policy drafts')+' not yet published',go:()=>go('policies')},pending&&{t:'blue',icon:'hourglass',txt:pl(pending,'policy acknowledgement','policy acknowledgements')+' pending',go:()=>go('policies')}],
payroll:db.statutory.filter(s=>!s.on).map(s=>({t:'slate',icon:'shield-check',txt:s.name+' is switched off',go:()=>go('statutory')}))};
const attention=Object.values(flags).flat().filter(x=>x&&x.t!=='slate').length;
return <>
<Hero title="Master data" sub="Everything the HR modules read from — people, structure, rules and pay. Keep it right here and attendance, leave and payroll stay right everywhere." actions={<><button className="btn" onClick={()=>act.importEmployees()}><Icon name="upload" size={17}/>Import data</button><button className="btn pri" onClick={()=>go('employees')}><Icon name="users" size={17}/>Open Employee Master</button></>}/>
<div className="stats">
<Stat icon="users" t="blue" label="People on roster" value={hc} sub={db.employees.length-hc+' exited records kept'} onClick={()=>go('employees')}/>
<Stat icon="hard-hat" t="orange" label="Contract workers" value={db.agencies.some(a=>a.workers!=null)?workers:'—'} sub={'Through '+pl(db.agencies.filter(a=>a.status==='Active').length,'agency','agencies')} onClick={()=>go('contractors')}/>
<Stat icon="map-pin" t="teal" label="Locations" value={db.branches.filter(b=>b.status==='Active').length} sub={'Across '+pl(db.companies.length,'company','companies')} onClick={()=>go('branches')}/>
<Stat icon={attention?'alert-triangle':'check-circle'} t={attention?'amber':'green'} label="Needs attention" value={attention} sub={attention?'Items flagged below':'Everything is in order'}/>
</div>
<div className="ov-grid">{nav.map(g=>{const f=flags[g.id].filter(Boolean);return <div key={g.id} className="ov"><div className="ov-h"><span className="tile lg" style={tone(g.t)}><Icon name={g.icon} size={24}/></span><div><h3>{g.l}</h3><p>{g.desc}</p></div></div>
<div className="ov-links">{g.items.map(it=><div key={it.id} className="ov-l" onClick={()=>go(it.id)}><span className="tile sm" style={tone(g.t)}><Icon name={it.icon} size={16}/></span>{it.l}<span className="n">{counts[it.id]}</span><Icon name="chevron-right" size={16} className="go"/></div>)}</div>
<div className="flags">{f.length?f.map((x,i)=><div key={i} className="flag click" style={tone(x.t)} onClick={x.go}><Icon name={x.icon} size={16}/>{x.txt}<Icon name="arrow-right" size={15} className="go"/></div>):<div className="flag" style={tone('green')}><Icon name="check-circle" size={16}/>Nothing needs attention</div>}</div></div>})}</div>
</>
}
const PAGES={overview:OverviewPage,employees:EmployeesPage,contractors:ContractorsPage,classes:ClassesPage,companies:CompaniesPage,branches:BranchesPage,departments:DepartmentsPage,designations:DesignationsPage,grades:GradesPage,shifts:ShiftsPage,leaves:LeavesPage,policies:PoliciesPage,components:ComponentsPage,statutory:StatutoryPage};
export { AppCtx, NAV, TopTabs, PAGES, Toasts, Icon, ICONS, Empty, tone, deriveDB, STATUS_TONE, TYPE_TONE }
