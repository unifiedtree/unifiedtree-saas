# Dev-only: persist the SDK access token in sessionStorage so it survives
# hot-reloads AND so Playwright E2E can inject it (see e2e/auth/setup.ts).
# Without this, `pnpm dev:platform` keeps the token in memory only and E2E
# tests land on the login page.
VITE_DEV_TOKEN_STORAGE=session
