{
  "name": "@unifiedtree/design-sync-entry",
  "version": "0.0.0",
  "private": true,
  "description": "design-sync entry: the one component namespace claude.ai/design builds with. Re-exports @unifiedtree/ui-kit (minus the three components the platform overrides) plus the platform's HRMS primitives. Not consumed by the app.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
